# Progress

## Phases

| Phase | Status | Summary |
| ----- | ------ | ------- |
| Phase 0 — Research and workspace bootstrap | `Complete` | StarRocks source research completed and written up in [`RESEARCH.md`](./RESEARCH.md), covering the mechanisms adopted, those deliberately rejected, and the reasoning for each. A ten-crate cargo workspace was bootstrapped with crate boundaries drawn deliberately along the two seams that matter: the frontend/backend split and the row-format/column-format storage split, so that both remain deployment and routing choices rather than rewrites. `htap-common` carries the shared MVCC `Version` domain and the `FencingToken` type, together with the common error enum — these are shared by every other crate and so were built first. `ci.sh` runs `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo build --workspace` and `cargo test --workspace`; all four stages are green on a clean rebuild. |
| Phase 1 — Row store | `Complete` | Built the LSM row-store engine (`htap-rowstore`) with WAL, memtable, immutable SSTs, manifest tracking, snapshot isolation, and point lookups. Covered by unit tests, integration tests, bounded property-based reference model tests, and process-level crash tests: `wal_recovery` (record framing, torn-tail repair, atomicity, GC), `wal_crash` (`kill_9_loses_no_committed_data`, `a_cleanly_exiting_child_recovers_every_commit`), `sst` (block index, Bloom filter, multi-block iter, CRC corruption detection), `engine` (`test_commit_and_snapshot_visibility`, `test_reopen_preserves_data_and_version`, `test_first_writer_wins_conflict`, `test_delete_resurrection_regression`, `test_auto_flush_and_manual_flush`, `test_flush_then_reopen_empty_wal`), `mvcc_properties` (`prop_mvcc_engine_matches_model` proving snapshot stability, no future visibility, delete correctness, first-writer-wins, and recovery equivalence), and `engine_crash` (`engine_kill_9_recovers_all_reported_commits`, `engine_cleanly_exiting_child_recovers_every_commit`). |
| Phase 2 — Column store | `Complete` | Built the columnar storage engine (`htap-colstore`) with immutable binary segment layout, plain and dictionary encodings, optional zstd compression, CRC32C frame and footer checksums, typed per-block zone maps (min/max/nullability), and vectorized scans with selective column decoding and conservative pushdown pruning. Covered by unit tests, segment roundtrip and corruption tests (`crates/htap-colstore/tests/segment_roundtrip.rs`), vectorized scan reference tests (`crates/htap-colstore/tests/scan.rs`), and deterministic zone-map pruning acceptance test (`crates/htap-colstore/tests/zone_map_skip.rs`). |
| Phase 3 — SQL layer | `Complete (local MVP)` | Completed local slice: sqlparser MySQL dialect parsing, strict catalog binder with typed `PointSelect` and `AnalyticSelect`, structural route classifier (`Route::RowstorePointRead`, `Route::RowstoreWrite`, `Route::CatalogDdl`, `Route::OlapScan`), and durable catalog-backed synchronous `LocalServer` executing across unpartitioned tables (SQL `CREATE TABLE`) and partitioned tables created via SQL DDL (`CREATE TABLE ... PARTITION BY RANGE/LIST`) or native non-SQL API `LocalServer::create_partitioned_table` (finite Range/List topology). Supports multi-row `INSERT` routing across partitions in one commit version/payload, complete-PK `DELETE` and `SELECT` routed by partition key (preserving rowstore fast path), and narrow analytical scans over logical rowstore and base-plus-delta rows across all partitions using server-root `<root>/colstore` for materialized `Column`/`Converting` partitions with projection-aware compact reads (PK+requested column union, single safe predicate-leaf SegmentReader pushdown, delta suppression/overlay, deterministic PK ordering, and full residual SQL filter/aggregate/group evaluation; internal `ScanStats`/pruning evidence; logical rows used in SQL, vectorized aggregation deferred; conservative finite range/list partition pruning, bounded in-process partition scan workers, and deterministic global merge/order are implemented for narrow local OLAP; distributed fanout, disk spilling, query cancellation, and resource quotas remain deferred) with reopen recovery and `EmbeddedClient` façade. SQL partitioning verified: MySQL `CREATE TABLE ... PARTITION BY RANGE [COLUMNS]` and `PARTITION BY LIST [COLUMNS]` (including `VALUES LESS THAN MAXVALUE`) are parsed via vendored `sqlparser` and bound to validated catalog partition models; manual/generic `partition_by` ASTs and unsupported partition options/subpartitions/expressions/multi-column COLUMNS are rejected with clear errors. Typed SQL partition lifecycle DDL (`ALTER TABLE <table> ADD/DROP/REORGANIZE PARTITION`) is supported for strict finite range/list forms and final MAXVALUE where supported, routed to `CatalogDdl` and validated under `alter_partitions_internal` with rowstore snapshot collapse empty safety gates (populated DROP/REORGANIZE rejected). Native `LocalServer::alter_partitions` API remains available with candidate catalog validation, atomic CAS, empty safety, and checked ID allocation without ID burn. Catalog enforces non-null PK partition keys, half-open `[lower, upper)` range bounds, exact list values, and validates overlaps/duplicates/types. Format conversion is guarded to single-partition tables for `convert_table`. Covered by tests in `crates/htap-server/tests/local_server.rs` (including `test_partitioned_native_range_topology_catalog_reopen_continuation`, `test_partitioned_native_list_topology_catalog_reopen_continuation`, `test_partitioned_boundary_unmatched_null_type_errors`, `test_partitioned_multi_row_insert_spanning_partitions_one_version_point_delete`, `test_partitioned_composite_pk_partition_key_not_first`, `test_partitioned_olap_across_partitions_and_empty_aggregate`, `test_partition_pruning_range_and_list_and_conservative_cases`, `test_scan_worker_count_equivalence`, `test_multi_partition_order_by_directions_nulls_and_tie_breaking`, `test_convert_table_multi_partition_guard`, `test_partitioned_empty_topology_rejection_no_catalog_mutation`, `test_server_sql_alter_partition_lifecycle`, `test_server_alter_partitions_drop_empty_and_populated_guard`, `test_server_alter_partitions_reorganize_empty_and_populated_guard`), `crates/htap-catalog/tests/catalog_recovery.rs` (`test_partitioning_legacy_decode_and_reopen`, `test_range_partitioning_routing_and_boundaries`, `test_list_partitioning_routing`, `test_partitioning_duplicate_violations`, `test_range_overlap_and_order_violations`, `test_partitioning_type_and_null_violations`, `test_partitioning_ownership_and_method_consistency`, `test_partitioning_cas_and_reopen_lifecycle`, `test_partition_alteration_add_range_and_list`, `test_partition_alteration_drop_range_and_list`, `test_partition_alteration_reorganize_contiguous`, `test_partition_alteration_cas_and_reopen`), `crates/htap-sql/tests/parse_bind.rs` (including `test_mysql_partition_ddl_parsed_and_bound`, `test_mysql_partition_ddl_negative_parser_and_binder`, `test_mysql_alter_partition_parsed_and_bound`, `test_mysql_alter_partition_negative`, and `test_negative_create_table`), `crates/htap-sql/tests/route.rs`, `crates/htap-client/tests/embedded_client.rs`, and `crates/htap-convert/tests/materialization.rs`. Explicitly deferred at the time (joins, CTEs, expressions, `ORDER BY` expressions/aliases, `LIMIT`/`OFFSET`, `HAVING`, `OR`/`NOT`/arithmetic/casts, `AVG`/`DISTINCT` aggregates, and non-PK DML (`UPDATE`, `DROP TABLE`) were delivered in Phase 9, see that row): populated partition reorganization data migration, physical data reclamation for dropped partitions, multi-partition movement, hash tablets, distributed serving/failover, window functions, compound AND pushdown beyond one leaf, `!=` pushdown, vectorized aggregation / operator pipelines, hash buckets / tablet sharding, multi-tablet/distributed scans, quotas/spill/cancellation, DataFusion/Arrow, full MySQL breadth, and non-partition `ALTER` (sessions/`BEGIN`/`COMMIT`/`ROLLBACK` were delivered in Phase 10, see that row). |
| Phase 4 — HTAP conversion | `Complete (local MVP)` | Built the local partition-scoped row-to-column conversion engine (`htap-convert`) with durable columnar manifest envelope (`HTAPTBM1`), deterministic four-phase state machine (`SnapshotPinned` -> `SegmentsWritten` -> `ReadyToPublish` -> `Column`), atomic per-tablet manifest and catalog publication, rowstore-authoritative base-plus-delta overlay via converter APIs (`read_column_partition` and projection-aware compact reads `read_column_partition_compact`, verified in `crates/htap-convert/tests/materialization.rs`) and server-root `<root>/colstore` analytical scans with direct SegmentReader pushdown optimization for the compact base path, and online point writes and point reads on converting and columnar partitions. Added deterministic table-wide conversion reports (`TableConversionReport`, `PartitionConversionReport`), Row->Column conversion per partition (`convert_table_to_column`), Column->Row metadata demotion (`convert_table_to_row`) retaining rowstore authority and leaving columnar files on disk, explicit synchronous conversion policy ticks (`conversion_tick`, `tick`), and fail-closed startup validation on reopen (`LocalServer::open`). Covered by tests in `crates/htap-convert/tests/tablet_manifest.rs`, `crates/htap-convert/tests/materialization.rs` (`test_demote_partition_to_row_clearing_manifest_and_retained_data`, `test_conversion_tick_resumes_snapshot_pinned`), `crates/htap-sql/tests/route.rs`, `crates/htap-server/tests/local_server.rs` (`test_server_convert_table_multi_partition_reports_and_demotion_equivalence`, `test_server_conversion_tick_idempotent_and_resume_snapshot_pinned`, `test_server_open_fail_closed_missing_or_corrupt_manifest`), and catalog recovery conversion tests (`crates/htap-catalog/tests/catalog_recovery.rs`: `test_conversion_metadata_and_manifest_roundtrip_and_reopen`, `test_invalid_conversion_combinations`, `test_invalid_column_manifest_and_paths`, `test_conversion_stale_cas`). Explicitly deferred: physical reverse data transcoding, physical rowstore/columnar reclamation, delete vectors, background compaction, autonomous background conversion scheduler, compound AND pushdown beyond one leaf, `!=` pushdown, vectorized aggregation / operator pipelines, and full multi-tablet/distributed partition/table conversion semantics. |
| Phase 5 — Data movement | `Complete (local MVP)` | Built single-node data movement engine (`htap-movement`) and server integration (`LocalServer::data_mover` / `LocalServerDataMover`). Supports durable job lifecycle tracking (`HTAPJOB1`), CSV and JSONLines import streaming, export partition materialization (exports materialize full logical partition before writing), batching and atomic commit replay, and tablet snapshot clone/verify/repair with catalog CAS reconciliation. Covered by tests in `crates/htap-movement/tests/jobs.rs`, `crates/htap-movement/tests/import_export.rs`, `crates/htap-movement/tests/tablet_simulation.rs`, and `crates/htap-server/tests/local_server.rs` (`test_server_data_mover_integration`, `test_server_data_mover_clone_verify_repair`). Distributed multi-node movement, SQL COPY grammar, and background replication are deferred. |
| Phase 6 — Distribution and coordination | `Complete (local MVP)` | Built local coordination, leadership fencing, deterministic placement, and replica activation MVP (`htap-coord`). Implemented synchronous `Coordinator` trait and durable `LocalCoordinator` backed by `HTAPCRD1` binary envelopes, sorted membership, strictly monotonic fencing tokens, and coordinator-fenced catalog CAS (`fenced_catalog_compare_and_set`). Implemented pure deterministic placement planner (`plan_placement`) and coordinator-fenced staging and local activation simulation (`stage_placement_addition`, `activate_placement_addition`, `activate_placement_plan`). Covered by integration tests in `crates/htap-coord/tests/local_coordinator.rs`, `crates/htap-coord/tests/placement_movement.rs`, movement tablet simulation tests in `crates/htap-movement/tests/tablet_simulation.rs`, and server/movement integration tests in `crates/htap-server/tests/local_server.rs`. Local same-root exclusive ProcessLock/cross-process contention rejection is implemented; explicitly deferred: direct `CatalogStore` CAS and older movement repair APIs bypass coordinator fence; no Raft/`openraft`, ZooKeeper backend, watches/locks/KV semantics, distributed/cross-node exclusion/consensus, remote physical movement, leader handoff, ongoing replication, capacity/rack placement, or live rebalance are implemented. |
| Phase 7 — Hardening, benchmarks, local MVP | `Complete (local evidence MVP)` | Built local microbenchmark suite (`htap-bench`, `benches/local_mvp.rs`) measuring rowstore point lookups (`rowstore/point_get_stable_snapshot`), colstore zone-map scans (`colstore/equality_zone_map_scan`, verifying 99% skip), conversion (`convert/local_converter_row_to_column`), CSV import (`movement/fixed_csv_import`), coordinator placement planning (`coord/placement_planning`), and coordinator leadership fenced CAS (`coord/leadership_fenced_cas`). Maintained synchronous in-process embedded client façade (`htap-client`, `EmbeddedClient`) covered by integration tests in `crates/htap-client/tests/embedded_client.rs`. Added root `README.md`, `docs/BENCHMARKS.md`, `docs/OPERATIONS.md`, and updated `ci.sh` with compile-only benchmark check (`cargo bench --workspace --no-run`). Deferred at the time: `htapd` daemon, MySQL wire protocol, network endpoints (delivered in Phase 8); Docker image/Compose, ZooKeeper/Raft backends, physical power-loss fsync testing, and TPC-C/TPC-H compliance remain deferred. |
| Phase 8 — Network server | `Complete (local MVP)` | Built `htap-wire`, a hand-written synchronous MySQL text-protocol server (`WireServer`, `WireServerConfig`) over `Arc<LocalServer>`: one accept thread plus one thread per connection (`std::net`, no async runtime), statements serialized by the server's own `execution_lock`, every statement auto-committed. Handshake v10 with `mysql_native_password` (auth-switch for other proposed plugins); `COM_QUERY` (text result sets), `COM_PING`, `COM_INIT_DB`, `COM_QUIT`; every other command (`COM_STMT_PREPARE`/`EXECUTE`, `COM_RESET_CONNECTION`, `COM_CHANGE_USER`, ...) answered with `ERR 1047`. Start-up compatibility shim (`SET` as no-op OK, `USE`, `SELECT 1`/`VERSION()`/`DATABASE()`/`@@sysvar[, ...]`). Result-set terminator always header `0xFE`; OK-packet `info` carries a private `version=<n>`/`version=none`/empty convention recovered by the client. Type mapping for `Bool`/`Int32`/`Int64`/`Float64`/`String`/`Bytes`/`Timestamp`; error mapping from `HtapError` categories to MySQL codes 1064/1146/1213/1235/1105 plus protocol codes 1045/1040/1049/1047. Payloads ≥16 MB close the connection. Built `htapd` (`crates/htapd`), a thin binary (`--root`, `--listen` default `127.0.0.1:3307`, `--max-connections` default 64, `--password`/`HTAPD_PASSWORD`) with no signal handler (storage recovers on next start per ADR-004/008/009) and a non-loopback bind warning. Built `htap-client::RemoteClient` (`connect`, `connect_with`, `execute`, `ping`, `server_version`, `close`) returning the same `StatementResult` as `EmbeddedClient`, with server errors mapped back by code. Security model: loopback bind by default, one implicit (unchecked) username, single shared password with no TLS (cleartext query/result traffic), non-cryptographic scramble RNG. Covered by unit tests in `crates/htap-wire/src/*.rs` (`sha1_empty_string`, `sha1_abc`, `sha1_multi_block`, `native_password_scramble_matches_test_vector`, `packet_round_trip`, `oversize_packet_rejected`, `lenenc_int_round_trip`, `lenenc_str_and_nul_str_round_trip`, `seq_counter_wraps_and_resets`, `read_fully_handles_partial_reads_and_stop_at_boundary_only`, `handshake_v10_round_trip`, `handshake_response41_round_trip_with_and_without_db`, `auth_switch_round_trip`, `error_map_all_htap_variants`, `err_packet_round_trip`, `shim_set_returns_ok`, `shim_version_comment_single_row`, `shim_multi_sysvar_with_aliases`, `shim_use_db`, `shim_passthrough_for_normal_sql`, `column_def_round_trip_all_types`, `text_row_null_and_bytes_are_raw_not_hex`, `text_row_numeric_and_timestamp_round_trip`, `datetime_text_conversions`, `command_ok_header_is_always_0x00_and_never_in_trans`, `terminator_header_is_always_0xfe_legacy_and_deprecated`, `config_defaults_are_loopback_only`, `command_info_convention`); `crates/htap-wire/tests/wire_server.rs` (23 tests including `test_handshake_empty_password_ok`, `test_handshake_wrong_password_rejected_1045`, `test_handshake_correct_password_ok`, `test_auth_switch_to_native_password`, `test_ssl_request_rejected_and_pre41_rejected`, `test_ddl_insert_point_select_round_trip`, `test_analytic_select_round_trip`, `test_typed_values_null_bytes_float_timestamp_round_trip`, `test_syntax_error_maps_to_1064`, `test_missing_table_maps_to_1146`, `test_too_many_connections_returns_1040`, `test_concurrent_connections_dense_versions`, `test_com_ping`, `test_com_init_db_known_and_unknown_db`, `test_shim_set_and_version_comment`, `test_shutdown_joins_and_frees_port`, `test_unknown_command_returns_1047`, `test_prepared_statement_command_rejected_cleanly`, `test_oversized_packet_closes_connection`, `test_legacy_eof_terminator_used_when_client_does_not_negotiate_deprecate_eof`, `test_resultset_packets_modern_vs_legacy`, `test_dml_versions_are_reported`, `test_mysql_crate_driver_interop` against a dev-only `mysql` crate v28 driver); and `crates/htap-client/tests/remote_client.rs::test_remote_client_matches_embedded_client_ddl_dml_select`, `crates/htap-client/src/remote.rs::decode_command_convention`. Deferred at the time: TLS, compression, prepared statements/binary protocol, multi-statements/multi-results, per-user ACL/RBAC, Docker packaging, and a selectable single-role `htapd` mode; see ADR-016 (sessions/`BEGIN`/`COMMIT`/`ROLLBACK` were delivered in Phase 10, see that row; prepared statements/binary protocol, `COM_RESET_CONNECTION`/`COM_CHANGE_USER`, ≥16 MiB messages, CSPRNG scramble, and multi-statements were delivered in Phase 11, see that row). |
| Phase 9 — SQL breadth and cross-engine joins | `Complete (local MVP)` | Built a general query executor (`htap-sql::{query, expr, binder_query}` binding `BoundStatement::Query(BoundQuery)`; `htap-server::query_exec` executing it via new `Route::Query`) supporting `INNER`/`LEFT`/`RIGHT`/`CROSS` joins (left-deep chains, comma joins), table aliases, qualified names, `*`/`t.*`, arithmetic (`+ - * / %`, `/` always `Float64`, checked integer overflow), comparisons (incl. column-vs-column, literal-on-left), `AND`/`OR`/`NOT`, `IS [NOT] NULL`/`TRUE`/`FALSE`, `LIKE`, `IN (list)`, `BETWEEN`, `CASE` (simple/searched, lazy), `CAST`, scalar functions (`UPPER`/`LOWER`/`LENGTH`/`CHAR_LENGTH`/`CONCAT`/`ABS`/`COALESCE`/`IFNULL`/`NULLIF`), aggregates (`COUNT`/`SUM`/`AVG`/`MIN`/`MAX` with `DISTINCT`), `GROUP BY`/`HAVING`, `SELECT DISTINCT`, `ORDER BY` (expressions/aliases/ordinals, `NULLS FIRST`/`LAST`), `LIMIT`/`OFFSET` (incl. MySQL `LIMIT off, cnt`), `UNION`/`UNION ALL` with numeric widening, derived tables, non-recursive `WITH` CTEs, and uncorrelated scalar/`IN`/`EXISTS` subqueries. Every base table side of a join is materialized through the existing `scan_partition_compact` path (rowstore for `Row`, columnar+delta overlay for `Column`/manifest-bearing `Converting`, rowstore fallback for `SnapshotPinned` `Converting`) at **one** MVCC snapshot per statement, then hash-joined in memory with per-slot partition pruning and single-leaf predicate pushdown (residual on the outer-join null-supplying side). A purely syntactic shape gate (`is_narrow_select_shape` in `crates/htap-sql/src/binder.rs`) preserves R5 structurally: complete-PK `SELECT` still binds `PointSelect` -> `Route::RowstorePointRead`, narrow single-table scans still bind `AnalyticSelect` -> `Route::OlapScan` unchanged, everything else routes to `Route::Query`. Added `UPDATE t [alias] SET ... [WHERE ...]` (`Route::RowstoreUpdate`: complete-PK point read-modify-write + one `Mutation::Put`, or scan-all-partitions-at-one-snapshot + one transaction for all rewrites, inheriting the 2PC transaction's effective payload cap (nominally 16 MiB, actually about 4 MiB — see Phase 10 row and `docs/LIMITATIONS.md`) with no chunking; PK/partition-key assignment, subqueries, and `UPDATE ... FROM`/`JOIN`/`ORDER BY`/`LIMIT` rejected), `DROP TABLE [IF EXISTS] t` (`Route::CatalogDdl`, one catalog CAS; at the time, rowstore/columnar data of dropped tablets was not physically reclaimed — Phase 15 later made that same CAS mark the dropped tablets `pending_reclaim` and added `compaction_tick`/`reclaim_tick` to physically reclaim them, see that row; refuses while any partition is `Converting`), and `SHOW TABLES [LIKE p]`/`SHOW DATABASES`/`SHOW COLUMNS FROM t`/`DESCRIBE t`/`DESC t` (`Route::CatalogRead`, answered from the catalog snapshot only). To guarantee dropped identifiers are never reissued, `CatalogSnapshot.id_high_water: IdHighWater { table, partition, tablet, replica }` (`crates/htap-catalog/src/model.rs`) is now persisted and every allocator draws from `max(persisted, live max) + 1`; the `HTAPCAT1` catalog envelope format version bumped 1 -> 2 (`crates/htap-catalog/src/local.rs`), a version-1 catalog still decodes (counters fall back to the live maximum) and is rewritten as version 2 on the next CAS, and a version-1-only binary refuses to open a version-2 catalog (and a version-2 payload missing `id_high_water` is rejected as corruption). `LocalServer::open` runs a one-time legacy migration (`migrate_legacy_id_high_water`) that seeds the tablet counter from the `colstore/` on-disk inventory (needed because a v1-era `ALTER TABLE ... DROP PARTITION` could leave tablet directories the live catalog maximum cannot see), `LocalCatalogStore::compare_and_set` rejects any successor whose high-water mark would regress, and `htap-coord::plan_placement`/`stage_placement_addition` allocate and raise replica ids from the same persisted mark (a `ReplicaId` names a movement package directory). Known gap: the legacy migration does not recover replica ids removed the same way; see `docs/LIMITATIONS.md`. Covered by `crates/htap-sql/src/expr.rs` unit tests (`three_valued_logic_tables`, `numeric_promotion_and_overflow`, `like_in_between_case_cast`, `scalar_functions`, `subquery_and_aggregate_context`, `expr_type_inference`), `crates/htap-sql/tests/query_bind.rs` (`test_join_binding_kinds_aliases_and_wildcards`, `test_join_binding_errors`, `test_expressions_functions_and_type_checks`, `test_aggregates_group_by_having_and_grouping_rules`, `test_order_by_limit_distinct`, `test_subqueries_ctes_derived_tables_and_union`, `test_update_drop_show_binding`, `test_bound_predicate_evaluation_with_joined_rows`), `crates/htap-sql/tests/route.rs` (`test_route_classification`, `test_point_read_fast_path_pinned_against_general_query_path`), `crates/htap-sql/tests/parse_bind.rs` (`test_negative_select_and_delete`, `test_bind_analytic_select_negative`), `crates/htap-server/tests/query_exec.rs` (`test_joins_across_row_column_and_converting_tables`, `test_outer_joins_null_padding_residual_on_and_null_keys`, `test_expressions_aggregates_having_order_limit_distinct`, `test_union_derived_tables_ctes_and_subqueries`, `test_partition_pruning_and_pushdown_through_general_path`, `test_single_snapshot_across_engines_and_freshness`, `test_general_query_over_reopened_server`, `test_update_by_primary_key_and_reopen_recovery`, `test_update_by_filter_across_partitions_and_storage_formats_with_reopen`, `test_update_by_primary_key_on_column_and_converting_partitions`, `test_show_tables_databases_columns_and_describe`, `test_drop_table_reopen_and_no_id_reuse`, `test_legacy_catalog_seeds_tablet_high_water_from_colstore_inventory`), `crates/htap-server/tests/local_server.rs` (`test_analytic_unsupported_clauses`, `test_storage_descriptors_dml_and_point_reads_and_unsupported_non_point`), `crates/htap-catalog/tests/catalog_recovery.rs` (`test_catalog_v1_envelope_decodes_and_counters_fall_back_to_live_max`, `test_catalog_id_high_water_prevents_reuse_after_removal`, `test_catalog_cas_rejects_regressing_id_high_water`, `test_catalog_v2_payload_without_id_high_water_is_rejected`, `test_corruption_and_truncation`), `crates/htap-coord/tests/placement_movement.rs::test_plan_placement_allocates_above_id_high_water`, `crates/htap-client/tests/embedded_client.rs::test_embedded_client_unsupported_sql_preserves_error_categories`, `crates/htap-client/tests/remote_client.rs::test_remote_client_matches_embedded_client_ddl_dml_select`, and `crates/htap-wire/tests/wire_server.rs::test_general_sql_over_wire` (same SQL breadth reachable unchanged over the MySQL wire protocol). Deferred at the time (window functions, correlated subqueries, `FULL OUTER`/`NATURAL`/`USING` joins, parenthesized nested join trees, recursive CTEs, `EXCEPT`/`INTERSECT`, `GROUP BY` ordinals, `INSERT ... SELECT`, filtered `DELETE`, `TRUNCATE`, and integer `DIV` were delivered in Phase 13; cost-based optimization, spilling, and bounded parallelism were delivered in Phase 14 — see those rows): `LIMIT BY`, `UPDATE` with joins/subqueries, non-partition `ALTER`, vectorized/pipelined execution, worker-pool parallelism and memory bounds/spilling for `LEFT`/`RIGHT`/`FULL` and non-equi/`CROSS` joins, semi-join rewrites of `IN`/`EXISTS`, broader string/date function coverage, and MySQL implicit string<->number coercion (physical reclamation on `DROP TABLE` was delivered in Phase 15, see that row). |
| Phase 10 — Sessions and explicit transactions | `Complete (local MVP)` | Built `htap-server::session::Session` (`LocalServer::open_session`, `EmbeddedClient::open_session`; one `htap-wire` connection = one session for its lifetime, rolled back on disconnect) implementing `BEGIN`/`START TRANSACTION [READ ONLY \| READ WRITE \| WITH CONSISTENT SNAPSHOT]`, `COMMIT`, `ROLLBACK`, `SET autocommit = 0\|1\|ON\|OFF\|TRUE\|FALSE`, `SET @x = expr[, ...]` (evaluated against this session's own variables), `SET [SESSION] TRANSACTION ISOLATION LEVEL REPEATABLE READ` (only level accepted) and `READ ONLY`/`READ WRITE` (applies to the next transaction only, even with `SESSION`, because the parser AST doesn't distinguish the two forms), `SET NAMES`/known read-only variables as no-ops, `GLOBAL` scope rejected, and `@x`/`@@var` in expressions everywhere including zero-table `SELECT`. Added `htap-sql::variables` (`system_variable_value`, `SessionVarsView`, `classify_set_target`, `validate_isolation_level`, `parse_autocommit_value`) as the single system-variable registry, replacing the ad hoc fakes in `htap_wire::shim::system_variable`; the wire shim shrank to `USE`/`SELECT 1`/`VERSION()`/`DATABASE()`/`SCHEMA()` and the two `SET CHARACTER SET`/`SET CHARSET` forms `vendor/sqlparser` cannot parse at all. Uncommitted writes buffer in a session's own `WriteSet` (never touching WAL/journal/memtable — a crash is an implicit rollback) with an incremental effective payload cap (nominally `MAX_PAYLOAD_SIZE` = 16 MiB of raw mutation JSON, actually about 4 MiB once the journal `Intent` frame's own JSON-number-array payload encoding is accounted for — `intent_frame_size_bound`, see `docs/LIMITATIONS.md`) and read-your-own-writes overlay (`crate::session::overlay_rows`) below relational operators for point reads, narrow scans, the general executor, and `UPDATE`, across `Row`/`Column`/`Converting` partitions; `COMMIT` runs the existing 2PC path once via `TransactionManager::commit` (never `commit_request`) against the transaction's own pinned snapshot. Snapshot isolation, first-writer-wins, write skew permitted, reported as `REPEATABLE READ`; a write-write conflict at `COMMIT` and a stale snapshot vs. a columnar base published mid-transaction both return `Conflict` and poison the transaction; commit-time catalog revalidation catches a concurrent `DROP TABLE`/partition `ALTER`; DDL inside any open transaction is rejected without poisoning it; `BEGIN` while active implicitly commits the previous transaction; a `DurablePending` commit outcome moves the session to a quarantined state rejecting every further statement (including `ROLLBACK`) with the original non-retryable error (MySQL 1105, never 1213). Fixed two correctness gaps: `Engine::prepare` now runs the first-writer-wins check before any journal write (previously only in `apply_prepared_locked`, after the `Commit` record was fsynced), and an autocommit write now commits against its own read snapshot rather than a fresh one assigned by `commit_request`'s `begin()` (previously could lose a race against a concurrent `copy_from_*`/import). Added `TransactionManager::recovery_required`/a manager-wide `RecoveryLatch` (recording a `RecoveryCause` of `ParticipantIo` or `JournalIo`) that rejects every *other* `commit` call once any commit has returned `DurablePending`, with a distinct non-retryable `HtapError::RecoveryRequired` (never the blocking transaction's own `DurablePending`, never MySQL 1213), until `recover()` conclusively resolves it in-process (only possible for a `ParticipantIo` latch) or the manager reopens (always clears it). A follow-up fix pass hardened `recover()` further: it now fsyncs the journal before replaying any commit into a participant, cross-checks each registered participant's own `committed_version()` against the journal's replayed max version (failing as `HtapError::Corruption` on any mismatch), restores `next_txn_id` via `fetch_max` (never regressing it), and truncates a failed journal append back to its pre-append offset. It also moved the applied-external-transactions ledger capacity check into `Engine::prepare` (before any journal write, not just at apply, rejecting with `HtapError::InvalidArgument`). A third fix pass: gave `Journal` its own `poisoned` state (set on a failed `fsync` in `append`/`sync`, on a truncate-after-failed-write that itself fails, or on an append that wrote bytes but then failed to fsync) that rejects every further `append`/`append_nosync`/`sync` until a fresh reopen; made a failed `Intent` or `Abort` journal write latch the manager as `RecoveryCause::JournalIo` exactly like a `Commit`-boundary failure already did (only for a real I/O error, never an oversize-frame rejection); made `recover()` check up front for an existing `JournalIo` latch or a poisoned journal and, if either holds, return `RecoveryRequired` immediately without attempting any replay (a `JournalIo` latch is therefore cleared only by reopening, never by calling `recover()` again in-process); and scaled `intent_frame_size_bound`'s conservative overhead by participant count (128 bytes/participant, on top of the existing 512-byte fixed overhead), which lowered the effective single-participant payload cap slightly (see `docs/LIMITATIONS.md` for the new byte figure). `LocalServer::execution_lock` is held per statement and per `COMMIT`, not across a whole transaction. Covered by `crates/htap-server/tests/session.rs` (38 tests, including `test_session_id_unique_and_monotonic`, `test_drop_rolls_back_open_transaction`, `test_write_set_payload_cap_enforced_incrementally_per_statement`, `test_read_your_own_writes_point_select`, `test_read_your_own_writes_analytic_scan`, `test_read_your_own_writes_general_query_join`, `test_double_update_in_one_transaction_composes`, `test_insert_then_delete_in_one_transaction_nets_to_nothing`, `test_statement_failure_does_not_pollute_write_set`, `test_commit_flushes_buffered_writes_as_one_version`, `test_commit_write_write_conflict_returns_clean_conflict_and_poisons_session`, `test_commit_catalog_conflict_on_concurrent_drop_table`, `test_stale_snapshot_vs_conversion_returns_conflict_and_poisons_session`, `test_read_only_transaction_commit_skips_2pc`, `test_rollback_after_poison_succeeds`, `test_durable_pending_from_commit_leaves_session_in_outcome_pending_state`, `test_autocommit_zero_starts_implicit_transaction`, `test_begin_while_active_implicitly_commits_previous`, `test_begin_implicit_commit_failure_does_not_start_new_txn`, `test_ddl_rejected_inside_explicit_transaction_and_txn_survives`, `test_set_user_variable_and_select_it_back`, `test_set_autocommit_variable_matches_state`, `test_set_transaction_isolation_level_rejects_unsupported_level`, `test_set_transaction_read_only_rejects_write`, `test_connector_startup_set_statements_accepted`, `test_commit_while_manager_latched_by_other_session_keeps_txn_open`, `test_commit_of_write_set_exceeding_intent_frame_is_rejected_and_txn_stays_open`, `test_autocommit_oversize_insert_rejected_cleanly_before_any_journal_write`), `crates/htap-server/tests/session_concurrency.rs` (`test_concurrent_sessions_write_write_conflict_first_committer_wins`, `test_r5_point_read_still_bypasses_analytics_inside_and_outside_transaction`, `test_autocommit_update_conflicts_with_concurrent_import_instead_of_losing_it`), `crates/htap-server/tests/session_recovery.rs` (`test_uncommitted_writes_never_visible_after_reopen`, `test_committed_transaction_visible_after_reopen`, `test_reopen_after_session_commit_durable_pending_resolves_outcome`), `crates/htap-txn/tests/two_phase_commit.rs` (`test_conflict_detected_before_journal_commit_not_after`, `test_stale_snapshot_non_conflicting_key_still_commits`, `test_commit_after_durable_pending_is_rejected_until_recovery`, `test_latch_via_commit_sync_hook_survives_in_process_recover_until_reopen`, `test_latch_via_participant_apply_failure_clears_in_process_recover`, `test_ledger_full_commit_rejected_at_prepare_before_journal_growth`, `test_recover_detects_engine_ahead_of_journal_as_corruption`, `test_in_process_recover_under_journal_io_latch_is_rejected_and_applies_nothing`), `crates/htap-txn/tests/journal.rs` (`test_append_nosync_failure_truncates_partial_write_and_journal_stays_openable`, `test_append_sync_failure_then_shorter_frame_reopen_succeeds_or_is_rejected`, `test_truncate_failure_poisons_journal`, `test_intent_append_failure_latches_manager_as_journal_io`, `test_abort_append_failure_latches_manager_as_journal_io`), `crates/htap-rowstore/tests/engine.rs::test_first_writer_wins_conflict` (unchanged, now also exercised via `prepare`), `crates/htap-sql/tests/query_bind.rs` (`test_user_and_system_variable_binding`, `test_global_scope_rejected`), `crates/htap-sql/tests/route.rs::test_narrow_shape_gate_excludes_variables`, `crates/htap-sql/src/variables.rs` unit tests (`constant_variables_and_aliases_resolve`, `dynamic_variables_read_session_state`, `classify_set_target_dynamic_readonly_unknown_and_global`, `isolation_level_validation`, `autocommit_value_parsing`), `crates/htap-client/tests/session.rs` (`test_embedded_session_begin_commit_rollback`, `test_embedded_autocommit_execute_unaffected_by_open_session_on_same_server`), `crates/htap-wire/tests/wire_server.rs` (`test_wire_begin_commit_rollback_round_trip`, `test_wire_rollback_on_disconnect`, `test_wire_concurrent_sessions_conflict_returns_1213`, `test_wire_set_autocommit_and_user_variable_round_trip`, `test_wire_sysvar_reads_now_reflect_session_state`), `crates/htap-wire/src/error_map.rs::durable_pending_never_maps_to_the_retryable_conflict_code`, `crates/htap-wire/src/error_map.rs::recovery_required_never_maps_to_the_retryable_conflict_code`, and `crates/htap-wire/src/shim.rs` unit tests (`shim_charset_set_forms_return_ok`, `shim_no_longer_answers_plain_set_or_sysvar_reads`). See "Sessions and explicit transactions (Phase 10)" in `docs/ARCHITECTURE.md` and ADR-018. Deferred at the time: `SELECT ... FOR UPDATE`/locking reads, prepared statements/binary protocol, savepoints, XA, IPC/multiprocess sessions, per-user ACL, idle-transaction timeout/reaping, MVCC garbage collection, and a grace period before a new columnar base dooms an open transaction (prepared statements/binary protocol delivered in Phase 11, see that row). |
| Phase 11 — MySQL binary protocol, prepared statements, connection commands | `Complete (local MVP)` | Built the MySQL binary protocol and prepared statements (`htap-wire::{binary_codec, prepared}`, `htap-sql::prepare`): `COM_STMT_PREPARE`/`EXECUTE`/`CLOSE`/`RESET`/`SEND_LONG_DATA` (`COM_STMT_FETCH` cleanly rejected as unsupported server-side cursors). Parameterization is AST-level placeholder substitution (`count_placeholders`/`substitute_placeholders` sharing one recursive walk over every literal-bearing position the binder accepts — INSERT VALUES, UPDATE SET/WHERE, DELETE WHERE, SELECT projection/WHERE/HAVING/GROUP BY/ORDER BY/JOIN ON, subquery bodies, derived tables, CTE bodies, UNION branches, `LIMIT`/`OFFSET`), never a text re-render/reparse; `checked_placeholder_count` cross-checks the walk against a raw tokenizer scan of the SQL text and rejects a `?` in an unsupported position as `HtapError::Unsupported` rather than under-substituting. `PREPARE` response metadata (`resolve_prepare_output_schema`) is best-effort: the real output schema only when every placeholder's type is inferable from local context, probed through the real, unmodified binder; otherwise `num_columns = 0` and generic parameter definitions. A per-statement bound-parameter-type cache backs `new_params_bound_flag = 0` re-executes (amendment A2). `binary_codec::decode_execute` decodes the full binary parameter type matrix except `TIME` (`TINY`/`SHORT`/`INT24`/`YEAR`/`LONG`/`LONGLONG`/`FLOAT`/`DOUBLE`/`NEWDECIMAL`/`DECIMAL`-as-validated-text/`VARCHAR`/`VAR_STRING`/`STRING`/`ENUM`/`SET`/blob family/`DATE`/`DATETIME`/`TIMESTAMP`/`NULL`); unsigned 64-bit values above `i64::MAX` are rejected cleanly (no `UInt64` value type in the engine, documented as a permanent limitation). A narrow binder coercion (amendment A3) accepts a string literal for a `BYTES` column and the integer literals `0`/`1` for a `BOOL` column in INSERT/UPDATE/WHERE, resolving the wire-level ambiguity between a bound `String` and a bound `Vec<u8>` (both arrive as the same wire type). Added `COM_RESET_CONNECTION` and `COM_CHANGE_USER`, both routed through `htap_server::Session::reset()` and respecting the ADR-018 `CommitOutcomePending` quarantine (a quarantined session's reset/change-user request returns the stored error, changing nothing); `COM_CHANGE_USER` re-authenticates via a new `verify_credentials` seam (Phase 12 per-user-ACL hook) and closes the connection on failure. Messages of 16 MiB or more are now reassembled/split (`codec::read_message_with_stop`/`write_message`) rather than closing the connection, bounded by a real, configurable `WireServerConfig::max_allowed_packet` (default 64 MiB, `htapd --max-allowed-packet`/`HTAPD_MAX_ALLOWED_PACKET`, reported dynamically as `@@max_allowed_packet`); an oversize message is rejected with `ER_NET_PACKET_TOO_LARGE`/1153 and the connection closed. The handshake scramble now comes from the OS CSPRNG (`getrandom::fill`, no fallback) instead of a seeded xorshift generator. `CLIENT_MULTI_STATEMENTS`/`CLIENT_MULTI_RESULTS` are advertised but honored only when negotiated: a batch (`htap_sql::parse_many`) runs sequentially through `Session::execute_statement` with `SERVER_MORE_RESULTS_EXISTS` set on every result but the last, stopping at the first error (including `DurablePending`/`RecoveryRequired`); `COM_STMT_PREPARE` rejects multi-statement text even when negotiated. `WireServer` shutdown now force-closes connections blocked mid-packet via a `live_connections` registry of `try_clone`d streams with RAII unregistration. `RemoteClient`/`WireClient` gained `prepare`/`execute_prepared`/`close_prepared`/`close_stmt` and `query_multi`. See "Prepared statements and binary protocol (Phase 11)" and the "Network layer" section in `docs/ARCHITECTURE.md`, ADR-019, and "Prepared statements and binary protocol scope and deferred features (Phase 11)" in `docs/LIMITATIONS.md`. Covered by `crates/htap-sql/tests/prepare.rs` (21 tests: `test_supported_shapes_count_agrees_and_substitution_matches_literal_sql`, `test_no_supported_shape_ever_mismatches_tokenizer_count`, `test_placeholder_in_unsupported_position_returns_unsupported_error`, `test_negative_numbers_and_i64_min`, `test_f64_edge_values`, `test_non_finite_float_is_rejected`, `test_null_values`, `test_bytes_values`, `test_strings_with_quotes_backslashes_and_unicode`, `test_resolve_prepare_output_schema_point_select`, `test_resolve_prepare_output_schema_filter`, `test_resolve_prepare_output_schema_general_query_path_filter`, `test_resolve_prepare_output_schema_joined_filter_is_none`, `test_resolve_prepare_output_schema_unresolvable_placeholder_type_is_none`, `test_resolve_prepare_output_schema_dml_is_empty`, `test_resolve_prepare_output_schema_show_and_ddl_are_none`, `test_numeric_text_decimal_round_trips_exactly_into_bigint_column`, `test_numeric_text_negative_decimal_with_fraction_round_trips`, `test_numeric_text_validates_strictly`, `test_case_in_order_by_resolves_output_schema_without_error`, `test_hint_count_matches_placeholder_count_for_every_supported_shape`), `crates/htap-wire/src/binary_codec.rs` unit tests (full parameter type matrix round trip, NULL-bitmap offsets 0/2, `new_params_bound_flag` caching, unsigned `LONGLONG` boundary, `TIME`/unknown-type rejection, invalid `DECIMAL` text, `VAR_STRING` UTF-8/non-UTF-8 decoding, binary row round trip for every result column type, `decode_binary_datetime_rejects_invalid_calendar_fields`, `decode_binary_datetime_zero_month_and_day_together_is_the_sentinel`, `encode_binary_datetime_rejects_year_outside_0_to_9999`, `encode_binary_row_propagates_out_of_range_timestamp_error`, `encode_execute_request_propagates_out_of_range_timestamp_error`), `crates/htap-wire/tests/fuzz_decode.rs` (`fuzz_decode_execute_never_panics`, `fuzz_change_user_request_decode_never_panics`, `fuzz_handshake_response41_decode_never_panics`, `fuzz_decode_binary_row_never_panics`; a deterministic 20k-iteration fuzz of the binary decoders), `crates/htap-wire/src/prepared.rs` unit tests (registry insert/close/reset, statement cap, long-data accumulation/clearing/poisoning, `insert_skips_ids_still_in_use_when_next_id_wraps`), `crates/htap-wire/src/codec.rs` unit tests (`test_message_reassembly_at_exact_boundary_with_trailing_empty_packet`, `test_message_reassembly_rejects_over_max_allowed_packet_before_full_read`, `test_message_reassembly_rejects_sequence_id_mismatch_across_chunks`, `test_write_message_splits_exact_multiple_and_non_multiple`, `write_message_and_read_message_sequence_ids_wrap_at_256`), `crates/htap-wire/src/result_codec.rs` unit tests (`command_ok_reports_the_status_it_is_given`, `more_results_flag_sets_server_more_results_exists`), `crates/htap-wire/src/server.rs::connection_guard_decrements_count_and_unregisters_on_panic_unwind`, `crates/htap-wire/src/handshake.rs::scramble_is_printable_and_varies`, `crates/htap-wire/src/shim.rs::shim_never_matches_multi_statement_text`, `crates/htap-sql/src/variables.rs::max_allowed_packet_reads_dynamically_but_set_stays_a_no_op`, `crates/htap-sql/tests/query_bind.rs::test_where_bytes_column_compared_against_string_literal_coerces`, `crates/htap-server/tests/session.rs` (`test_reset_clears_state`, `test_reset_while_outcome_pending_is_rejected_without_mutation`, `test_execute_and_execute_statement_are_equivalent`, `test_execute_statement_respects_commit_outcome_pending`), and `crates/htap-wire/tests/wire_server.rs` (58 tests total, including `test_prepared_statement_unsupported_kinds_rejected`, `test_prepared_statement_unknown_id_and_close_and_reset`, `test_prepared_statement_send_long_data`, `test_prepared_statement_param_type_cache_new_params_bound_zero`, `test_prepared_statement_in_transaction_and_commit_outcome_pending`, `test_prepared_statements_mysql_crate_interop_all_types`, `test_prepare_placeholder_in_limit_and_subquery`, `test_wire_reset_connection_clears_state_and_prepared_statements`, `test_wire_reset_connection_while_commit_outcome_pending_stays_quarantined`, `test_wire_quit_allowed_while_commit_outcome_pending`, `test_wire_change_user_reauth_and_reset`, `test_wire_change_user_wrong_password_closes_connection`, `test_wire_change_user_while_commit_outcome_pending_stays_quarantined`, `test_wire_change_user_reuses_switched_scramble_on_later_change_user`, `test_wire_large_payload_over_16mb_round_trip`, `test_wire_max_allowed_packet_rejects_oversize_query_and_closes_connection`, `test_max_allowed_packet_variable_reflects_wire_config`, `test_wire_multi_statements_sequential_execution_and_more_results_flag`, `test_wire_multi_statements_stops_on_first_error`, `test_wire_multi_statements_stops_on_durable_pending`, `test_wire_multi_statements_rejected_without_capability`, `test_wire_prepare_rejects_multi_statement_text_even_when_negotiated`, `test_wire_prepare_and_execute_case_in_order_by`, `test_wire_prepared_decimal_param_round_trips_exactly_into_bigint_column`, `test_wire_status_flags_reflect_autocommit_and_transaction_state`, `test_pre_auth_oversize_handshake_rejected_before_allocation`, `test_shutdown_force_closes_connection_blocked_mid_packet`, `test_shutdown_force_close_rolls_back_open_transaction`, `test_wire_mysql_connector_startup_still_works`, `test_shim_charset_set_forms_over_wire`), and `crates/htap-client/tests/prepared.rs` (`test_remote_prepared_statement_matches_embedded_literal_execution`, `test_remote_prepared_statement_close_then_execute_errors`). Deferred/permanent gaps at the time: `COM_STMT_FETCH`/server-side cursors, unsigned 64-bit values above `i64::MAX`, exact `DECIMAL`, `TIME`-typed parameters, best-effort (not exhaustive) `PREPARE` metadata, and shim-only forms not usable inside a multi-statement batch. TLS, compression, and per-user ACL delivered in Phase 12 (see that row); the engine's own bounded `DECIMAL` type, including a decimal result column over both wire protocols, delivered in Phase 17 (see that row) — only an arbitrary-precision wire representation beyond the engine's own 18-digit maximum remains unsupported. |
| Phase 12 — TLS, compression, and per-user accounts | `Complete (local MVP)` | Built TLS (`htap-wire::tls`, `rustls` 0.23 with the `ring` crypto provider — `aws-lc-rs` needs `cmake`, confirmed unavailable in this environment): `Conn` enum (`Plain`/`Tls`) replacing bare `TcpStream` on every connection function; `CLIENT_SSL` advertised only when `WireServerConfig::tls` is configured; a strict 32-byte `SSLRequest` decode (`decode_ssl_request`) followed by a real second `HandshakeResponse41` read over the upgraded stream, trusting only the `CLIENT_SSL` bit from the pre-TLS request; `htapd --tls-cert`/`--tls-key`/`--require-secure-transport` and `HTAPD_TLS_CERT`/`HTAPD_TLS_KEY`/`HTAPD_REQUIRE_SECURE_TRANSPORT`; `require_secure_transport` rejects a plaintext login before credentials are checked; cert/key match validated at load (`CertifiedKey::keys_match()`) and `WireServer::reload_tls_certs()` swaps a live `ReloadableCertResolver`'s certificate for future handshakes only, keeping the old cert on a failed reload and leaving existing connections untouched (no `SIGHUP` trigger); client-side `TlsMode::{Disabled, Required{ca_cert, server_name}, InsecureSkipVerifyDoNotUseInProduction}` with no silent plaintext fallback. Built MySQL compressed-packet framing (`htap-wire::compression::CompressedStream`, layered `Conn -> CompressedStream<Conn> -> codec.rs`): `CLIENT_COMPRESS`/`CLIENT_ZSTD_COMPRESSION_ALGORITHM` (`0x0400_0000`, verified against `mysql_common`) advertised when `WireServerConfig::compression_enabled` (default on; `--disable-compression`/`HTAPD_DISABLE_COMPRESSION` to turn off); zstd preferred over zlib, server-clamped to level `1..=3`; a 7-byte compressed-packet header with an independent sequence counter, multiple ordinary packets per frame and multi-frame messages both supported, a 50-byte `MIN_COMPRESS_LENGTH` threshold below which payloads are sent uncompressed, resumable partial-frame reads across a read timeout, and a flush after every response; decompression bounded by multiple independent limits (an upfront rejection of a declared length exceeding the connection's configured maximum, a `.take(declared_length + 1)` decode cap so decoding stops one byte past the frame's own declared length, an exact declared-vs-actual length check, and a zstd `window_log_max(24)` (16 MiB) cap) rather than trusting the declared header length; a framing error (bad sequence id, truncated/corrupt frame, or empty raw frame) permanently latches the stream failed, while a read timeout does not and resumes on the next call; compression and TLS both activate only after the authentication OK packet and are not renegotiated by `COM_CHANGE_USER`; client-side `CompressionMode::{Disabled, Zlib, Zstd{level}}`. Built a catalog-backed per-user account/privilege model: `HTAPCAT1` bumped `FORMAT_VERSION` 2 -> 3 to add `accounts: Vec<Account>`, `grants: Vec<Grant>`, `accounts_initialized: bool` to the existing single-CAS `CatalogSnapshot` (`LEGACY_FORMAT_VERSION` stays `1`, so this build decodes v1/v2/v3 uniformly via `#[serde(default)]`; `compare_and_set` rejects any `accounts_initialized` or account-high-water-mark regression); `Account{id: AccountId, username, password_hash: Option<[u8; 20]>, locked, is_superuser}` (manual `Debug` redacting the hash) and `Grant{account: AccountId, scope: PrivilegeScope::{Global, Table(TableId)}, privileges: PrivilegeSet}` (hand-rolled `u16` bitflags: `SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, ALTER`, the last grantable only via `ALL`); a disclosed vendor `sqlparser` patch adding `IDENTIFIED BY` to `CREATE`/`ALTER USER`, `'user'@'host'` names to `DROP USER`, and a new `Statement::ShowGrants` AST node; new `htap-sql` bound statements and binder support for `CREATE/ALTER/DROP USER`, `GRANT`/`REVOKE`, `SHOW GRANTS` (only the `%` host accepted, `WITH GRANT OPTION` rejected at bind time — no delegated administration); `LocalServer::bootstrap_root_account` creating `root` from `--password` exactly once behind the `accounts_initialized` latch (adopting an existing `root` on every later start, warning if a still-configured password no longer matches its stored hash, never resurrecting a deliberately dropped `root`); `LocalServer::authenticate_session(username, scramble, auth_response)` (challenge/response, never a plaintext password) returning a uniform `HtapError::PermissionDenied`/1045 for unknown user, locked account, or wrong password alike, verified via a constant-time hash comparison (`htap_common::password::constant_time_eq_20`) with the unknown-user/locked-account/mismatched-response paths running the same dummy verification work so login timing does not reveal whether a username exists; `htap-server::privilege::check_privileges` enforcing a pre-bind visibility check (rejecting `CREATE/ALTER/DROP USER`/`GRANT`/`REVOKE` from a non-superuser before the statement is bound, and covering `ALTER TABLE`/`DROP TABLE` targets in its AST table walker) plus a post-bind privilege check under the same `execution_lock`-held catalog snapshot as bind+dispatch, re-resolved every statement (never cached), with existence-masking `NotFound` for zero privileges and the new `HtapError::PermissionDenied`/MySQL 1142 for insufficient-but-nonzero privileges, `SHOW TABLES` filtering, `PREPARE`'s CTE-order-aware AST table walker masking invisible tables before schema resolution and reusing the same catalog snapshot the visibility check produced for schema resolution, `EXECUTE` re-checking independently, `SET @x = expr` evaluation now running its visibility check under `execution_lock`, and multi-statement/`COM_STMT_EXECUTE` inheriting enforcement via the shared `Session::execute_statement` entry point; a v3-tagged catalog payload missing `accounts`/`grants`/`accounts_initialized`/`id_high_water.account` is rejected as `HtapError::Corruption` rather than silently defaulting (v1/v2 unaffected); `DROP TABLE`/`DROP USER` cascading grant cleanup in the same CAS as the drop; dropping the last superuser rejected; `CREATE TABLE` never auto-grants; catalog temp file mode `0600` on Unix. `LocalServer::execute` (no session) remains unchecked by design. See "TLS and compression (Phase 12)" and "Accounts and privileges (Phase 12)" in `docs/ARCHITECTURE.md`, ADR-020, ADR-021, and "TLS, compression, and account/privilege scope and deferred features (Phase 12)" in `docs/LIMITATIONS.md` for the full contract. Covered by `crates/htap-wire/tests/tls.rs` (11 tests: `test_wire_tls_handshake_round_trip`, `test_wire_require_secure_transport_rejects_plaintext_login`, `test_wire_client_tls_required_against_non_tls_server_fails`, `test_wire_tls_ca_mismatch_rejected`, `test_mysql_crate_driver_interop_over_tls`, `test_wire_shutdown_force_closes_idle_tls_connection`, `test_wire_tls_invalid_cert_path_fails_start`, `test_wire_tls_start_with_mismatched_cert_and_key_fails`, `test_wire_tls_cert_reload_serves_new_cert_to_new_connections`, `test_wire_tls_cert_reload_failure_keeps_old_cert`, `test_wire_tls_cert_reload_mismatched_key_rejected_keeps_old_cert`), `crates/htap-wire/tests/compression.rs` (10 tests: `test_wire_compression_zlib_round_trip`, `test_wire_compression_zstd_round_trip`, `test_wire_compression_zstd_level_22_round_trip`, `test_wire_compression_large_payload_over_16mb_round_trip`, `test_wire_compression_decompression_bomb_closes_connection`, `test_wire_compression_disabled_by_server_config`, `test_compression_request_to_disabled_server_uses_uncompressed_connection`, `test_change_user_over_compressed_connection`, `test_mysql_crate_driver_interop_with_compression`, `test_mysql_crate_driver_interop_with_tls_and_compression`), `crates/htap-wire/src/compression.rs` unit tests (below/above-threshold zlib/zstd round trips, resumable partial reads for both zlib and zstd (`test_zstd_read_resumes_after_timeout_in_header`, `test_zstd_read_resumes_after_timeout_in_payload`), oversize rejection, `test_zstd_window_larger_than_16mb_is_rejected`, `test_sequence_id_mismatch_rejected`, `test_empty_raw_frame_rejected`), `crates/htap-wire/src/handshake.rs::decode_ssl_request` and capability-toggle unit tests, `crates/htap-catalog/tests/catalog_recovery.rs` (`test_catalog_v3_round_trip_with_accounts_and_grants`, `test_catalog_v2_envelope_decodes_with_empty_accounts`, `test_catalog_v2_envelope_without_account_fields_decodes`, `test_catalog_v3_rejects_dangling_grant`, `test_catalog_v3_rejects_duplicate_username`, `test_catalog_cas_rejects_accounts_initialized_regression`, `test_catalog_cas_rejects_regressing_account_high_water`, `test_catalog_future_version_rejected`, `test_catalog_file_permissions_restricted_after_publish`, `test_partition_alterations_preserve_account_state`, `test_catalog_v3_payload_missing_security_fields_is_rejected`), `crates/htap-catalog/src/model.rs` unit tests (`test_account_and_grant_validation_failures`, `test_allocate_account_and_account_debug_redaction`), `vendor/sqlparser/src/parser/mod.rs::tests::test_user_management_statements` (including `test_user_management_password_display_escaping`), `crates/htap-sql/tests/parse_bind.rs` (`test_bind_account_management_statements`, `test_bind_account_management_rejections`, `test_grant_table_vs_global_grantee_binding`, `test_grant_grantee_parsing_regression`), `crates/htap-server/tests/accounts.rs` (16 tests), `crates/htap-server/tests/bootstrap.rs::bootstrap_adopts_existing_root_account`, `crates/htap-server/tests/privileges.rs` (20 tests, including `test_prebind_visibility_masks_alter_table_targets` and an extended `test_account_ddl_requires_superuser`), `crates/htap-server/tests/session.rs` (`test_authenticate_session_accepts_account_without_password`, `test_authenticate_session_locked_account_rejected`, `test_authenticate_session_rejects_unknown_account`, `test_authenticate_session_rejects_wrong_password`, `test_authenticate_session_returns_account_principal`, `test_authenticate_session_unknown_user_same_error_as_wrong_password`, `test_change_user_can_switch_between_authenticated_accounts`, `test_change_user_failure_preserves_existing_principal_and_state`, `test_change_user_resets_session_state_before_switching_principal`, `test_change_user_switches_to_authenticated_account`, `test_account_ddl_rejected_inside_open_transaction`), and `crates/htap-wire/tests/accounts.rs` (10 tests, end-to-end over the wire including a real `mysql` crate driver login as a catalog account). Deferred/permanent gaps: roles and delegated administration, hosts other than `%`, `ACCOUNT LOCK`/`UNLOCK` SQL syntax, `caching_sha2_password`, `SIGHUP`-triggered TLS cert reload, unsalted password hashes with no KDF work factor (offline-crackable if the catalog file leaks; the comparison itself is constant-time), and `CREATE TABLE` not auto-granting. |
| Phase 13 — SQL query breadth (windows, correlated subqueries, join trees, recursion, set ops) | `Complete (local MVP)` | Extended the Phase 9 general query executor (`htap-sql::{query, expr, binder_query}`, `htap-server::query_exec`, no on-disk format change) with the SQL breadth Phase 9 explicitly deferred. `GROUP BY`/`ORDER BY` ordinals (`GROUP BY 2` binds the 2nd select-list item's expression, out-of-range/wildcard/aggregate targets are bind errors; `ORDER BY 1` resolves by output-column position). Integer `DIV` (`crates/htap-sql/src/expr.rs`: truncating toward zero, `Int32`/`Int64`-only operands, always `Int64` result, never widens to `Float64`, division by zero -> `NULL`, `i64::MIN DIV -1` a checked-overflow error). `EXCEPT`/`INTERSECT` (`ALL` and `DISTINCT`) reusing UNION's existing branch-binding/column-count/type-widening validation, with correct occurrence-count multiset semantics in the executor (`EXCEPT ALL` keeps `max(0, left_count - right_count)` copies, `INTERSECT ALL` keeps `min(left_count, right_count)`). `FULL OUTER`/`NATURAL`/`USING` joins with real per-join-node column coalescing (`query::VisibleColumn`/`VisibleSchema`; `INNER`/`LEFT` -> left operand's own expression, `RIGHT` -> right operand's, `FULL` -> `Coalesce` of both, correctly recursing through an already-merged operand rather than re-flattening to physical columns; unqualified resolution requires a unique match over the whole visible schema; `SELECT *` expands merged columns first in left-input order; qualified `t.col`/`t.*` unaffected; `NATURAL`'s empty-intersection case degrades to `CROSS` only for `INNER`, keeping `LEFT`/`RIGHT`/`FULL`'s own null-preserving kind otherwise). Arbitrarily nested parenthesized join trees (`query::JoinTree`, built directly and always from the parsed `FROM` clause, 1:1 with the SQL's own parenthesization) with a bind-time-only, regenerated-fresh, never-cached per-node `ON`-condition rebasing scheme and, at the time, a total/pure `lower_to_flat` eligibility predicate (purely left-deep in shape, any join kind) that kept every pre-Phase-13 and plain non-nested Phase-13 query on a separate, unchanged flat executor; a generalized `join_rows` (explicit left/right widths, not assumed-one-physical-slot) backed both executors and fixed a pre-existing `Int64`/`Timestamp` -> `Float64` hash-key-widening collision above `2^53`; a mandatory, permanent differential test pinned the flat and recursive tree evaluators byte-identical on every documented lowerable shape (Phase 14's row below records the later unification of these two evaluators into one, and the resulting change in what that same differential test now proves). `DELETE` accepts an arbitrary `WHERE` filter (`Route::RowstoreDelete { key: Option<Vec<u8>> }`, replacing complete-PK-only `DELETE`; one transaction, same ~4 MiB effective payload cap and no-chunking rule as filtered `UPDATE`, requiring `SELECT` in addition to `DELETE` when filtered). `TRUNCATE TABLE`/`TRUNCATE t` bind to the exact same unfiltered-`DELETE` bound representation (transactional, rollback-able, payload-capped — a disclosed deviation from real MySQL `TRUNCATE`, which is non-transactional and unbounded); unsupported `TRUNCATE` options (multiple targets, partitions, identity, cascade) are bind errors; `IF EXISTS` behaves like `DROP TABLE`'s. `INSERT ... SELECT` binds a query source through the general query binder with an exact per-column static type match (no widening, NULL literal/variable permissive), the UPDATE-style "type mismatch" wording, requiring the same explicit target column list every `INSERT` already requires; execution materializes the source fully at the statement's one snapshot before building target mutations, so a self-referencing `INSERT INTO t SELECT ... FROM t` reads only the pre-insert snapshot and inserts each source row exactly once. Correlated subqueries one level deep only (`Expr::CorrelatedColumnRef`, a `SubqueryRunner` execution-callback trait in `htap-sql::expr` with zero storage types in its signature, a per-statement `SubqueryBudget` capping total invocations and re-entrant nesting depth — `SubqueryBudget::new(10_000, 20)` in `query_exec.rs` — erroring `HtapError::InvalidArgument` naming which cap fired) in `WHERE`/`SELECT`/`HAVING`, reusing the statement's single pinned snapshot/write-set overlay; a reference needing a grandparent lookup is the specific bind error "correlated subqueries may only reference the immediately enclosing query"; in an aggregate query, a correlated subquery in `HAVING`/the projection may only correlate on the outer query's own `GROUP BY` keys. `WITH RECURSIVE`: one self-referencing CTE whose body is a two-branch `UNION`/`UNION ALL`, a working-table placeholder slot (`TableSlot::WorkingTableSlot`, resolved by the executor to the previous iteration's rows, never a real table lookup) executing a fixed-point loop bounded by three independent named caps (`MAX_RECURSIVE_ITERATIONS = 1_000`, `MAX_RECURSIVE_ROWS = 1_000_000`, `MAX_RECURSIVE_BYTES = 256 MiB`, each erroring and naming which cap fired); the recursive term is restricted (no aggregates/`GROUP BY`/window functions/`DISTINCT`/`ORDER BY`/`LIMIT`, exactly one top-level FROM occurrence of the working table, not on the null-supplying side of an outer join); nested/mutually-recursive CTEs are rejected. Window functions (`ROW_NUMBER`/`RANK`/`DENSE_RANK`/`NTILE`/`LAG`/`LEAD`/`FIRST_VALUE`/`LAST_VALUE` and `COUNT`/`SUM`/`AVG`/`MIN`/`MAX` as window functions) with `PARTITION BY`/`ORDER BY`, `ROWS`, peer-based `RANGE` (any number of `ORDER BY` keys, `UNBOUNDED`/`CURRENT ROW` bounds only), and a project-specific value-offset `RANGE` frame (O4: exactly one numeric/`Timestamp` `ORDER BY` key; ASC `n PRECEDING -> V-n`/`n FOLLOWING -> V+n`, DESC flipped; NULL order-key rows form their own peer group rather than erroring; a negative/non-finite literal offset is a bind error; boundary overflow is a checked-arithmetic runtime error; a `Timestamp` offset is a plain integer of microseconds, a documented project-specific extension since there is no `INTERVAL` type). Aggregate discovery scans window `PARTITION BY`/`ORDER BY`/argument expressions too, so `RANK() OVER (ORDER BY SUM(rev) DESC)` alone makes a query an aggregate query; nested windows and a window inside an ordinary aggregate's argument are bind errors; windows are evaluated in a dedicated stage after `HAVING` and before final projection (so a window can combine with `GROUP BY`/aggregates, reading a surviving group's aggregate values), and `HAVING` referencing a window result by alias is a bind error (the window stage has not run yet when `HAVING` runs). See ADR-022 for the correlated-subquery execution boundary and the `JoinTree`/flat-lowering/differential-test design, and "Query routing" / the Phase 13 paragraph in `docs/ARCHITECTURE.md`. Covered by `crates/htap-sql/src/expr.rs` unit tests (`numeric_promotion_and_overflow` extended for `IntDiv`, `subquery_runner_trait_and_correlation_eval`, `subquery_invocation_and_nesting_caps`), `crates/htap-sql/tests/query_bind.rs` (`test_join_tree_lowering_and_nested_groups`, `test_natural_using_coalescing_all_join_kinds`, `test_natural_using_ambiguity_and_errors`, `test_correlated_subquery_binding_and_depth_limit`, `test_correlated_subquery_grouped_context`, `test_correlated_subquery_binding_is_case_insensitive`, `test_recursive_cte_binding_and_output_schema`, `test_recursive_cte_binding_rejects_invalid_shapes`, `test_recursive_cte_binding_restrictions_and_non_recursive_forms`, `test_window_ranking_and_offset_function_binding`, `test_window_aggregate_and_value_functions_with_frames`, `test_value_offset_range_frame_binding_and_key_restrictions`, `test_window_functions_over_group_by_and_aggregate_discovery`, `test_having_cannot_reference_window_result`, `test_expressions_functions_and_type_checks` extended for `DIV`), `crates/htap-sql/tests/parse_bind.rs` (`test_bind_truncate`, `test_bind_insert_select`, `test_bind_delete_general_filters`), `crates/htap-sql/tests/prepare.rs` and `crates/htap-wire/tests/wire_server.rs` (`test_prepared_insert_select_with_bound_parameter`, `test_prepared_delete_with_filter_reports_affected_rows`, exercising placeholder substitution through window/`TRUNCATE`/join-tree positions), `crates/htap-server/tests/query_exec.rs` (`test_order_by_and_group_by_ordinals_execute`, `test_except_and_intersect_multiset_semantics`, `test_intersect_binds_tighter_than_union`, `test_nested_join_groups_and_full_join_execute`, `test_nested_join_groups_using_qualified_access_derived_tables_and_subqueries`, `test_using_and_natural_joins_merge_columns`, `test_flat_and_tree_join_evaluators_match_for_lowerable_queries`, `test_hash_join_preserves_large_integer_keys`, `test_delete_by_filter_on_row_table_reports_affected_rows_and_remaining_rows`, `test_delete_by_filter_on_column_table_reports_affected_rows_and_remaining_rows`, `test_delete_by_filter_prunes_range_partition_and_preserves_other_partitions`, `test_unfiltered_delete_empties_table_and_reports_affected_rows`, `test_insert_select_basic`, `test_insert_select_halloween`, `test_insert_select_from_join_with_aggregate`, `test_insert_select_into_partitioned_target`, `test_insert_select_from_column_format_source`, `test_insert_select_duplicate_pk_aborts_atomically`, `test_correlated_exists_in_where`, `test_correlated_in_subquery`, `test_correlated_subquery_nested_two_levels`, `test_correlated_subquery_caps_fire_during_execution`, `test_correlated_subquery_column_outer_pruning_and_having`, `test_correlated_scalar_subquery_in_select`, `test_correlated_subquery_in_having_grouped`, `test_correlated_subquery_self_reference`, `test_correlated_subquery_across_row_column_converting_and_partitions`, `test_recursive_cte_counting_and_hierarchy_traversal`, `test_recursive_cte_union_distinct_vs_all_semantics`, `test_recursive_cte_iteration_and_row_cap_bounded_time`, `test_recursive_cte_large_working_set`, `test_recursive_cte_over_column_and_partitioned_tables`, `test_recursive_cte_with_type_widening`, `test_recursive_cte_outer_query_uses_cte`, `test_window_ranking_functions_ties_and_peers`, `test_window_ntile_lag_lead`, `test_window_functions_over_group_by_execute`, `test_window_result_used_in_order_by_and_limit`, `test_window_functions_across_storage_formats_and_partitions`, `test_window_aggregate_and_value_functions_execute`, `test_window_aggregate_default_and_explicit_frames`, `test_window_first_last_value_frames`, `test_window_peer_range_frames`, `test_value_offset_range_frame_numeric_with_nulls`, `test_value_offset_range_frame_timestamp_with_nulls`, `test_window_aggregates_across_storage_formats_and_partitions`, `test_drop_table_reopen_and_no_id_reuse` (regression), `test_legacy_catalog_seeds_tablet_high_water_from_colstore_inventory` (regression)), `crates/htap-server/tests/session.rs` (`test_read_your_own_writes_nested_join_group`, `test_truncate_in_transaction_rollback_restores_rows`, `test_truncate_in_read_only_transaction_is_rejected`, `test_delete_by_filter_in_transaction_rollback`, `test_delete_by_filter_reads_own_writes`, `test_delete_by_filter_payload_cap_rejects_atomically`, `test_delete_by_filter_rejected_in_read_only_transaction`, `test_delete_by_filter_commit_is_one_version`, `test_insert_select_uncommitted_within_transaction`, `test_insert_select_payload_cap_exceeded`, `test_insert_select_in_read_only_transaction`, `test_correlated_subquery_sees_uncommitted_session_writes`, `test_recursive_cte_reads_uncommitted_rows_and_rollback_hides_them`), `crates/htap-server/tests/session_recovery.rs` (`test_uncommitted_delete_by_filter_vanishes_after_reopen`, `test_uncommitted_insert_select_vanishes_after_reopen`), `crates/htap-server/tests/privileges.rs` (`test_join_using_ungranted_table_is_masked`, `test_natural_join_with_ungranted_table_is_masked`, `test_nested_join_tree_with_ungranted_table_is_masked`, `test_full_outer_join_with_ungranted_table_is_masked`, `test_visible_but_unselectable_join_tables_are_denied_post_bind`, `test_filtered_delete_subquery_with_ungranted_table_is_masked`, `test_delete_without_grants_masks_filtered_target_table`, `test_delete_requires_only_delete_for_point_targets_but_select_for_filters`, `test_truncate_requires_delete_only_and_masks_hidden_tables`, `test_truncate_if_exists_missing_table_is_a_noop`, `test_truncate_if_exists_invisible_table_masks_as_missing`, `test_insert_select_requires_select_on_source_and_leaves_target_unchanged_when_denied`, `test_insert_select_masks_invisible_source_as_missing_table`, `test_insert_select_masks_hidden_sources_in_subquery_and_cte`, `test_insert_select_requires_insert_on_target`, `test_recursive_cte_privilege_checks_masking_and_self_reference`), and `crates/htap-server/tests/local_server.rs::test_truncate_empties_table_and_reports_affected_rows`. Deferred at the time (cost-based optimization, spilling, and bounded parallelism were added in Phase 14, see that row, which also unified the flat and recursive tree join evaluators described above into one): vectorized/pipelined execution, worker-pool parallelism for the general path, memory bounds/spilling, `LIMIT BY`, `UPDATE` with joins/subqueries/`ORDER BY`/`LIMIT`, non-partition `ALTER TABLE`, DATE/DECIMAL/EXTRACT/SUBSTRING/INTERVAL/views, and semi-join rewrites of `IN`/`EXISTS`. |
| Stage R — Shared durability primitives (`docs/PROBLEMS.md` P1) | `Complete` | Behavior-preserving refactor, no on-disk format change and no pre-existing test edits: consolidated the durability building blocks that were separately hand-written in seven crates into one shared `htap-common` module — `fs::{sync_dir, write_new_tmp_file, remove_file_if_exists, fsync_file, atomic_publish, read_file_exact_bounded, read_file_exact_bounded_from_file}`, `envelope::{encode_envelope, decode_envelope, encode_bare_frame, EnvelopeError, SizeCheckMode}`, and `bytecursor::ByteReader` (checked little-endian reads returning a corruption error instead of panicking). Migrated all six whole-file envelopes (`HTAPCAT1` catalog, `HTAPCRD1` coordinator, `HTAPJOB1` movement job, `HTAPMNF1` tablet package manifest, `HTAPTBM1` tablet column manifest, `HTAPMAN1` rowstore manifest), the WAL and txn-journal bare frame headers, the rowstore `VISIBLE` marker publish, and the hand-written little-endian reads in `htap-colstore::{encoding, segment}` and `htap-rowstore::sst`. Each call site kept its own error-message text, temp-file cleanup policy, and directory-sync propagation-vs-swallowing choice; `HTAPMAN1` alone kept its single exact-size check (`SizeCheckMode::ExactMatch`) while the other five kept a two-step truncated/trailing check (`SizeCheckMode::TruncatedThenTrailing`); the WAL still stops at the first bad frame while the journal still distinguishes a repairable torn final frame from mid-log corruption. `htap-rowstore/src/wal.rs::fsync_dir` — a sixth `sync_dir`-shaped function found during the audit, unconditional rather than `cfg(unix)`-gated like the other five — was deliberately left un-migrated, since moving it either direction would change untested non-Unix behavior; it now carries a comment saying so. One message text changed as an accepted, test-pinned exception: `Manifest::read_from_file`'s untested trailing-probe error text, now produced by the shared `read_file_exact_bounded`. See the storage-format compatibility table in `docs/ARCHITECTURE.md` ("Dual-format storage") and `docs/DECISIONS.md`. Covered by `crates/htap-common/src/fs.rs` (`fs::tests::test_not_found_preserved`, `::test_normal_read_within_bound`, `::test_exact_bound_matches_size`, `::test_oversized_file_rejected_as_corruption`, `::test_empty_file_returns_empty_vec`, `::test_remove_file_if_exists_handles_missing_and_existing_files`, `::test_atomic_publish_cleans_tmp_on_write_failure_when_enabled`, `::test_atomic_publish_retains_tmp_on_write_failure_when_disabled`, `::test_atomic_publish_cleans_tmp_on_rename_failure_when_enabled`, `::test_atomic_publish_retains_tmp_on_rename_failure_when_disabled`), `crates/htap-common/src/envelope.rs` (`envelope::tests::round_trip`, `::reports_too_small`, `::reports_bad_magic_before_other_header_faults`, `::reports_unsupported_version_before_bad_crc`, `::reports_payload_too_large`, `::size_modes_distinguish_short_input`, `::size_modes_distinguish_trailing_input`, `::reports_checksum_mismatch`, `::bare_frame_layout_is_len_crc_payload`), `crates/htap-common/src/bytecursor.rs` (`bytecursor::tests::fixed_width_reads_decode_little_endian_values`, `::failed_fixed_width_read_does_not_advance`, `::failed_length_prefixed_read_does_not_advance`, `::length_arithmetic_is_overflow_safe`, `::empty_and_exact_boundary_reads_succeed`, `::expect_exhausted_reports_remaining_bytes`), golden-bytes and per-fault-class tests per envelope (`crates/htap-rowstore/src/manifest.rs::{test_manifest_v1_golden_bytes, test_manifest_v2_golden_bytes, test_manifest_read_from_file_trailing_content_rejected}`, `crates/htap-rowstore/src/wal.rs::wal_frame_golden_bytes`, `crates/htap-txn/src/journal.rs::journal_frame_golden_bytes`, `crates/htap-catalog/src/local.rs::catalog_envelope_golden_bytes`, `crates/htap-coord/src/lib.rs::test_coordinator_state_golden_bytes`, `crates/htap-movement/src/job.rs::test_job_envelope_golden_bytes`, `crates/htap-movement/src/tablet.rs::test_manifest_envelope_golden_bytes`, `crates/htap-convert/src/lib.rs::test_tablet_column_manifest_golden_bytes`), and every pre-existing recovery/crash test passing unchanged: `crates/htap-rowstore/tests/{external_ledger,sst,engine,engine_crash,durable_completion,wal_crash,wal_recovery}.rs`, `crates/htap-txn/tests/{journal,two_phase_commit,rowstore_adapter}.rs`, `crates/htap-catalog/tests/catalog_recovery.rs`, `crates/htap-coord/tests/{local_coordinator,placement_movement}.rs`, `crates/htap-movement/tests/{jobs,tablet_simulation,import_export}.rs`, `crates/htap-convert/tests/{tablet_manifest,materialization}.rs`, `crates/htap-colstore/tests/{segment_roundtrip,scan,zone_map_skip}.rs`. |
| Phase 14 — Cost-based optimization, `EXPLAIN`, spilling, and bounded parallelism | `Complete (local MVP)` | Lifted exactly three named `docs/LIMITATIONS.md` gaps for the general executor only (`Route::RowstorePointRead`/`Route::OlapScan` byte-for-byte unchanged): **statistics** — `ANALYZE TABLE t` (`BoundStatement::AnalyzeTable`, routes as `Route::CatalogDdl`) scans every partition at one MVCC snapshot via the existing `scan_partition_compact` path and records exact per-table row count and per-column null count/min/max/distinct-count (`crates/htap-server/src/analyze.rs::AnalyzeAccumulator`, capped at a configurable `LocalServer::with_analyze_distinct_limit`/default 200,000 values, past which `distinct_count` is `None`); each column's min/max bound reserves the replacement's memory before dropping the old reservation (`AnalyzeAccumulator::replace_bound`), so a failed reservation leaves the existing bound and its accounting exact — verified by `crates/htap-server/src/analyze.rs::analyze::tests::replace_bound_preserves_existing_reservation_when_budget_is_exhausted`; disclosed, not fixed: `ANALYZE TABLE` fully materializes each partition before reserving its memory, so usage can overshoot before the check runs and a partition estimated above the budget fails `ANALYZE` outright; stats publish via catalog CAS of only the `stats` field, re-verifying the table id after a long scan (`publish_table_stats`), retrying on an unrelated concurrent CAS conflict. `HTAPCAT1` `FORMAT_VERSION` bumped 3 -> 4 (`TableDescriptor.stats: Option<TableStats>`, `#[serde(default)]`); a v3 payload decodes with `stats: None` on every table, and a v4 payload with a corrupted CRC is rejected as `HtapError::Corruption`. **Cost-based optimization** — a storage-agnostic `htap-sql::optimize` stage (`StatsLookup` trait, `estimate_row_count`/`estimate_equality_selectivity`/`estimate_range_selectivity`/`estimate_join_cardinality`, each returning `(value, EstimateSource::{Stats,Default})`), a predicate-atom inventory with provenance (`PredicateAtom{origin, mobility}`, correctly pinning a conjunct that crosses an outer-join boundary or references a null-supplying slot), join reordering by subset dynamic programming for up to 8 relations per connected `Inner`/`Cross` component and a greedy cheapest-extension heuristic above that (a recursive CTE's recursive term is never reordered into or merged with an enclosing component), an always-on predicate-conservation validator (`validate_predicate_conservation`) that falls back to the identity plan on any internal inconsistency instead of risking a wrong result, and a `PhysicalQuery` entry point (`optimize::optimize`) enabled by default for every `Route::Query` execution (`ExecContext::optimization_mode` defaults to `OptimizationMode::Enabled`). **`EXPLAIN`/`EXPLAIN ANALYZE`** — `BoundStatement::Explain{inner, analyze}` renders one row per plan node (`node_id, parent_id, operation, table, est_rows, estimate_source, build_side`) via `crates/htap-server/src/explain.rs`; a `Route::RowstorePointRead`/`Route::OlapScan` inner statement renders a single-node plan and never calls `htap_sql::optimize::optimize` at all (verified by name, not just by outcome); `ANALYZE` additionally executes the statement and reports root-level `actual_rows`/`actual_time_ms` (child-node attribution is best effort). **Spilling scope** — the `MemoryBudget` and every spill path below belong to the general executor (`Route::Query`) only; a single-table `SELECT` with `ORDER BY`, `GROUP BY`, or a plain aggregate and no join routes to `Route::OlapScan` instead, which has no memory budget and never spills, by design (several early spill tests were silently exercising this unbudgeted analytic path and were rewritten to use a join or another shape that reaches the general executor). The budget bounds each operator's own working memory (hash tables, sort runs, aggregate state, partition buffers), not the rows materialized between operators, because the executor is not pipelined. **Spilling** — a per-statement `MemoryBudget` (`crates/htap-server/src/memory_budget.rs`, atomic byte counter, RAII `MemoryReservation`, default 256 MiB via `LocalServer::with_query_memory_budget`), a non-durable spill file format (`crates/htap-server/src/spill.rs`, magic `HTAPSPIL`, no CRC/fsync/version-range contract by design — spill scratch is explicitly exempt from the envelope contract) under `<data-root>/spill/<statement-id>/`, swept in full on `LocalServer::open` after the root lock is acquired (disclosed, not fixed: a failed sweep only logs the error rather than returning one, and a stale file left behind can collide once with a statement id reused after a process restart, since `SpillWriter::create` opens with `create_new(true)`, failing that one statement with an `AlreadyExists` I/O error that heals on its own once the id has been consumed); one level of grace hash partitioning spills a hash-equi-join whose build side would exceed budget (partition count sized from the input and the remaining budget, capped at `HASH_JOIN_MAX_SPILL_PARTITIONS` = 128, shared by window spilling to bound open file descriptors and the writer buffers the budget doesn't count; falling back to the memory-budget error, never recursing, if a partition is still over budget after partitioning), `GROUP BY` and the set operators (`DISTINCT`/`EXCEPT`/`INTERSECT`/`UNION DISTINCT`) each partition into their own fixed `GROUP_BY_SPILL_PARTITIONS`/`SET_OPERATION_SPILL_PARTITIONS` = 16 partitions (including `AVG`/`DISTINCT` aggregates, which merge correctly rather than averaging-of-averages; preserving `ALL`-variant multiplicities; unlike the hash-join/window cap, this fixed 16 does not scale with input size or the remaining budget — a disclosed limitation, since an input much larger than roughly 16x the budget fails with the memory-budget error instead of spilling successfully, and budget-sized partitioning for these operators is deferred), `ORDER BY` (external merge, preserving `NULLS FIRST`/`LAST` and ties), and window functions (`RANK`/`ROW_NUMBER`/running aggregates, hash-partitioned by the `PARTITION BY` key — no `PARTITION BY` is a single partition — reassembled in original input row order after processing one partition at a time). `GROUP BY` spill reserves each partition's rows as they are read back and releases that reservation before in-memory aggregation begins, avoiding a double charge on the way in; because the rows themselves are still resident in memory while the aggregate state is (re-)charged, peak memory during one partition's aggregation can approach about twice the budget — a disclosed imprecision, not an exact bound (`crates/htap-server/src/query_exec.rs`'s group-by spill path). Spill telemetry is per operator: `LocalServer::{last_query_hash_join_spilled, last_query_group_by_spilled, last_query_sort_spilled, last_query_distinct_spilled, last_query_set_operation_spilled, last_query_window_spilled}` each report whether that operator kind spilled in the caller thread's most recent statement (test telemetry, not a monitoring API); every spill test below asserts its own named operator actually spilled, closing a prior coarse-telemetry gap where an `ORDER BY` spill test could pass because its `JOIN` spilled instead. **Parallelism** — bounded intra-query parallelism reusing `std::thread::scope` (no new dependency), a shared `LocalServer::query_parallelism` (default `available_parallelism()`, configurable via `with_query_parallelism`) threaded through `ExecContext` so nested parallel stages share one worker budget rather than multiplying threads; parallel `GROUP BY` above a size threshold (deterministic across worker counts 1/2/N); parallel hash-join build+probe restricted to `INNER`/`CROSS` joins with the build side on the right, sharded by `hash(join key) % k` and reassembled by originating left-row ordinal for the documented no-`ORDER BY` determinism contract; `LEFT`/`RIGHT`/`FULL` joins stay single-threaded. **Code health (P2, partial — decision recorded in ADR-023, see also `docs/PROBLEMS.md`)**: one `EvalServices`/`eval_context!` constructor for the expression-evaluation context replacing every hand-built `EvalContext { .. }` literal in `crates/htap-server/src/{query_exec,lib,session}.rs`; `SelectBody` always carries a tree-shaped `join_tree` now (the flat `joins`/`tree_only` fields and the separate flat-loop join executor were removed — `evaluate_join_tree` is the only join execution path); the `#[allow(clippy::too_many_arguments)]` count in `crates/htap-server/src/query_exec.rs` dropped to zero via context structs. The two binder entry points (`htap_sql::binder::bind` for `PointSelect`/`AnalyticSelect`, `htap_sql::binder_query::bind_select_body` for `BoundQuery`) were deliberately kept separate, by the Option B decision in ADR-023, so R5 stays a structural (`is_narrow_select_shape` runs before any general-binder code) rather than runtime property; **the planned shared-leaf-helper extraction between the two binders (literal binding, cast-target mapping, comparability checks, scalar-function signature checks, schema column lookup) was not delivered in this diff** — both binders still duplicate that leaf-level logic, an open item, not claimed as fixed. Verified by `crates/htap-sql/tests/parse_bind.rs::test_analyze_table_accept_and_reject_forms`; `crates/htap-catalog/tests/catalog_recovery.rs::{test_catalog_v3_envelope_defaults_table_stats_to_none, test_catalog_v4_corrupted_crc_is_rejected}`; `crates/htap-server/tests/analyze.rs` (`test_analyze_table_row_count_nulls_min_max`, `test_analyze_distinct_count_cap_falls_back_to_unknown`, `test_analyze_reopen_recovery`, `test_analyze_concurrent_unrelated_catalog_mutation`, `test_analyze_missing_table_errors`, `test_analyze_target_succeeds`); `crates/htap-sql/tests/optimize.rs` (`full_join_with_predicates_on_both_sides`, `inner_join_nested_under_left_join`, `left_join_with_right_side_filter_in_on`, `left_join_with_right_side_filter_in_where`, `random_small_join_shapes_preserve_predicates_rows_and_order`; 5 tests) plus `crates/htap-sql/src/optimize.rs` unit tests (`optimize::tests::{estimates_row_count_from_statistics, estimates_equality_selectivity_from_distinct_count, estimates_supported_range_from_statistics, estimates_join_cardinality, extracts_simple_where_conjuncts, outer_join_on_conjuncts_are_pinned, inner_on_predicate_under_null_supplying_side_is_pinned, optimize_keeps_pinned_inner_on_predicate_on_an_inner_join, left_side_left_join_on_predicate_remains_pinned, predicate_spanning_outer_join_boundary_is_post_join_only, where_on_left_join_null_supplying_side_is_post_join_only, correlated_where_predicate_is_post_join_only, disconnected_equi_join_subgraphs_require_a_cross_join, mixed_cross_and_equi_joins_preserve_all_relations, reorders_four_table_chain_and_places_each_join_predicate, reorders_four_table_star_without_losing_slots, test_predicate_conservation_catches_dropped_predicate, test_predicate_conservation_catches_duplicated_predicate, predicate_conservation_rejects_hoisting_from_outer_join_null_side, outer_join_hoisting_shape_passes_conservation_validation, outer_join_sinking_shape_passes_conservation_validation, optimize_falls_back_when_conservation_validation_fails, optimize_remaps_columns_when_join_predicate_moves, pushdown_uses_cost_with_stats_and_first_match_without_stats, random_small_select_shapes_pass_conservation_validation}`, 32 tests total — the storage review's fix for a predicate that could cross an outer-join boundary and silently change results added the pinning/conservation tests named above, see ADR-023); `crates/htap-server/tests/explain.rs` (`test_explain_primary_key_point_lookup_uses_rowstore_fast_path`, `test_explain_analytic_scan_uses_olap_fast_path`, `test_explain_join_estimates_report_default_then_stats_provenance`, `test_explain_analyze_reports_root_actual_rows_and_time`, `test_explain_statistics_after_analyze_table`, `test_plain_explain_select_permitted_inside_open_transaction`, `test_analyze_statistics_survive_reopen`, `test_explain_create_table_renders_plan_without_creating_table` (plain `EXPLAIN` on `CREATE TABLE` renders a plan without creating the table), `test_explain_analyze_create_table_executes_and_creates_table` (`EXPLAIN ANALYZE` on `CREATE TABLE` actually executes it); 9 tests); `crates/htap-server/src/memory_budget.rs` unit tests (`memory_budget::tests::{test_reserve_within_limit, test_reserve_past_limit_fails, test_dropping_reservation_releases_bytes, test_row_estimator_counts_strings}`); `crates/htap-server/src/spill.rs` unit tests (`spill::tests::{spill_rows_round_trip, truncated_header_and_record_are_corruption, corrupt_large_length_is_rejected_without_payload_allocation, spill_dir_removes_tracked_files_on_drop, spill_reader_uses_a_bounded_buffer}`, 5 tests — the last proves the reader now streams through a fixed buffer instead of loading a whole spill file); `crates/htap-server/tests/spill.rs` (`test_spill_uses_server_root_and_reopen_sweeps_abandoned_files` (renamed from `test_reopen_sweeps_abandoned_spill_files`; this is the test that actually proves spill scratch lives under `<data-root>/spill/`, not the system temp directory), `test_hash_join_spills_under_tiny_budget_same_result`, `test_skewed_hash_join_spill_exceeds_partition_budget_cleanly`, `test_hash_join_mixed_int_float_keys_normalizes_correctly`, `test_spilled_hash_join_matches_across_row_column_and_converting_tables`, `test_large_join_order_by_spills_and_preserves_order`, `test_group_by_spills_under_tiny_budget_same_result`, `test_ungrouped_aggregate_spill_returns_one_row_including_empty_input`, `test_order_by_spills_preserves_order_and_nulls`, `test_select_distinct_spill_deduplicates`, `test_union_distinct_spill_deduplicates`, `test_except_intersect_all_spill_preserves_multiplicity`, `test_window_row_number_spills_under_tiny_budget_same_result`, `test_window_rank_spills_preserves_ties_and_nulls`, `test_window_running_sum_spills_under_tiny_budget_same_result`, `test_window_without_partition_by_fails_when_its_input_exceeds_budget`, `test_window_skewed_partition_fails_while_other_queries_succeed`; 17 tests, each asserting its own named operator's per-operator spill-telemetry accessor — `test_ungrouped_aggregate_spill_returns_one_row_including_empty_input` asserts `!last_query_group_by_spilled()` since an ungrouped aggregate is never partitioned, every other test asserts its operator did spill); `crates/htap-server/tests/parallel.rs` (`test_nested_parallel_stages_do_not_multiply_thread_count`, `test_group_by_parallel_determinism_across_worker_counts`, `test_group_by_parallel_avg_and_distinct_correct`, `test_group_by_with_variable_does_not_parallelize`, `test_inner_join_parallel_determinism_across_worker_counts`, `test_inner_join_parallel_matches_serial_row_order`, `test_inner_join_parallel_key_normalization_consistency`; 7 tests — the added test proves the parallelism gate now inspects the expressions workers evaluate, not merely whether services are absent); and `crates/htap-server/tests/differential.rs` (`differential_query_execution_paths_agree`, `differential_outer_join_hoisting_predicate_from_inner_on_clause`, `differential_outer_join_sinking_inner_join_predicate_referencing_null_side`; 3 tests, identity-optimized baseline vs. fully optimized vs. fully optimized under a tiny memory budget forcing every spillable stage to spill, asserting the same row bag and, where an order contract exists, the same row order). Also: `crates/htap-server/tests/query_exec.rs::test_correlated_subquery_is_optimized_once_per_statement` (correlated subqueries are optimized once per statement, not per outer row, using the test-telemetry `LocalServer::last_query_optimizer_invocations` accessor) and `crates/htap-server/src/query_exec.rs`'s `query_exec::memory_regression_tests::growing_distinct_group_charges_only_incremental_state_bytes` unit test (a growing aggregate group is charged only the increment, not its full new size, each time it grows). **Float overflow (external-review round, MySQL-compatible):** float `+`/`-`/`*`/`/` and `SUM`/`AVG` overflow return `HtapError::InvalidArgument("DOUBLE value is out of range in '<op>'")` instead of producing `NaN`/`Infinity`; division by zero is unchanged and still yields `NULL`. This closes a real risk, since `serde_json` cannot round-trip a non-finite float (it encodes as JSON `null`, which fails to decode back into a non-`Option` float field): on the row-mutation path this was already caught, if confusingly, by `RowstoreParticipant::decode_payload` running during 2PC `prepare` (pre-commit — confirmed by reading `crates/htap-txn/src/rowstore.rs`), so no corruption was ever possible there; the catalog statistics path had no such incidental protection and was the real brick risk, now closed by rejecting non-finite bounds in `ANALYZE TABLE` and by `TableStats` structural validation (column count, null count, distinct count, min/max type agreement, `min <= max`, and finiteness) on every catalog publish — no `HTAPCAT1` format change. `serde_json`'s `float_roundtrip` feature is enabled workspace-wide as a precaution, not a bug fix: a review claimed the default parser could decode a stored `DOUBLE` one ULP off; tested with the feature switched off, the default parser round-tripped 50,000 random finite bit patterns plus adjacent doubles, subnormals, the extremes, and `-0.0` bit-identically on this `serde_json` version, so no drift was demonstrated — the feature is kept anyway for a version-independent exactness guarantee, at some parse-speed cost, with no on-disk format change. Verified by `crates/htap-common/src/types.rs::types::tests::test_serde_json_float64_roundtrips_bit_identically`, `crates/htap-server/tests/session_recovery.rs::test_double_values_round_trip_bit_identically_after_reopen`, and `crates/htap-server/tests/spill.rs::test_spilled_set_operations_and_distinct_preserve_awkward_doubles`. Spilled set operations and `SELECT DISTINCT` emit rows by source index rather than looking one up in a map keyed by a decoded row, making them independent of round-trip exactness (hardening, not a fix for a demonstrated bug). The narrow analytic scan path's own `SUM` accumulator (`crates/htap-server/src/olap.rs::Accumulator::SumFloat`) now carries the same overflow check, closing what was previously an unverified, code-inspection-only gap — verified by `crates/htap-server/tests/query_exec.rs::test_analytic_float_sum_overflow_returns_out_of_range_error`. `CAST(... AS DOUBLE)` from a string (`CAST('nan'/'inf'/'1e999' AS DOUBLE)`) and an out-of-range float literal (e.g. `1e400`) return the same `"DOUBLE value is out of range"` error — verified by `crates/htap-server/tests/query_exec.rs::{test_non_finite_double_casts_and_literals_are_rejected, test_non_finite_cast_update_fails_at_statement_and_leaves_value_unchanged}`. Also verified by `crates/htap-server/tests/query_exec.rs::{test_float_sum_overflow_returns_out_of_range_error, test_float_avg_overflow_returns_out_of_range_error}` and `crates/htap-server/tests/session_recovery.rs::{test_float_add_overflow_rejected, test_float_subtract_overflow_rejected, test_float_multiply_overflow_rejected, test_float_divide_by_tiny_overflow_rejected, test_non_finite_float_update_reports_reopen_outcome}` (the last proves a rejected non-finite write leaves a consistent, reopenable outcome rather than a corrupted catalog or WAL). **Catalog statistics structural validation:** `crates/htap-catalog/tests/catalog_recovery.rs::{test_catalog_validate_accepts_correct_statistics, test_catalog_validate_rejects_column_count_mismatch, test_catalog_validate_rejects_distinct_exceeds_non_null, test_catalog_validate_rejects_max_type_mismatch, test_catalog_validate_rejects_min_type_mismatch, test_catalog_validate_rejects_min_greater_than_max, test_catalog_validate_rejects_nonfinite_max_float, test_catalog_validate_rejects_nonfinite_min_float, test_catalog_validate_rejects_null_count_exceeds_rows}` (9 tests, including the non-finite-float and split min-type/max-type cases named in this pass's brief). `ANALYZE TABLE` is gated exactly like `CREATE TABLE`/`DROP TABLE`/user-management DDL: `BoundStatement::AnalyzeTable` is in `Session::is_ddl`'s match list, so it is rejected inside any open transaction (explicit or implicit autocommit-off), the transaction survives the rejection, and `EXPLAIN ANALYZE` follows the transaction rules of the statement it actually executes (plain `EXPLAIN` is permitted inside an open transaction since it only plans, never executes) — proven by `crates/htap-server/tests/session.rs` (`test_analyze_table_rejected_inside_explicit_transaction_and_txn_survives`, `test_explain_analyze_wrapping_ddl_rejected_inside_open_transaction`, `test_explain_analyze_wrapping_insert_rejected_inside_read_only_transaction`) and `crates/htap-server/tests/explain.rs::test_plain_explain_select_permitted_inside_open_transaction`. Evidence gap, disclosed rather than silently assumed: no test exercises hash-join spilling for `LEFT`/`RIGHT`/`FULL` joins specifically (the spill code path is not structurally restricted to `INNER`/`CROSS` the way the *parallel* hash join is, but only `INNER` equi-joins are covered by a spill test). Two further disclosed gaps from the external-review round: window evaluation carries every materialized column of the joined input into its spill partitions, not just the columns the query needs (measured: 8 columns / ~424 B per row where 3 are needed), inflating window partition sizes under a budget — a column-trimming improvement is deferred, not delivered; and the optimizer's leaf-cost and outer-join cardinality estimates are cost-quality-only weaknesses, confirmed by external review to never change results (outer joins are never reordered by the DP/greedy join-reordering step, and null-padding of an outer join's unmatched rows is independent of which side was chosen as the hash build side). See ADR-023, `docs/PROBLEMS.md`'s Phase 14 update, and the "Phase 14" section of `docs/ARCHITECTURE.md`. |
| Phase 15 — DROP TABLE reclaim, rowstore compaction/GC, transaction journal checkpoint | `Complete (local MVP)` | Narrow local slice, no query-execution or row/column format change (`Route::RowstorePointRead`/`Route::OlapScan`/`Route::Query` byte-for-byte unchanged). **Rowstore compaction (`Engine::compact_once`, `crates/htap-rowstore/src/engine.rs`):** selects one *contiguous* run of the manifest's SST list — tiered by `floor(log_4(entry_count))` (no byte-size field exists to tier by), or an explicit `CompactionInput.explicit_sst_ids` set for the forced-priority `DROP TABLE` path — and splices its merged output into that run's original manifest position, never prepending it. An initial draft prepended the output to index 0, which could let a newer, unselected SST's `Put`/`Delete` for a shared key lose to older compacted data (a live resurrection bug found by a storage-review panel); fixed and pinned by `crates/htap-rowstore/tests/compaction_ordering.rs::{test_non_newest_compaction_run_keeps_newer_ssts_authoritative, test_selected_tombstone_never_resurrects_older_unselected_value, test_compaction_output_keeps_selected_runs_original_manifest_position, test_scattered_dropped_partition_is_purged_over_contiguous_passes, test_sandwiched_partition_becomes_exactly_absent_in_one_pass, test_explicit_set_with_protected_middle_compacts_only_one_sub_run}` (also `test_newer_layer_wins_before_after_compaction_and_reopen`, 7 tests). Per key, every version above `input.gc_horizon` is kept unconditionally; among versions at or below it, only the single newest survives (`Put` or `Delete`, never elided): `crates/htap-rowstore/tests/compaction.rs::{test_compact_once_noop_below_tier_threshold, test_compact_once_merges_tier_and_preserves_read_results, test_compact_once_collapses_versions_below_horizon_keeps_above, test_compact_once_never_resurrects_value_via_partial_compaction_tombstone, test_compact_once_drops_partition_unconditionally_including_above_horizon, test_compact_once_empty_output_removes_manifest_entry_with_no_new_sst, test_compact_once_forced_priority_includes_dropped_partition_sst_outside_normal_tier, test_compact_once_protected_partition_ids_excludes_sst_from_candidate_set, test_preview_compaction_candidate_partitions_matches_what_compact_once_actually_touches, test_partitions_possibly_present_reports_absent_after_full_purge, test_compaction_crash_after_manifest_publish_before_readstate_swap, test_compaction_crash_during_output_sst_write, test_concurrent_flush_and_compaction_serialized_by_commit_lock, test_preview_compaction_candidates_returns_exact_selected_ssts_and_partitions, test_flush_roll_and_gc_is_idempotent_with_empty_memtable}` (15 tests). **Storage-review "X batch" fix (GC horizon clamp, X2), re-clamped to `visible_version` in a follow-up "Y batch" pass:** `compact_once` clamps `input.gc_horizon` before using it, and collapses versions and raises `gc_low_water` using that one clamped value, always, for every publishing pass. An earlier draft exempted the `u64::MAX` sentinel horizon (used by callers that want "collapse everything") from raising `gc_low_water` at all on the theory that it was a write-side-only signal; that let versions actually collapse without the read-side floor ever rising to match, so a real snapshot at an older version could read silently-collapsed data instead of being rejected with the "below GC low-water" error `Engine::get`/`scan_partition`/`prepare` are supposed to raise (`test_infinite_gc_horizon_is_clamped_to_committed_version`, whose committed and visible versions happen to coincide, so it does not distinguish the two). A second storage re-review found the fix had clamped to the wrong watermark: `effective_gc_horizon = input.gc_horizon.min(committed_version)` could raise `gc_low_water` above `visible_version` whenever `apply_external` had advanced `committed_version` without yet advancing `visible_version` (2PC/external-apply commits are committed before they are published), which would then reject every fresh snapshot — snapshots are bounded by `visible_version`, never `committed_version`. The clamp is now `effective_gc_horizon = input.gc_horizon.min(read_state.visible_version)`; `committed_version` still feeds `committed_version_high_water` unchanged. `crates/htap-rowstore/tests/horizon_clamp.rs::{test_infinite_gc_horizon_is_clamped_to_committed_version, test_infinite_gc_horizon_does_not_exceed_visible_version}` (2 tests). **Disclosed, not silently fixed:** the pre-existing `test_preview_compaction_candidate_partitions_matches_what_compact_once_actually_touches` passed only because its partition ids happened to coincide with its SST ids, so it never actually exercised `preview_compaction_candidates`'s partition-mapping logic; the real bug (`preview_compaction_candidates` was passing partition ids to the selector where SST ids were expected) was caught only by a server-level test exercising a busy-tablet stall, not by this rowstore-level test. The regression test that closes the gap is `crates/htap-rowstore/tests/preview_sst_ids.rs::test_preview_sst_ids_selects_dropped_partition_ssts_for_forced_compaction` (an exact 3-SST set where partition ids and SST ids deliberately differ). **Manifest watermarks, `HTAPMAN1` v2 -> v3:** `committed_version_high_water`/`gc_low_water`, both monotonic, refusing to publish a regression; `Engine::get`/`scan_partition`/`prepare`'s first-writer-wins check reject a real snapshot below `gc_low_water`. `crates/htap-rowstore/tests/manifest_v3.rs::{test_refused_regression_on_disk, test_refused_ledger_above_high_water}`; `crates/htap-rowstore/tests/gc_low_water.rs::test_gc_low_water_rejects_old_snapshots_and_persists_across_reopen`; `crates/htap-rowstore/tests/purge_reopen.rs::test_dropped_newest_partition_reopens_with_manifest_v3_high_water`; `crates/htap-rowstore/tests/wal_purge.rs::test_wal_gc_does_not_resurrect_purged_partition_after_reopen` (the new `Engine::flush_roll_and_gc`, one `commit_lock` acquisition covering flush, forced WAL roll, and WAL GC); and three pre-existing `crates/htap-rowstore/tests/external_ledger.rs` tests fixed to account for the v3 ledger/high-water cross-check. **The engine's own directory lock:** `Engine::open` takes an exclusive advisory lock on `<rowstore>/LOCK` for its lifetime; every test helper that opened a second, unlocked `Engine` against a live server's rowstore directory was audited and fixed (this itself uncovered a hollow test: the reclaim crash-orphan test's parent process dropped the table before spawning the crashing child, so the child's copy failed "not found" and never persisted a `Running` job — fixed alongside the lock). **Storage-review "V batch" fix:** `Engine::get`/`scan_partition` used to read `gc_low_water` via `self.commit_lock.lock().manifest.gc_low_water`, so a pure reader could block behind a writer holding `commit_lock` — a lock-order inversion (readers held `read_state.read()` and then took `commit_lock`, while every writer takes `commit_lock` and then `read_state.write()`) that could deadlock a concurrent reader and writer, e.g. a data-mover export racing an SQL INSERT or `compaction_tick`. `ReadState` now carries its own `gc_low_water` field, mirrored from `commit_guard.manifest.gc_low_water` under `commit_lock` at the same point flush/compaction already swap `read_state.ssts` (`Engine::open`, `flush_locked`, `compact_once`), so `get`/`scan_partition` only ever take `read_state.read()`. `crates/htap-rowstore/tests/concurrent_reads.rs::test_concurrent_reads_do_not_deadlock_with_flush_and_compaction` (real concurrent threads, bounded-time). `cargo test -p htap-rowstore --no-fail-fast`: 181 passed, 0 failed (includes both new tests). **Movement/reclaim leases (`htap-movement`):** `LocalDataMover`'s per-tablet lease set now serves both movement I/O and compaction, `try_acquire_reclaim_lease` (all-or-nothing) and `acquire_reclaim_leases_best_effort` (partial); `LocalDataMover::delete_tablet_movement_artifacts` deletes a tablet's package directory and every movement job record referencing it (S2), idempotently, only while the caller holds that tablet's reclaim lease. `crates/htap-movement/tests/leasing.rs::{best_effort_reclaim_reports_denied_tablets, concurrent_reclaim_holds_lease_while_callback_runs, reclaim_callback_deletes_tablet_artifacts_while_lease_is_held, reclaim_lease_releases_after_callback_success_error_and_panic, reclaim_leases_are_mutually_exclusive_and_release_after_drop, test_movement_and_reclaim_lease_mutual_exclusion}` (6 tests) and `crates/htap-movement/tests/movement_fixes.rs::{clone_retry_with_same_job_id_succeeds_while_movement_lease_is_held, csv_import_blocked_by_reclaim_lease_does_not_persist_orphaned_job, reclaim_can_delete_clone_artifacts_idempotently}` (3 tests, closing an M1/M2-class finding that a completed `clone_tablet` retry called an internal, unleased verify while already holding the lease, and that a refused lease used to leave an orphaned `Running` job record). **Storage-review "V batch" fix:** `LocalDataMover::delete_tablet_movement_artifacts`'s job-directory scan used to propagate `HtapError::Corruption` from any undecodable `JOB` file it encountered while looking for records belonging to the target tablet, so one corrupt, unrelated job directory (belonging to a *different* tablet) blocked reclamation of the tablet actually being cleaned up. It now skips a job entry it cannot decode (an absent or `HtapError::Corruption` result continues the scan) instead of failing the whole scan. `crates/htap-movement/tests/corrupt_job_isolation.rs::corrupt_unrelated_job_does_not_block_tablet_artifact_reclamation`. **Storage-review "X batch" fix (export leasing, X1):** `copy_to_csv_writer`/`copy_to_jsonl_writer`/`export_to_file` (COPY TO CSV/JSONL and file export, `crates/htap-movement/src/export.rs`) previously released their per-tablet movement lease before the scan-and-write completed on some paths, leaving a window where a reclaim lease (and therefore `compact_once`/`DROP TABLE` reclamation) could run against a tablet mid-export. The lease is now held (via the same `LocalDataMover` lease set movement and reclaim already share) for the export's entire scan-and-write; an export attempted while a reclaim lease is held on the tablet now fails with `HtapError::Conflict` instead of racing it. `crates/htap-movement/tests/export_leasing.rs::exports_hold_tablet_leases_against_reclaim`. **Storage-review "X batch" fix (fsync parent directories on artifact delete, X3):** `LocalDataMover::delete_tablet_movement_artifacts` now calls `sync_dir` on the tablets directory, the jobs directory, and the movement root after removing a tablet's package directory and its referencing job directories, so a crash immediately after deletion cannot leave the removed directory entries resurrectable from stale directory-metadata on reopen. **"Y batch" fix (Y3, missing jobs directory):** `delete_tablet_movement_artifacts` treats an absent `jobs/` directory (e.g. a tablet whose reclaim races a root that has never run a movement job) the same as an empty one and still runs its parent-directory fsyncs, rather than propagating the directory-open error. `crates/htap-movement/tests/missing_jobs_dir.rs::reclaim_succeeds_when_jobs_directory_is_missing`. **F5 disclosed, not fixed:** `delete_tablet_movement_artifacts` holds `LocalDataMover`'s single global lease-state mutex for its entire body — artifact removal, the job-directory scan, and all three `sync_dir` calls — so every lease acquire/release for *any* tablet stalls behind one tablet's reclaim deletion; a liveness/performance cost only, no correctness issue. `cargo test -p htap-movement --no-fail-fast`: 68 passed, 0 failed (includes both new tests). **`txn.journal` checkpoint (`htap-txn`):** `TransactionManager::checkpoint()` (new `HTAPTXC1` envelope, `crates/htap-txn/src/checkpoint.rs`) drops resolved `Intent`/`Commit`/`Abort` records past a durable, regression-refusing baseline; any error partway through the journal rewrite unconditionally latches `RecoveryCause::JournalIo`, even when a defensive re-open of the old handle happens to succeed (closing a finding that an earlier draft could return `Err` while leaving the manager holding a stale, untrusted handle). `crates/htap-txn/src/checkpoint.rs`'s own unit tests (`checkpoint_round_trips, absent_checkpoint_returns_none, corrupted_checkpoint_crc_is_rejected, future_checkpoint_version_is_rejected, publish_refuses_regressing_txn_id_high_water, publish_refuses_regressing_version_high_water, checkpoint_envelope_matches_golden_bytes, checkpoint_with_trailing_bytes_is_rejected, truncated_checkpoint_is_rejected`; 9 tests, corrected from an earlier miscount of 7 that omitted the last two) and `crates/htap-txn/src/manager.rs`'s unit tests (`test_checkpoint_crash_after_baseline_before_rewrite_preserves_evidence, test_checkpoint_crash_mid_rewrite_recovers_torn_replacement, test_checkpoint_crash_after_rewrite_before_handle_reopen_preserves_evidence, test_checkpoint_shrinks_journal_after_commits, test_checkpoint_reopen_has_fewer_records, test_small_journal_limit_is_sustained_by_opportunistic_checkpointing, test_recovery_uses_checkpoint_baseline_high_water_marks, test_checkpoint_refused_before_recovery, test_checkpoint_rejects_participant_version_mismatch`; 9 tests). `crates/htap-txn/tests/two_phase_commit.rs` gained one more test for the fold's Commit-with-no-Intent rejection. **Storage-review "X batch" fix (checkpoint against the bootstrap ceiling, X4):** `checkpoint()`'s journal rewrite read the journal through the manager's normal, already-reopened handle, which was reopened at the *configured* `max_journal_size` by the time `checkpoint()` could run — so a journal a single oversized commit had pushed past the configured limit could never be checkpointed at all, failing the opportunistic post-commit checkpoint on every subsequent commit. `checkpoint()` now reads the live journal through `self.configured_max_journal_size.max(RECOVERY_BOOTSTRAP_MAX_BYTES)` — i.e. whichever of the configured limit or the 2 GiB bootstrap ceiling is larger, not a fixed 2 GiB cap (a fixed 2 GiB ceiling would itself refuse a journal between 2 GiB and a larger configured limit) — so a journal within that ceiling but over the configured limit can still be folded and rewritten back under it. No test covers the more-than-2-GiB case, since it would require constructing a journal over 2 GiB. `manager::tests::test_checkpoint_compacts_journal_that_exceeds_configured_limit` (an oversized commit is checkpointed back under the configured limit, then a normal, non-bootstrap `Journal::open_with_options` accepts the result) and `manager::tests::test_finalize_open_restores_configured_journal_limit` (a bootstrap-ceiling reopen must not itself rewrite the journal; only `finalize_open`'s subsequent checkpoint-then-reopen-at-configured-limit does), both in `crates/htap-txn/src/manager.rs`. `cargo test -p htap-txn --no-fail-fast`: 85 passed, 0 failed. **"Y batch" fix (Y4, colstore directory fsync):** the reclaim callback's colstore step (`reclaim_tick_locked`) now calls `sync_dir` on `<root>/colstore` immediately after removing a tablet's colstore directory and before `delete_tablet_movement_artifacts`/the catalog CAS that sets `colstore_and_movement_reclaimed`, so a crash right after the removal cannot leave it resurrectable from stale directory metadata. No dedicated test: an fsync's effect is not observable from a normal (non-crash-injected) test. **`DROP TABLE` reclaim wiring and `compaction_tick` (`htap-server`):** `execute_drop_table` appends one `PendingReclaim` entry (`CatalogSnapshot.pending_reclaim`, `HTAPCAT1` v4 -> v5) in the same CAS that removes the table; `LocalServer::reclaim_tick`/`compaction_tick` reclaim colstore/movement artifacts and confirm rowstore purge (via `partitions_possibly_present_in_ssts`/`_in_memtables`, exact presence, not the prior conservative min/max-range check that could never report a sandwiched dropped partition as fully absent) across as many ticks as needed, removing the entry once both flags are true. `compaction_tick`'s GC horizon is `min(TransactionManager::visible_version(), every pinned session snapshot, every in-flight conversion's snapshot_version)` minus a configurable `with_gc_horizon_retention_slack` (default 0); two production bugs found and fixed during this work: the tick's own forced-priority lease initially collided with its own best-effort re-request on the same tablets (self-denial, so dropped tables were never purged), and the busy-tablet preview initially passed partition ids to the selector as SST ids (fixed, see the rowstore section above). **Storage-review "X batch" fix (`entries_purged` only counts confirmed CAS successes, X5):** `compaction_tick`'s returned `CompactionTickReport.entries_purged` used to count every `pending_reclaim` entry the tick *attempted* to mark `rowstore_purge_confirmed`, regardless of whether the catalog CAS marking them actually succeeded; a concurrent CAS conflict (`HtapError::Conflict`, silently retried on the next tick) could make the report overstate how many entries were durably confirmed on that call. `entries_purged` is now assigned only inside the `Ok(())` branch of the CAS, so it is `confirmed.len()` on success and `0` on a lost race, never an attempted-but-unpublished count; exercised by the existing `entries_purged > 0` assertion in `crates/htap-server/tests/compaction_tick.rs::dropped_table_is_purged_from_rowstore_before_pending_entry_is_removed` (no dedicated concurrent-CAS-conflict regression test exists for the undercount-on-conflict path itself). **Storage-review "V batch" fix (convergence cap fallback):** the protection-convergence loop above (bounded at `MAX_ITERATIONS = 8`) protects every partition denied a lease in each iteration; if it still has not stabilized when the cap is reached, `compaction_tick` now skips the `compact_once` call entirely for that tick (`per_iteration_reports` stays empty) but still runs `flush_roll_and_gc`, purge confirmation, catalog CAS, and `reclaim_tick_locked` exactly as it would on a converged tick, returning `ran: true` with `reason: Some("compaction candidate protection did not converge")` rather than silently doing nothing or erroring. `crates/htap-server/tests/compaction_convergence.rs::leased_dropped_tablets_are_all_protected_in_one_tick`. `crates/htap-server/tests/compaction_tick.rs::{horizon_protects_an_open_transaction_snapshot, commit_and_rollback_unregister_snapshot_and_allow_versions_to_collapse, dropped_table_is_purged_from_rowstore_before_pending_entry_is_removed, movement_lease_blocks_compaction_rewrite_of_busy_tablet, best_effort_compaction_runs_bounded_iterations_with_busy_tablet, compaction_tick_refuses_while_transaction_recovery_is_required}` (6 tests); `crates/htap-server/tests/reclaim.rs::{test_drop_converted_table_reclaims_colstore_before_returning, test_drop_then_create_same_name_isolates_new_artifacts_and_reclaims_old_after_lease_release, test_drop_while_movement_job_holds_tablet_lease_stays_pending, test_crash_orphaned_running_job_is_failed_and_reclaimed_at_open, test_open_finalizes_and_compacts_oversized_transaction_journal, test_open_time_reclaim_failure_does_not_fail_open}` (6 named tests; `cargo test -p htap-server --test reclaim` reports 5 passed, 1 ignored — `test_open_time_reclaim_failure_does_not_fail_open` is `#[ignore]`d because no deterministic public hook can force only the best-effort open-time reclaim callback to fail; `test_open_finalizes_and_compacts_oversized_transaction_journal` is the end-to-end oversized-journal self-heal through `LocalServer::open`/`finalize_open`); `crates/htap-server/tests/sandwiched_purge.rs::dropping_sandwiched_partition_preserves_unrelated_tables`; `crates/htap-server/tests/tier_shift_protection.rs::test_compaction_protects_ssts_containing_leased_tablet_partition` (proves protecting one SST makes a different tier eligible without rewriting any unleased partition); `crates/htap-server/tests/movement_artifacts_reclaim.rs::test_dropped_table_reclaim_deletes_movement_package_and_job_artifacts` (S2); `crates/htap-server/tests/purge_no_resurrection.rs::test_confirmed_dropped_rowstore_purge_does_not_resurrect_after_restart`. **Catalog `pending_reclaim` (`htap-catalog`):** `crates/htap-catalog/tests/catalog_recovery.rs`'s v5 round-trip/legacy-decode/validation tests (byte-level envelope-version assertions, v1-v4 legacy decode with `pending_reclaim == []`, a hand-crafted v5 payload missing the key rejected as `Corruption`, `validate()` rejecting overlap/duplicate cases). Workspace: `cargo test --workspace --no-fail-fast` — 1469 passed, 0 failed, 1 ignored. See ADR-024 in `docs/DECISIONS.md`, `docs/ARCHITECTURE.md`'s "Rowstore compaction, garbage collection, and DROP TABLE reclaim (Phase 15)" and "Transaction journal checkpoint (Phase 15)" sections, and `docs/LIMITATIONS.md`'s matching "scope and deferred features" sections for the full contract and disclosed gaps (shared-keyspace protection is per SST not per row; the explicit-SST-id path compacts one contiguous run per call; compaction is explicit-tick-only; tombstones are never elided; movement/reclaim leases are intentionally non-durable; `ALTER TABLE ... DROP/REORGANIZE PARTITION` and demoted column files are not reclaimed by this path; the `MANIFEST` v2 external-apply ledger's own hard cap is untouched; three liveness-only gaps disclosed, not fixed: a post-manifest-publish `compact_once` error leaves the in-memory manifest stale until reopen, blocking later flushes; a flush's new SST reader can fail to open after its manifest publish, in which case a later compaction touching that SST fails safely with `Corruption` until reopen; and an undecodable movement job directory is skipped by reclaim, never deleted or reported). A final "X batch" review round found and fixed five further issues (X1: exports now hold their tablet lease for the whole scan-and-write, so a reclaim lease and an export can no longer race the same tablet; X2: `compact_once`'s GC horizon is clamped and always raises `gc_low_water` to match, closing a silent-stale-read gap the removed `u64::MAX` write-side exemption left open; X3: tablet artifact deletion now fsyncs its parent directories; X4: `checkpoint()` reads the journal through a raised ceiling `open`/`recover()` also use, so an oversized journal can still be checkpointed back under its configured limit instead of failing every opportunistic checkpoint; X5: `entries_purged` only counts confirmations whose catalog CAS actually succeeded) and disclosed two latent, currently-unreachable API hazards in `docs/LIMITATIONS.md` (`TransactionManager::new` does not itself load the durable checkpoint baseline — its only caller, `open_with_options`, overwrites it immediately after; the `txn.checkpoint` file name is fixed per directory, which would collide if two journals ever shared one directory, which `LocalServer` never does). A follow-up "Y batch" storage re-review then corrected two of those fixes and added three more findings: **X2 was re-clamped to `visible_version`**, not `committed_version` — clamping to `committed_version` could raise `gc_low_water` above `visible_version` whenever `apply_external` had committed ahead of what was published, rejecting every fresh snapshot; `committed_version` still feeds `committed_version_high_water` unchanged. **X4's ceiling is `max(configured_max_journal_size, RECOVERY_BOOTSTRAP_MAX_BYTES)`**, not a fixed 2 GiB cap, since a fixed cap would itself refuse a journal between 2 GiB and a larger configured limit. **Y3:** `delete_tablet_movement_artifacts` now treats a missing `jobs/` directory as empty rather than propagating its open error, still running the parent-directory fsyncs. **Y4:** the colstore reclaim step now fsyncs `<root>/colstore` right after removing a tablet's colstore directory, before the catalog CAS. **F5 disclosed, not fixed:** `LocalDataMover`'s single global lease-state mutex is held across artifact deletion, the job-directory scan, and all parent-directory fsyncs, so any tablet's lease acquire/release stalls behind another tablet's reclaim deletion (a performance-only cost). This pass also corrected a pre-existing miscount: `checkpoint.rs` has 9 unit tests, not 7 (`checkpoint_with_trailing_bytes_is_rejected` and `truncated_checkpoint_is_rejected` were previously omitted from the named list). Updated workspace total: `cargo test --workspace --no-fail-fast` — 1469 passed, 0 failed, 1 ignored. |
| Phase 16 — Owner plus IPC (concurrent multiprocess use) | `Complete (local MVP)` | No on-disk format change, no new durability invariant (ADR-004/008/009 unchanged); root-lock arbitration itself is unchanged from Phase 6 (`1083fbd`). **Batch A — foundations:** A1 split the server's duplicated statement pipeline into a shared `LocalServer::prepare_statement` (catalog load, visibility check, bind, privilege check) plus each caller's own dispatch, fixing `docs/PROBLEMS.md` P3; `crates/htap-server/src/session.rs::session::tests::session_statement_checks_privileges_once` proves the privilege check runs exactly once for a session-driven statement (shown to fail against the pre-fix double-call code first, per the Phase 15 regression-test lesson). A2 added `HtapError::Ambiguous(String)` with an `is_ambiguous()` predicate (`crates/htap-common/src/error.rs::error::tests::test_error_display_formatting`) mapped to the same non-retryable MySQL code as `DurablePending`/`RecoveryRequired`, pinned away from any driver-auto-retry code by `crates/htap-wire/src/error_map.rs::error_map::tests::ambiguous_never_maps_to_the_retryable_conflict_code`. A3 turned on the vendored parser's own `serde` feature (additive manifest changes only, no vendored-source edit) and derived serialization on the SQL result types, the statement AST, and `Principal`: `crates/htap-sql/src/result.rs::result::tests::{test_command_result_round_trip_serialization, test_query_result_round_trip_serialization, test_statement_result_serializes_as_command, test_statement_result_deserializes_as_query}`, `crates/htap-sql/src/ast.rs::ast::tests::{test_ast_partition_ddl_serialization, test_ast_select_with_placeholders_serialization, test_ast_insert_with_binary_literal_serialization, test_ast_nesting_depth_within_limits}` (the binary-literal test confirms the parser's literal-value type round-trips a byte sequence containing zero bytes and bytes above the ASCII range bit-identically, the concrete non-text-safe case this phase's AST-forwarding decision depends on), and `crates/htap-server/src/session.rs::session::tests::{test_principal_superuser_serialization, test_principal_account_serialization}`. **Batch B — protocol, ownership graph, owner/client split:** B1 renamed the old `LocalServer` body to `OwnedServer`, introduced `ServerMode` (`Owner(OwnerRuntime)` / Unix-only `Client { client: IpcClient }`), and gave `Session` the matching `SessionBackend::{Owner, Remote}` split; `OwnerRuntime`'s hand-written `Drop` joins the IPC listener before its own `OwnedServer` handle drops, so `<root>/LOCK` is never released while a listener thread might still touch storage — `crates/htap-server/tests/session_open.rs::{test_open_owner, test_concurrent_open_conflict}` cover the owner path and the second-opener-becomes-a-client path this graph makes possible. B2 (`crates/htap-server/src/ipc/protocol.rs`) defined the length-prefixed JSON frame protocol (a four-byte big-endian length, symmetric 16 MiB cap checked before allocation on both sides) and every request/response/error shape: `ipc::protocol::tests::{all_requests_round_trip, all_response_payloads_round_trip, all_errors_are_mirrored, counter_overflow_names_use_known_static_values_or_fallback, non_utf8_binary_values_round_trip, non_finite_floats_are_rejected_and_negative_zero_is_preserved, deeply_nested_ast_is_rejected_without_panicking, oversized_request_and_response_frames_are_rejected_before_allocation, oversized_write_is_rejected_without_writing}` (9 tests). B3 (`crates/htap-server/src/ipc/owner.rs`) is the owner-side listener at `<root>/htap.sock` (mode `0600`, stale-socket cleanup before bind, non-fatal lock-only fallback on a bind failure or a non-socket file at that path): `ipc::owner::tests::serialization_failure_is_returned_as_a_normal_error_frame` (renamed by batch F below to `post_dispatch_serialization_failure_returns_ambiguous_outcome` once its classification changed from `InvalidArgument` to `Ambiguous`) plus the integration suite `crates/htap-server/tests/ipc_owner_{execute, handshake_ok, handshake_mismatch, session_txn, disconnect_mid_txn, shutdown_bounded, non_socket_file, socket_path_too_long}.rs` (8 files, `execute_matches_in_process_result`, `handshake_and_execute_succeed`, `handshake_mismatch_returns_error_then_closes`, `transaction_commit_is_visible_afterwards`, `disconnect_rolls_back_open_transaction`, `dropping_owner_with_idle_client_is_bounded_and_releases_lock`, `regular_socket_path_file_keeps_owner_in_lock_only_mode`, `too_long_socket_path_keeps_owner_in_lock_only_mode`). B4 (`crates/htap-server/src/ipc/client.rs`) is the client-side connector (bounded startup-race retry, and the write-boundary rule: zero request bytes written or a statically-read-only request is `Conflict`, any possibly-written non-read-only request is `Ambiguous`, never auto-retried), proven against a fake in-process listener by `ipc::client::tests::{connect_to_missing_or_gone_socket_returns_conflict, connect_handshake_that_never_answers_returns_conflict_within_deadline, fully_written_execute_without_response_is_ambiguous, closed_peer_before_request_write_returns_conflict, catalog_snapshot_mid_response_returns_conflict, transport_failure_classification_obeys_write_boundary}` (6 tests, covering all five boundary cases from the plan plus the classification-function unit test). **Batch C — session forwarding, fallback, real multiprocess tests:** C1 added `SessionBackend::Remote` forwarding for every reachable session method, an `AmbiguousOutcomePending` quarantine state (mirroring `DurablePending`'s existing re-raise contract, including at the `reset` gate) and a terminal `RemoteDisconnected` state, proven end-to-end against a real in-process owner listener over a real socket by `crates/htap-server/tests/ipc_client_session_{plain_select, begin_execute_commit, rollback, ddl_rejected_in_txn, ambiguous_commit, ambiguous_bootstrap, ambiguous_autocommit_dml, disconnected_before_request, catalog_snapshot}.rs` (9 files, 1 test each: `remote_session_plain_select_reads_preexisting_data`, `remote_session_begin_execute_commit_persists_insert`, `remote_session_rollback_discards_buffered_insert`, `remote_session_ddl_in_transaction_matches_local_rejection`, `remote_session_preserves_ambiguous_commit_outcome`, `bootstrap_root_account_preserves_ambiguous_outcome`, `remote_session_preserves_ambiguous_autocommit_dml_outcome`, `remote_session_preserves_disconnect_before_request_outcome`, `remote_session_forwards_catalog_snapshot_request`). A privilege gap found by review after C1's first pass — a client-mode session kept the superuser principal because `authenticate_session` returned `Unsupported` instead of forwarding — was closed by making it forward for real; `crates/htap-server/tests/ipc_client_session_privilege_enforcement.rs::ipc_client_session_enforces_account_privileges` proves a non-superuser account is refused an ungranted privilege identically on a local and a remote session. C2 restricted the whole `ipc` module, `ServerMode::Client`, and `SessionBackend::Remote` to `#[cfg(unix)]`, with the pre-Phase-16 `Conflict` behavior preserved unconditionally outside Unix; verified by read-through of the non-Unix branch only, since this workspace's CI runs Linux exclusively — no cross-compiled build was run. C3 (`crates/htap-server/tests/ipc_multiprocess.rs`, with a new `server_ipc_child` binary following the existing `server_lock_child` pattern) exercises the real end-to-end path with actual child processes: `first_opener_owner_second_opener_client_visible_write` (owner + client, a client-side write visible to a fresh client read), `startup_race` (a client connects while the spawned owner is still starting, succeeding within the bounded backoff), `kill_signal_mid_connection` (a real `SIGKILL` to the owner mid-request; the in-flight client call returns `Conflict` or `Ambiguous`, never panics or hangs — this real-process scenario proves the end-to-end path fails safely, not every classification branch, which is what B4's fake-listener tests are for), `post_kill_takeover` (a fresh open after the kill becomes the new owner, proving the lock and socket both release cleanly), and `eight_simultaneous_openers` (a real eight-way barrier race of concurrent same-root opens, each opened handle held until every opener has returned so the winner's listener cannot tear itself down mid-race; asserts exactly one opener becomes owner, the other seven become clients, every opener succeeds with no error, and the whole race completes within a bounded 10-second deadline — fixed from an earlier draft that dropped each handle immediately after opening it, which let the winner's listener shut down and remove the socket while the rest were still connecting, so it only ever raced 2 openers in practice). Workspace after C3 landed (pre-batch-D baseline, superseded by the count below): `cargo test --workspace --no-fail-fast` — 1523 passed, 0 failed, 1 ignored; `cargo clippy --workspace --all-targets -- -D warnings` clean. **Batch D — storage-review and external-review fixes:** a storage review of the Batch A-C2 diff (verdict: fix-first) plus an external review found 13 defects the passing test suite above had not caught. Both reviews found defects the passing suite had missed; the clearest are a completely broken prepared-statement path for every client-mode session and outright process panics on client-mode handles, neither of which had ever been exercised by a test before this batch. D1 (never letting a durable-pending outcome be silently downgraded): a `DurablePending` received over IPC is now latched into `SessionState::CommitOutcomePending` the instant `Session::remote_request` sees it, before it can be re-observed as a retryable `Conflict` after a later disconnect — `crates/htap-server/tests/ipc_client_session_durable_pending_latch.rs::remote_session_latches_durable_pending_commit_outcome`. D2 (stale session status after an error): `IpcConnection::request` now returns the response's `SessionStatus` on the error path too, not only on success, so `Session::in_transaction()` cannot lie after a transaction-ending failure — `ipc_client_session_status_after_error.rs::remote_session_applies_status_from_failed_commit_response`. D3 (client-mode panics): the blanket `Deref` from `LocalServer` to the storage core is gone; roughly 30 owner-only methods (`compaction_tick`, `reclaim_tick`, `data_mover`, `txn_manager`, `colstore_dir`, `convert_table*`, `conversion_tick`, `tick`, `load_job`, `resume_job`, and the rest of the administrative surface) return `HtapError::Unsupported` instead of panicking on a client-mode handle, and `Debug` formatting plus the five configuration knobs' `with_*`/`set_*` forms are safe no-ops — `ipc_client_mode_unsupported_methods.rs::ipc_client_mode_owner_only_methods_return_unsupported_without_panicking`. D4 (broken prepared statements — the clearest defect either review found): the owner's `VisibilityCheck` handler had been discarding the real `CatalogSnapshot` and returning an empty one, so every prepared statement failed for a client-mode session; it now returns the real snapshot — `ipc_client_session_visibility_check.rs::remote_session_forwards_statement_visibility_check`. D5 (startup-reclaim/listener race): `LocalServer::open` now runs `reclaim_tick_locked(true)` under `execution_lock` strictly before starting the IPC listener, so a client can never execute while startup reclaim is deleting a dropped table's tablet directories (read-through verified against `crates/htap-server/src/lib.rs::open`; the existing reclaim suite staying green after the reorder was the check, no new dedicated test). D6 (silent close on an undecodable frame): a fully received but undecodable owner-side request frame now gets a definite `InvalidArgument` error reply instead of a bare disconnect, so a client never reports an unknown outcome for a statement that provably never ran — `ipc_owner_undecodable_frame.rs::undecodable_frame_returns_error_then_closes`. D7/D8 (a local failure wrongly treated as terminal): a purely local pre-send failure (an oversize request, rejected before any byte reaches the socket) is now classified `HtapError::InvalidArgument` ahead of the write-boundary rule instead of latching the session terminal, and the session stays usable for the next statement (the `set_max_allowed_packet` remote path was fixed the same way in passing, now checking the terminal-state gate up front and no longer swallowing its transport error) — `ipc_client_session_oversize_request.rs::oversized_request_is_rejected_locally_without_poisoning_session`. D9 (unbounded handshake, blocking rollback on drop): the owner now bounds its handshake read to 5 seconds, and dropping a remote-backend `Session` no longer issues a blocking rollback request over the socket — `ipc_owner_handshake_timeout.rs::silent_handshake_times_out_and_releases_connection_slot`. D10 (fabricated bootstrap report): a client-mode `bootstrap_root_account` now returns the owner's real `BootstrapReport` instead of a fabricated local one (verified by code read-through of `LocalServer::bootstrap_root_account`'s client-mode arm). D11 (non-fatal startup paths treated as fatal): `ipc::owner::start` now treats a root-canonicalize failure, a stale-socket removal failure, and a bind failure identically as a non-fatal lock-only fallback rather than failing `LocalServer::open` itself (read-through verified). D12 (string-based disconnect classification): disconnect classification is now a structural match on a dedicated `WireError::OwnerUnreachable`/`WireError::Ambiguous` variant instead of a string-prefix check (read-through verified). D13 is **not fixed**, and is recorded as a limitation instead: `Session::change_user` still checks "this is a client-mode session" before the terminal-state (quarantine/disconnected) gate, so a client-mode session already in `AmbiguousOutcomePending`/`RemoteDisconnected` gets the plain `Unsupported` error for `change_user` rather than its stored terminal error — a cosmetic difference only, since `change_user` was always going to fail in client mode either way. See `docs/LIMITATIONS.md`'s "Owner plus IPC (Phase 16) scope and deferred features" for the two limitations this batch adds (a client-mode `LocalServer`'s configuration setters are accepted no-ops; the `change_user` gate-ordering quirk above). Workspace after batch D: `cargo test --workspace --no-fail-fast` — 1530 passed, 0 failed, 1 ignored (re-measured for this update); `cargo clippy --workspace --all-targets -- -D warnings` clean. **Batch E — second storage re-review, plainly stated: this round found a security-relevant window and another process-panic path, and batch D's own "client-mode panics are fixed" claim was incomplete.** A second storage re-review of the batch D diff (verdict: fix-first) found 8 more defects. E1 (HIGH): `open_session` still panicked in 3 places in client mode — the exact defect class D3 claimed to have closed — and `authenticate_session` inherited it, so a dead or connection-saturated owner crashed the calling process (`htap-wire` hit this once per incoming connection) instead of returning an error; `LocalServer::open_session` is now `Result`-returning end to end, `authenticate_session` propagates the error, and `htap-wire`'s server module reports it to the client instead of unwinding — this changed `open_session`'s public signature, mechanically updating roughly 83 existing call sites across `crates/htap-server/tests/*.rs` (e.g. `crates/htap-server/tests/session.rs`, `session_recovery.rs`, `session_concurrency.rs`, `compaction_tick.rs`, `explain.rs`, `privileges.rs`) to `.unwrap()`; there is no dedicated regression test reproducing the dead-owner/connection-cap failure itself, so this fix is verified by code inspection (no remaining panic path) plus every session-opening test passing against the new fallible signature, not by a test that names the negative scenario directly. E2 (MED): a read-side `InvalidArgument` (an undecodable or oversize response) used to bypass the write-boundary rule, reporting a possibly-applied mutation as a local input error and leaving an oversize response's unread bytes to desynchronize the next request on that connection; fixed so only a genuinely pre-write local failure keeps `InvalidArgument`, every other `read_frame` failure is `Ambiguous` (non-read-only) or `OwnerUnreachable` (read-only), and the connection is marked dead after any framing failure — `crates/htap-server/src/ipc/client.rs::ipc::client::tests::malformed_or_oversized_response_marks_connection_dead_and_preserves_outcome_classification`. E3 (MED, security): the socket used to be created with the process's default file-creation-mask permissions and tightened to `0600` a moment later, directly at the published, world-traversable-directory path — another local user connecting in that window would have gotten an unauthenticated superuser session; the socket is now created inside a private, owner-only (`0700`) directory, tightened there, and atomically renamed into place, closing the window entirely — `crates/htap-server/src/ipc/owner.rs::ipc::owner::tests::published_socket_is_owner_only` asserted the published socket's mode at the time (batch F below found this test reimplemented publication itself rather than exercising the real startup path, and replaced it). E4 (LOW-MED): the handshake bound was a per-read socket timeout that a peer trickling one byte at a time could reset indefinitely, letting enough such connections make the owner unreachable; fixed to one absolute deadline across the whole frame read on both sides, while an established session (past the handshake) still has no deadline at all — `crates/htap-server/src/ipc/protocol.rs::ipc::protocol::tests::handshake_frame_deadline_is_absolute_when_peer_trickles`. E5 (LOW, a C2 regression): `Session::remote_request` was missing `#[cfg(unix)]`, breaking the non-Unix build; restored, verified by read-through only (this workspace's CI runs Linux exclusively, matching C2's own precedent). E6 (LOW): `catalog_snapshot`, `check_statement_visible`, and `set_max_allowed_packet` checked the terminal-state quarantine gate only on the remote branch, so an owner-backed (local) session could call them past quarantine while a remote one could not; the gate now runs once, ahead of the owner/remote split, so both modes agree — no dedicated new test, verified by code read-through plus the existing owner-mode quarantine tests in `crates/htap-server/tests/session.rs` staying green. E7 (LOW, documentation): the `last_query_*` diagnostic getters' client-mode defaults (`1`/`false`/`0`, not the owner's real last-query state) are now documented in `docs/LIMITATIONS.md`; no behavior change. E8 (LOW): `IPC_PROTOCOL_VERSION`'s doc comment now states which payload types force a manual bump, since a serde-layout mismatch in one of those types could otherwise pass the handshake's numeric-constant check and only fail later at frame decode — the underlying condition E2's fix protects against. Workspace after batch E (re-measured for this update): `cargo test --workspace --no-fail-fast` — 1533 passed, 0 failed, 1 ignored; `cargo clippy --workspace --all-targets -- -D warnings` clean. See ADR-025 in `docs/DECISIONS.md` (including its "Post-review fixes (batch D)" and "Post-review fixes (batch E)" sections), `docs/ARCHITECTURE.md`'s "Concurrent multiprocess use: owner plus IPC (Phase 16)" section, and `docs/LIMITATIONS.md`'s "Owner plus IPC (Phase 16) scope and deferred features" for the full contract and disclosed gaps. **Batch F — third storage re-review: two of the batch D/E fixes above had shipped with tests that could not have caught a regression, and writing a real test for one of them immediately exposed a further defect; both patterns are worth naming plainly.** A third storage re-review of the batch E diff (verdict: fix-first) found 7 more defects. F1 (E3's own regression test could not fail): `ipc::owner::tests::published_socket_is_owner_only` reimplemented the bind-inside-a-private-directory-then-rename publication sequence itself rather than calling `LocalServer::open`'s real startup path, so it kept passing even against a reverted fix; it is replaced by `crates/htap-server/tests/ipc_owner_socket_permissions.rs::startup_publishes_owner_only_socket_and_cleans_up_staging_directory`, which opens a real `LocalServer` and asserts the published `<root>/htap.sock` is a socket, mode `0600`. F2 (the staging-directory leak F1's new test then found): the private `.htap-ipc-<pid>-<id>` directory `ipc::owner::start` binds inside before publishing was never removed on the successful path, so every server that ever started left one behind in `<root>`; it is now removed immediately after the atomic rename publishes the socket, and `cleanup_stale_private_directories` scans for and removes any left by a crashed prior owner at startup, before a new private directory is created — the same regression test above asserts no `.htap-ipc-*` directory remains after a normal successful open (not merely after a crash), which is the specific stale claim batch E's docs left uncorrected (see `docs/OPERATIONS.md` and `docs/LIMITATIONS.md`). F3 (E1's own no-panic fix had no test either): `crates/htap-server/tests/ipc_client_open_session_owner_gone.rs::ipc_client_session_operations_error_when_owner_has_gone_away` now proves that, with the owner gone, both `open_session` and `authenticate_session` on a client-mode handle return an error within a bounded one-second deadline instead of panicking. F4 (a post-dispatch response-framing failure was reported as bad input): if a response failed to serialize *after* its statement had already run against the owner's storage, `ipc::owner::write_response` reported it as `HtapError::InvalidArgument` ("bad input"), which could have invited a caller to retry a mutation that had already happened; it now reports `HtapError::Ambiguous` (unknown outcome) instead, matching the write-boundary rule E2 already established for the read side. Latent today only, because no write path yet returns rows through this frame — it would have gone live the moment one did. Test: `ipc::owner::tests::post_dispatch_serialization_failure_returns_ambiguous_outcome` (renamed and re-targeted from B3's original `serialization_failure_is_returned_as_a_normal_error_frame`, which asserted the now-fixed `InvalidArgument` behavior; that name no longer exists). F5 (a failed write with bytes already on the wire didn't mark the connection dead): unlike the read side (E2), `IpcConnection::request`'s write path did not set the connection's `dead` flag when a partial write failed after some request bytes had already reached the OS, leaving a connection an out-of-tree caller could keep reusing after a desynchronizing partial write. It now marks the connection dead in that case too. Test: `crates/htap-server/src/ipc/client.rs::ipc::client::tests::partial_request_write_marks_connection_dead`. F6 (owner-gone login misreported as bad credentials): with the owner unreachable, every login attempt through `htap-wire`'s `authenticate` (and every `COM_CHANGE_USER` through `respond_change_user`) fell into the generic `Err(_)` arm and reported `ER_ACCESS_DENIED`/"Access denied", which would send an operator chasing a password problem that did not exist. Both call sites now match `HtapError::Conflict` first and report the real transport failure via `ER_UNKNOWN` instead. Verified by code read-through only; no dedicated wire-level integration test names this scenario (matching the read-through-only precedent already set by D5/D10-D12/E1/E5/E6 above). F7 (an interrupted system call during the handshake failed spuriously): `read_exact_until` — used only for the bounded handshake read on both the owner's accept path and the client's connect path (E4) — used to return `error.kind() == Interrupted` straight to the caller as a hard failure instead of retrying the read; a single `EINTR` (e.g. from an unrelated signal arriving mid-syscall) could fail an otherwise-healthy handshake. It now retries the same read against the same absolute deadline on `Interrupted`, exactly like the ordinary post-handshake path already does. Verified by code read-through only; no dedicated `EINTR`-injection test exists (this crate has no seam to inject one). Workspace after batch F (re-measured for this update): `cargo test --workspace --no-fail-fast` — 1535 passed, 0 failed, 1 ignored (see the exact command output cited at the end of this row's evidence); `cargo clippy --workspace --all-targets -- -D warnings` clean. See ADR-025's "Post-review fixes (batch F)" section, and `docs/LIMITATIONS.md`'s "Owner plus IPC (Phase 16) scope and deferred features" (updated to cite the replacement integration test and the staging-directory cleanup fix in place of the now-superseded `published_socket_is_owner_only` unit test) for the full contract. |
| Phase 17 — `DECIMAL` type: query layer and persistence (A6a + A6b, all five tasks) | `Complete (local MVP)` | **A6a (query layer, complete).** `DataType::Decimal { precision, scale }` / `Value::Decimal { value: i64, precision, scale }` (`crates/htap-common/src/types.rs`, `MAX_DECIMAL_PRECISION = 18`): a fixed-point value stored as a signed 64-bit integer scaled by a power of ten, exact arithmetic with round-half-away-from-zero, a declared-precision check on every value (`check_decimal_precision`), and numeric ordering/hashing consistent with each other. **Amendment 4 (derivation, not storage): a derived *result* precision/scale (`+`/`-`/`*`/`/`/`%`) is clamped to the 18-digit maximum rather than rejected** — MySQL's own behavior for an over-wide derived type — while a *value* that does not fit its declared precision is still a hard error and nothing ever silently becomes a `Float64`; see `docs/DECISIONS.md`'s ADR-026, lifting an earlier, narrower position (worst-case-derived-precision pre-rejection made ordinary money arithmetic like `SUM(amount_cents * 0.01)` or `DECIMAL(15,2) * DECIMAL(15,2)` unrepresentable). Decimal literals bind exact-numeric text to `DECIMAL` (MySQL-compatible; exponent notation still binds `Float64`), `CAST` supports the `DECIMAL`/`NUMERIC`/`DEC` spellings with MySQL's own precision/scale defaults, and `SUM`/`AVG`/`MIN`/`MAX`/`COUNT(DISTINCT)` work over decimals on both `Route::Query` and the narrow `Route::OlapScan` accumulator (`crates/htap-server/src/olap.rs`'s new `Accumulator::SumDecimal`), agreeing with the windowed form. Covered by `crates/htap-common/src/types.rs::{test_decimal_display_ordering_and_hashing, test_decimal_numeric_ordering_and_hashing_across_representations}`, `crates/htap-sql/src/expr.rs::{test_decimal_exact_sum_and_float_difference, test_decimal_rounding_half_away_from_zero_for_rescale_division_and_cast, test_decimal_arithmetic_overflow_and_precision_errors, test_decimal_required_precision_is_clamped_to_supported_bound, test_decimal_string_parsing_is_exact_and_rejects_non_decimal_syntax, d7_decimal_division_rounds_non_tie_remainders_correctly, d9_decimal_division_rounding_direction_uses_exact_quotient_sign, decimal_arithmetic_and_comparison, decimal_casts_integral_sources_at_target_scale}`, `crates/htap-sql/tests/query_bind.rs::{test_decimal_literal_binding, test_cast_to_decimal_binding}`, and `crates/htap-server/tests/decimal_aggregation.rs` (`test_tpch_style_money_aggregation_uses_exact_decimal_precision`, `test_decimal_sum_avg_min_max_and_distinct_count`, `test_decimal_sum_reports_precision_overflow`, `test_decimal_window_sum_and_avg_match_grouped_aggregation`, `test_columnar_and_row_paths_agree_on_decimal_aggregation`, `test_decimal_to_double_assignment_and_union_with_integers`). **A6b (persistence, all five tasks landed).** Task 1 relocated the decimal-text parser and precision checker from the SQL crate's private scope into `htap-common`'s public surface with a context-neutral signature, so the movement and wire crates can reuse them without a new SQL-crate dependency (no behavior change; `crates/htap-sql`'s own suite stays green). Task 2 (**the columnar segment format bump, gated on storage-review sign-off — see below**) added `DECIMAL` as `htap-colstore`'s eighth column type: `HTAPCOL1`'s `FORMAT_VERSION` 1 -> 2, `MIN_DECODABLE_VERSION` unchanged at 1 (so the accepted range on read widens to `1..=2`), `DECIMAL_INTRODUCTION_VERSION = 2` gating a new guard that rejects a legacy-tagged segment whose schema declares a decimal column, real decimal cases in the block encode/decode paths and the per-row predicate matcher (so decimal pushdown filtering is real, not excluded), and the composite-key codec's own decimal branch (mirroring the existing `Int64` branch exactly — see the key-codec invariant note in `docs/ARCHITECTURE.md`). Covered by `crates/htap-colstore/tests/segment_roundtrip.rs::{test_roundtrip_all_eight_types_and_forced_small_blocks, test_nullable_decimal_roundtrip_across_blocks_and_null_zone_map, test_corruption_decimal_payload_outside_declared_precision, test_corruption_footer_schema_out_of_range_decimal_type}`, `crates/htap-colstore/src/segment.rs`'s own unit tests (`segment::tests::{legacy_format_still_readable, legacy_version_with_decimal_rejected, decimal_roundtrip, decimal_multiblock_filter_eq_and_range}`), `crates/htap-colstore/tests/scan.rs::{test_decimal_lt_across_multiple_blocks_prunes_zone_maps, test_decimal_lte_across_multiple_blocks_prunes_zone_maps, test_decimal_gt_across_multiple_blocks_prunes_zone_maps, test_nullable_decimal_gt_and_gte_retain_null_containing_blocks}` (real pushdown, not residual-only filtering), and `crates/htap-common/src/keycodec.rs::{test_decimal_encoding_uses_normalized_unscaled_value, test_decimal_encoding_preserves_unscaled_order}`. Task 3 added the missing exact-numeric `CREATE TABLE` branch (`crates/htap-sql/src/binder.rs`, defaulting precision/scale per MySQL's own rules), fixed the literal-insert coercion path to use task 1's relocated helpers, taught the rowstore memtable's in-memory size estimator to count decimal bytes instead of erroring, and extended catalog recovery evidence: `crates/htap-catalog/tests/catalog_recovery.rs::{test_decimal_schema_json_round_trip_preserves_parameters, test_catalog_snapshot_zero_scale_decimal_round_trip, test_catalog_invalid_decimal_type_tag_is_corruption}` (the last is the unbumped-envelope fail-loud evidence cited by the ADR-008 addendum below), and `crates/htap-rowstore/src/memtable.rs::memtable::tests::test_decimal_value_is_persistable`. `crates/htap-server/tests/decimal_pointread.rs::test_decimal_primary_key_point_read_normalizes_scale` proves a `DECIMAL(10,2)` primary key inserted as `5.50` is found by `WHERE pk = 5.5` (a differently-scaled literal) **and** that the query plan is `RowstorePointRead` (both halves asserted, per R5) — the read-side counterpart to the key-codec's insert-side normalization test above. **Closed out.** The four gaps left open at the previous checkpoint are now each covered by a dedicated test: a `CREATE TABLE` with an over-precise/zero-precision/scale-exceeding-precision `DECIMAL` column rejected at bind time (`crates/htap-sql/tests/parse_bind.rs::test_bind_decimal_ddl_types_and_validation`), a decimal primary key inserted once and matched by both a plain-numeric and a differently-quoted textual literal (`crates/htap-server/tests/decimal_pointread.rs::test_decimal_primary_key_collision_normalizes_textual_spellings`), a decimal-primary-key range scan through SQL returning numeric (not lexical) order across multiple rows (`::test_decimal_primary_key_range_scan_returns_numeric_order`), and a decimal value surviving a full server close-and-reopen (`::test_decimal_value_survives_server_restart`). **Task 4 (movement CSV/JSON-lines codec) landed.** `crates/htap-movement/src/codec.rs` parses and formats a `DECIMAL` column on both the CSV and JSON-lines paths, sharing task 1's `htap-common` helpers: a value with more fractional digits than the column's declared scale is rounded half-away-from-zero on import (matching ordinary `INSERT`/`UPDATE` assignment semantics, not rejected), a value that overflows the declared precision is still a hard error, and JSON-lines represents a decimal as a string, rejecting a bare JSON number outright (to keep a float round trip from mangling the value before it ever reaches `parse_decimal_text`). Covered by `crates/htap-movement/tests/decimal_codec.rs` (`test_csv_decimal_roundtrip_cases`, `test_jsonl_decimal_roundtrip_cases_emit_strings`, `test_jsonl_decimal_number_is_rejected_with_column_name`, `test_null_decimal_roundtrip_through_csv_and_jsonl`, `test_decimal_precision_overflow_is_rejected_by_csv_and_jsonl`, `test_decimal_import_rounds_extra_fractional_digits_half_away_from_zero`). See `docs/OPERATIONS.md` section 2 for the operator-facing statement of this rounding behavior. **Task 5 (MySQL wire, `NEWDECIMAL`) landed.** `crates/htap-wire/src/result_codec.rs` and `binary_codec.rs` describe and round-trip a `DECIMAL` column definition and its values over both the text and binary result-row protocols (not only as a bound parameter, as before); a prepared `NEWDECIMAL` parameter still substitutes as validated numeric text through the pre-existing literal path. Covered by `crates/htap-wire/src/result_codec.rs`'s own unit test (`result_codec::tests::decimal_column_definition_and_text_row_round_trip`), `crates/htap-wire/src/binary_codec.rs`'s own unit tests (`binary_codec::tests::{binary_row_decimal_round_trip_preserves_value_precision_and_scale, decode_execute_rejects_invalid_decimal_text}`), and `crates/htap-wire/tests/wire_server.rs::{test_wire_decimal_text_result_preserves_values_and_column_metadata, test_wire_prepared_decimal_param_round_trips_exactly_into_bigint_column}`. **Format-bump gate.** Per CLAUDE.md's durability rule and this project's own established practice of recording a format bump as a consequence of the ADR that owns the affected surface rather than minting a new ADR for a purely additive type addition, the `HTAPCOL1` v1 -> v2 bump, its backward-compatibility argument (verified by inspection of the encode/decode paths, since no genuine version-1 fixture is checked in — `legacy_format_still_readable` demonstrates a hand-patched footer tag still decodes, which is different from proving no historical v1 byte layout changed), the legacy-plus-decimal rejection guard, and the per-envelope reasoning for why `HTAPCAT1`/`HTAPSST1`/`HTAPMNF1`/the WAL/the rest of the whole-file envelopes did not need a matching bump (serde's strict closed-set enum-tag matching, contingent on those enums keeping plain derived serialization) are recorded under ADR-008 in `docs/DECISIONS.md`, explicitly signed off by storage review per this phase's binding plan edit. See `docs/ARCHITECTURE.md`'s Phase 17 paragraph and its "Column store (OLAP)" / storage-format-compatibility sections, and `docs/LIMITATIONS.md`'s matching entry for the disclosed gaps. |
| Phase 17 (continued) — TPC-H workload kit: prerequisites (Batch A) and query-kit foundations (Batch B checkpoint 1) | `In progress` | **Batch A (SQL/movement prerequisites, complete).** A7: `crates/htap-movement/src/codec.rs`'s timestamp import now accepts calendar date and full datetime text in addition to raw integer microseconds (raw-microseconds parsing is tried first, so nothing that worked before changes behavior); a datetime's time-of-day component requires exactly two digits per field, and an out-of-range hour/minute/second is a hard error rather than rolling into the next day; export formatting is unchanged, still raw microseconds. Covered by `crates/htap-movement/src/codec.rs::codec::tests::{test_timestamp_calendar_date_and_datetime_imports, test_timestamp_datetime_requires_two_digit_time_components}`. A8/A9: regression tests pin the canonical query text of the TPC-H correlated-subquery shapes this workload kit needs — queries 2, 4, 17, 20, 21, 22 — against the existing depth-1 correlated-subquery support (ADR-022), with no code change required: `crates/htap-server/tests/tpch_correlated_subqueries.rs::{test_tpch_q2_minimum_cost_supplier_per_part, test_tpch_q4_order_priority_with_late_lineitem, test_tpch_q17_small_quantity_order_revenue, test_tpch_q20_potential_part_promotion, test_tpch_q21_suppliers_with_mixed_delivery_orders, test_tpch_q22_revenue_by_customer_demographics}` (6 tests); each clause was proven load-bearing by deleting it and observing the specific test fail (four of the six originally passed with their clause deleted, which external review caught and the fixtures were extended to close). The doubly-referenced-CTE shape (query 15, both the simple and the aggregating reference) needed no code change either, verified by `crates/htap-tpch/src/queries.rs::queries::tests::test_all_22_queries_bind_and_execute` (binds and executes query 15's real text, not a correctness check) and, at smaller scale with a column list, `crates/htap-server/tests/tpch_sql_prerequisites.rs::test_cte_referenced_twice_with_column_list`. Reproduced specification query text now carries the TPC copyright/permission notice (`crates/htap-server/tests/tpch_correlated_subqueries.rs`'s own header, matching `crates/htap-tpch/src/queries.rs`'s). Covered by `crates/htap-server/tests/tpch_sql_prerequisites.rs::test_tpch_sql_prerequisites_parse_bind_and_execute_dates_and_strings` (DATE storage/comparison/arithmetic, `EXTRACT` of year/month/day, three-argument `SUBSTRING`) — see "General query executor scope and deferred features" in `docs/LIMITATIONS.md`. **Batch B checkpoint 1 (new `htap-tpch` crate, in progress — the foundation for the rest of Batch B: a data generator, bulk loader, refresh functions, and power/throughput drivers remain to be built).** `crates/htap-tpch` provides the eight-table TPC-H schema DDL in foreign-key dependency order (`schema::{TABLE_NAMES, ddl_statements}` — foreign keys are represented by columns only, never enforced as engine constraints; `crates/htap-tpch/tests/schema_ddl.rs::test_tpch_schema_ddl_creates_expected_catalog_schema`), all 22 published query texts with the specification's published validation-default substitution parameters (`queries::query`, `params::fixed_parameters` — these document the published defaults, not the official parameter-generation-and-validation algorithm; `params::tests::test_fixed_parameters_appear_in_query_texts` cross-checks every one of those defaults against the query text `queries::query` returns for the matching query number, matched in the syntactic position each value belongs to rather than as a loose substring — a whole-token numeric match (so, e.g., query 19's quantity `1` cannot match on an incidental `1` elsewhere in the query text), query 16's sizes checked as exact comma-delimited entries of its own `p_size` list (so a single-digit size cannot match inside one of its own two-digit sizes), and query 19's quantities checked against their own `l_quantity` lower-bound comparisons — catching drift between the parameter table and the query text, two independent transcriptions of the same published data, without itself validating either one against the specification; Query 11 preserves its exact `0.0001 / SF` formula as SQL text rather than pre-computing it through floating point — `queries::tests::test_query_11_preserves_fraction_scale_factor` extracts the divisor that actually appears in the generated `FRACTION` formula at scale factors `"1"`, `"2"`, and `"0.5"` and asserts it equals the supplied scale factor each time (checking only scale factor 1, as an earlier version of this test did, cannot distinguish a correct implementation from one that ignores the parameter entirely, since the divisor is 1 either way; non-unit and fractional values close that gap), then executes each generated statement against the schema; separately, `queries::tests::test_query_11_rejects_zero_scale_factor` proves only that an invalid, zero scale factor is rejected, a different property), and an exact-integer scale-factor helper (`scale_factor::scale_factor`, parsing decimal text into an `i128` numerator/denominator pair rather than through `f64`, avoiding a demonstrated float-rounding pitfall — `scale_factor::tests::test_scale_factor_exact_vs_float_discrimination` shows `0.29 * 50` landing on `14.499999999999998` in ordinary binary floating point, one below the correct `14.5`/`15`; 7 tests total). **Row-limiting mechanism (TPC-H Clause 2.1.2.9).** Queries 2, 3, 10, 18, and 21 carry `LIMIT 100`/`10`/`20`/`100`/`100` respectively (the row counts come from each query's own Functional Query Definition, Clause 2.4.N.2, not from its substitution-parameter defaults), satisfying the clause's exactly-first-N-rows requirement via the third of its three permitted mechanisms — vendor-specific SQL syntax added to the `SELECT` statement, as opposed to an interactive-interface control statement (mechanism 1, e.g. `SET ROWCOUNT n`) or an implementation-specific fetch-loop control (mechanism 2, e.g. `while rowcount <= n`, per Clause 6.2.4) — applied consistently to all five queries that need it and none of the other two mechanisms; see ADR-027. **Correctness fixtures, 5 of 22 queries validated.** `crates/htap-tpch/tests/fixture.rs` builds a small hand-authored dataset (region/nation/part/supplier/partsupp/customer/orders/lineitem rows inserted via SQL) and an ordered/unordered multiset-comparison helper (`compare_results`, with its own tests `test_multiset_validation`, `test_multiset_validation_accepts_permuted_rows`, `test_multiset_validation_rejects_decimal_precision_mismatch` proving it actually rejects a wrong multiset, accepts a permuted one, and distinguishes decimal precision). `crates/htap-tpch/tests/correctness_fixture.rs::{test_q1, test_q2, test_q3, test_q4, test_q6}` assert hand-derived expected results (row values, not just row counts) for queries 1, 2, 3, 4, and 6 — the other 17 queries are **not** correctness-validated by this checkpoint. `crates/htap-tpch/src/queries.rs::queries::tests::test_all_22_queries_bind_and_execute` is a materially weaker claim: it proves every one of the 22 queries binds and executes without error against the schema at scale factor 1, but does not check a single returned row for the 17 queries without a hand-derived fixture. **Three previously-undocumented SQL binder features this kit depends on, landed earlier in this same checkpoint (evidence recorded here for the first time):** derived-table column lists (`(SELECT ...) AS t(a, b)`), column lists on non-recursive CTEs (`WITH t(a, b) AS (...)`, closing a gap where this previously returned an explicit "not supported" error while `WITH RECURSIVE` already accepted one), and the `DATE(string)` function form alongside the existing `DATE 'yyyy-mm-dd'` literal syntax — see "General query executor scope and deferred features" in `docs/LIMITATIONS.md` for the full contract, both binders' column-list rename-before-uniqueness-check ordering, and the complete test list (`crates/htap-server/tests/tpch_sql_prerequisites.rs`). Workspace: `cargo test --workspace` — 1670 passed, 0 failed, 1 ignored; `./ci.sh` green. See `docs/ARCHITECTURE.md`'s "TPC-H workload kit foundations (Phase 17, Batch A / Batch B checkpoint 1)" section, `docs/DECISIONS.md`'s ADR-027, and `docs/LIMITATIONS.md`'s matching section for the full contract, remaining Batch B work, and disclosed gaps. A separate TPC-H compliance/deviations disclosure document (the disclaimer, deviations list, non-comparability statement, and naming rules) is a later Batch B deliverable and is not written yet. |

---

## Requirement → Evidence map

Each requirement must be backed by a named, runnable test or benchmark. This
table is the contract between the requirements and the test suite.

| Requirement | Proving test / benchmark | Status |
| ----------- | ------------------------ | ------ |
| R1 — Block skipping via zone maps (≥90% skip criterion) | `crates/htap-colstore/tests/zone_map_skip.rs` (`test_deterministic_zone_map_skip_acceptance`), and Criterion microbenchmark `colstore/equality_zone_map_scan` (`crates/htap-bench/benches/local_mvp.rs`, asserting 99/100 block skip). | `complete` |
| R2 — Storage-format conversion (row ↔ column) | Completed local Row-to-Column conversion MVP with crash-resumable state machine (`SnapshotPinned` -> `SegmentsWritten` -> `ReadyToPublish` -> `Column`), atomic per-tablet manifest and catalog publication, authoritative rowstore base-plus-delta overlay via converter APIs (`read_column_partition` and projection-aware compact reads `read_column_partition_compact`), table-wide conversion reports (`convert_table_to_column`), Column-to-Row metadata demotion (`convert_table_to_row`) retaining rowstore authority and column files on disk, explicit synchronous conversion policy ticks (`conversion_tick`, `tick`), and fail-closed startup validation on reopen: `crates/htap-convert/tests/tablet_manifest.rs`, `crates/htap-convert/tests/materialization.rs` (including `test_compact_read_multiblock_pruning_and_stats`, `test_compact_read_mutation_sequence_and_deterministic_order`, `test_compact_read_pre_base_and_row_format_equivalence`, `test_compact_read_validation_errors`, `test_demote_partition_to_row_clearing_manifest_and_retained_data`, `test_conversion_tick_resumes_snapshot_pinned`), `crates/htap-sql/tests/route.rs`, `crates/htap-server/tests/local_server.rs` (`test_server_convert_table_multi_partition_reports_and_demotion_equivalence`, `test_server_conversion_tick_idempotent_and_resume_snapshot_pinned`, `test_server_open_fail_closed_missing_or_corrupt_manifest`), catalog recovery conversion tests (`crates/htap-catalog/tests/catalog_recovery.rs`), and Criterion microbenchmark `convert/local_converter_row_to_column` (`crates/htap-bench/benches/local_mvp.rs`). Physical reverse data transcoding, physical reclamation of demoted column files, delete vectors, delta-to-base background columnar compaction, autonomous background conversion scheduling, vectorized aggregation, and distributed partition/table conversion are explicitly deferred. As of Phase 15, the rowstore's own generic LSM compaction (`Engine::compact_once`) and `DROP TABLE` artifact reclamation are implemented as a narrow local MVP, including for converted tables' rowstore history — see that row and ADR-024; this does not touch the columnar side (delete vectors, delta-to-base compaction remain deferred as above). | `partial (local MVP)` |
| R3 — Sharding, placement, replication and repair | Completed single-node tablet placement, snapshot clone package manifest verification (`HTAPMNF1`), replica repair via catalog CAS, deterministic placement planner (`plan_placement`), and coordinator-fenced local replica activation simulation; placement is metadata/planning/local simulation, not sharded SQL serving: `crates/htap-movement/tests/tablet_simulation.rs`, `crates/htap-server/tests/local_server.rs` (`test_server_data_mover_clone_verify_repair`), `crates/htap-coord/tests/placement_movement.rs`, and Criterion microbenchmarks `coord/placement_planning` and `movement/fixed_csv_import` (`crates/htap-bench/benches/local_mvp.rs`). Since Phase 9, `plan_placement` allocates new replica ids from the catalog's persisted identifier high-water mark (`snapshot.id_high_water().replica + 1`), not just the live maximum, because a `ReplicaId` names an on-disk movement package directory that must never be reissued: `crates/htap-coord/tests/placement_movement.rs::test_plan_placement_allocates_above_id_high_water`. Distributed multi-node sharding, physical sharded SQL serving, dynamic network rebalancing, live peer replication, and capacity/rack placement are deferred. | `partial (local MVP)` |
| R4 — SQL breadth (CTEs, window functions, subqueries, cost model) | Completed narrow local slice: MySQL-dialect parser and strict catalog binder with typed `AnalyticSelect` (`crates/htap-sql/tests/parse_bind.rs`), structural route classifier (`crates/htap-sql/tests/route.rs`), durable catalog recovery tests (`crates/htap-catalog/tests/catalog_recovery.rs`), synchronous `LocalServer` executing narrow analytical scans over logical rowstore and base-plus-delta rows across all partitions using server-root `<root>/colstore` with projection-aware compact reads (PK+requested column union, single safe predicate-leaf `SegmentReader` pushdown, rowstore delta suppression/overlay, deterministic PK ordering, and full residual SQL filter/aggregate/group evaluation; conservative finite range/list partition pruning, bounded in-process partition scan workers, and deterministic global merge/order are implemented for narrow local OLAP; distributed fanout, disk spilling, query cancellation, and resource quotas remain deferred), and native partition execution (`LocalServer::create_partitioned_table`, `LocalServer::alter_partitions`) supporting multi-row `INSERT` across partitions in one commit version, complete-PK `DELETE` and `SELECT` (preserving rowstore fast path), covered by `crates/htap-server/tests/local_server.rs` (`test_analytic_select_row_and_column_formats`, `test_analytic_materialized_column_base_plus_delta`, `test_analytic_row_vs_column_base_plus_delta_equivalence`, `test_pushdown_predicate_selection_and_pruning_stats`, `test_partitioned_native_range_topology_catalog_reopen_continuation`, `test_partitioned_native_list_topology_catalog_reopen_continuation`, `test_partitioned_boundary_unmatched_null_type_errors`, `test_partitioned_multi_row_insert_spanning_partitions_one_version_point_delete`, `test_partitioned_composite_pk_partition_key_not_first`, `test_partitioned_olap_across_partitions_and_empty_aggregate`, `test_partition_pruning_range_and_list_and_conservative_cases`, `test_scan_worker_count_equivalence`, `test_multi_partition_order_by_directions_nulls_and_tie_breaking`, `test_convert_table_multi_partition_guard`, `test_partitioned_empty_topology_rejection_no_catalog_mutation`, `test_server_sql_alter_partition_lifecycle`, `test_server_alter_partitions_drop_empty_and_populated_guard`, `test_server_alter_partitions_reorganize_empty_and_populated_guard`), `crates/htap-convert/tests/materialization.rs` (`test_compact_read_multiblock_pruning_and_stats`, `test_compact_read_mutation_sequence_and_deterministic_order`), and synchronous `EmbeddedClient` (`crates/htap-client/tests/embedded_client.rs`) supporting `CREATE TABLE` (unpartitioned default or partitioned), typed `ALTER TABLE` partition lifecycle (`ADD`, `DROP`, `REORGANIZE` on empty sources), literal `INSERT`, PK `DELETE`, complete-PK `SELECT`, and narrow OLAP queries with reopen recovery and structured error mapping. Internal `ScanStats`/pruning evidence is captured, but SQL execution operates on materialized logical rows. Direct `SegmentReader` pushdown optimization is implemented for the compact base path. Proven SQL partition support: MySQL `CREATE TABLE ... PARTITION BY RANGE [COLUMNS]` and `PARTITION BY LIST [COLUMNS]` (including `VALUES LESS THAN MAXVALUE`) and `ALTER TABLE ... ADD/DROP/REORGANIZE PARTITION` are parsed via vendored `sqlparser` and bound to validated catalog models (`crates/htap-sql/tests/parse_bind.rs`: `test_mysql_partition_ddl_parsed_and_bound`, `test_mysql_partition_ddl_negative_parser_and_binder`, `test_mysql_alter_partition_parsed_and_bound`, `test_mysql_alter_partition_negative`), while binder rejects manual/unsupported partition ASTs (`test_negative_create_table`), avoiding lossy reinterpretation. Format conversion is guarded to single-partition tables for `convert_table`. Compound `AND` pushdown beyond one leaf and `!=` pushdown remain evaluated as residual SQL filters, and vectorized aggregation / operator pipelines remain deferred for the narrow `AnalyticSelect` path; simple unqualified source/projected column `ORDER BY` with ASC/DESC and NULLS FIRST/LAST/default policy, global deterministic tie-break is implemented for `AnalyticSelect`. **Phase 9 closed most of this row's remaining gap.** A general query executor (`htap-sql::{query, expr, binder_query}`, `htap-server::query_exec`, `Route::Query`) now implements joins (`INNER`/`LEFT`/`RIGHT`/`CROSS`, left-deep chains, comma joins), table aliases, qualified names, arithmetic/comparison/`AND`/`OR`/`NOT`/`IS [NOT] NULL`/`TRUE`/`FALSE`/`LIKE`/`IN`/`BETWEEN`/`CASE`/`CAST` expressions, scalar functions, aggregates (`COUNT`/`SUM`/`AVG`/`MIN`/`MAX` with `DISTINCT`), `GROUP BY`/`HAVING`, `SELECT DISTINCT`, `ORDER BY` of expressions/aliases/ordinals, `LIMIT`/`OFFSET`, `UNION`/`UNION ALL`, derived tables, non-recursive `WITH` CTEs, and uncorrelated scalar/`IN`/`EXISTS` subqueries — every base table side of a join materialized through the same per-partition storage path at one MVCC snapshot per statement, so a join across a `Row` table and a converted `Column`/`Converting` table is consistent. A purely syntactic shape gate (`is_narrow_select_shape`) keeps the narrow `AnalyticSelect`/`Route::OlapScan` path (and R5's `PointSelect`/`Route::RowstorePointRead`) unchanged and structurally isolated. `UPDATE` and `DROP TABLE` are also implemented (`Route::RowstoreUpdate`, `Route::CatalogDdl`). Verified in `crates/htap-sql/src/expr.rs` unit tests (`three_valued_logic_tables`, `numeric_promotion_and_overflow`, `like_in_between_case_cast`, `scalar_functions`, `subquery_and_aggregate_context`, `expr_type_inference`), `crates/htap-sql/tests/query_bind.rs` (`test_join_binding_kinds_aliases_and_wildcards`, `test_join_binding_errors`, `test_expressions_functions_and_type_checks`, `test_aggregates_group_by_having_and_grouping_rules`, `test_order_by_limit_distinct`, `test_subqueries_ctes_derived_tables_and_union`, `test_update_drop_show_binding`, `test_bound_predicate_evaluation_with_joined_rows`), and `crates/htap-server/tests/query_exec.rs` (`test_joins_across_row_column_and_converting_tables`, `test_outer_joins_null_padding_residual_on_and_null_keys`, `test_expressions_aggregates_having_order_limit_distinct`, `test_union_derived_tables_ctes_and_subqueries`, `test_partition_pruning_and_pushdown_through_general_path`, `test_single_snapshot_across_engines_and_freshness`, `test_general_query_over_reopened_server`). **Phase 13 closed the rest of this row's SQL-breadth gap** (see that row for the full evidence map): window functions (`ROWS`/peer `RANGE`/value-offset `RANGE` frames, evaluated post-`HAVING`), correlated subqueries (one level deep, with a bounded per-statement invocation/nesting budget), `FULL OUTER`/`NATURAL`/`USING` joins with real column coalescing, arbitrarily nested parenthesized join trees (a purely structural `JoinTree`, pinned equivalent to the existing flat executor by a mandatory differential test), `WITH RECURSIVE` (one self-referencing CTE, capped iterations/rows/bytes), `EXCEPT`/`INTERSECT`, `GROUP BY`/`ORDER BY` ordinals, integer `DIV`, `DELETE` by arbitrary filter, `TRUNCATE` (as a transactional `DELETE`), and `INSERT ... SELECT` (exact per-column type match) are now all implemented. **Phase 14 added the cost model this row named as outstanding** (`docs/PROGRESS.md`'s Phase 14 row, ADR-023): `ANALYZE TABLE` (exact per-table/per-column statistics, capped distinct count, catalog format v3 -> v4), a storage-agnostic `htap-sql::optimize` stage (statistics-driven cost estimators with disclosed provenance, predicate-atom reordering with a conservation validator, subset-DP join reordering up to 8 relations then greedy) enabled by default for `Route::Query`, `EXPLAIN`/`EXPLAIN ANALYZE` (verified to bypass the optimizer entirely for `Route::RowstorePointRead`/`Route::OlapScan`), a per-statement memory budget with disk spilling for hash joins/`GROUP BY`/`ORDER BY`/set operators/windows, and bounded intra-query parallelism for `GROUP BY` and `INNER`/`CROSS` joins. Still deferred: vectorized/pipelined execution (the optimizer picks a physical plan shape and a build side; it does not change the row-at-a-time evaluator into a vectorized one), histograms (only single-column min/max/null-count/capped-exact-distinct), table-level-only statistics (no per-partition breakdown), automatic staleness detection, and parallel execution of `LEFT`/`RIGHT`/`FULL` joins (structurally restricted to `INNER`/`CROSS`; hash-join spilling under memory pressure is not itself kind-restricted in code but is only covered by an `INNER`-join test — see the Phase 14 row's disclosed evidence gap). Non-partition `ALTER`, hash buckets / tablet sharding, populated partition reorganization data migration, physical storage reclamation for `ALTER TABLE ... DROP/REORGANIZE PARTITION` (empty partitions only, so currently inert) and for demoted column files, multi-tablet/distributed scans, query cancellation, and DataFusion/Arrow remain deferred (`DROP TABLE`'s own artifacts are physically reclaimed as of Phase 15, see that row). | `partial (local MVP)` |
| R5 — Mixed OLTP/OLAP: point lookups bypass the analytical engine | Row-store primary-key fast path: `crates/htap-rowstore/tests/engine.rs`, `crates/htap-rowstore/tests/mvcc_properties.rs`, and `crates/htap-rowstore/tests/engine_crash.rs`. Complete-PK `SELECT` strictly binds to `PointSelect` and routes to `Route::RowstorePointRead { key }` bypassing analytical planning and converter execution, remaining completely separate and unchanged. Verified in `crates/htap-sql/tests/route.rs`, `crates/htap-sql/tests/parse_bind.rs`, `crates/htap-server/tests/local_server.rs`, `crates/htap-client/tests/embedded_client.rs`, and Criterion microbenchmark `rowstore/point_get_stable_snapshot` (`crates/htap-bench/benches/local_mvp.rs`). Analytical queries route separately to `Route::OlapScan`. Since Phase 9, this separation holds even with a general query executor present (joins, expressions, subqueries — see R4): `bind_select` applies a purely syntactic shape test (`is_narrow_select_shape`, `crates/htap-sql/src/binder.rs`) *before* any deep binding, so a complete-PK lookup keeps taking `Route::RowstorePointRead` and a join/alias/`LIMIT`/subquery on what looks like a PK lookup fails the shape test and binds through `Route::Query` instead — clauses are never silently dropped. Pinned by `crates/htap-sql/tests/route.rs::test_point_read_fast_path_pinned_against_general_query_path`, which asserts complete-PK-shaped queries still bind `PointSelect` -> `Route::RowstorePointRead` while near-miss shapes with the same PK predicate plus a `LIMIT`, alias, `OR`, join, or arithmetic projection fail the shape gate and bind `BoundStatement::Query` -> `Route::Query` instead. Vectorized SQL pipelines and distributed scans remain deferred; cost-based planning and bounded intra-query parallelism for the general query path were delivered in Phase 14 (`GROUP BY` and `INNER`/`CROSS` hash joins only — see that row and ADR-023) without weakening this row's separation: `Route::RowstorePointRead`/`Route::OlapScan` never call `htap_sql::optimize::optimize` (verified by name in `crates/htap-server/tests/explain.rs`'s two R5-bypass tests), and worker-pool parallelism for `LEFT`/`RIGHT`/`FULL` and non-equi/`CROSS` joins remains deferred. | `partial` |
| R6 — Fencing tokens prevent stale-leader split-brain | Completed local single-node coordinator and fenced placement MVP: durable synchronous `Coordinator` and `LocalCoordinator` persisting `HTAPCRD1` state envelopes with atomic publish, sorted membership, strictly monotonic fencing tokens, coordinator-fenced catalog compare-and-set (`fenced_catalog_compare_and_set`), deterministic placement planning (`plan_placement`), and local activation simulation (`activate_placement_addition`, `activate_placement_plan`); placement is metadata/planning/local simulation, not sharded SQL serving. Verified by tests in `crates/htap-coord/tests/local_coordinator.rs` (`test_stale_fence_rejection`, `test_fenced_catalog_cas_stale_and_current`, `test_token_monotonicity_and_reopen_no_reuse`), `crates/htap-coord/tests/placement_movement.rs` (`test_leadership_turnover_stale_token_rejects`, `test_corrupt_or_fence_failure_leaves_target_unready`, `test_target_staged_unhealthy_nonleader`, `test_clone_verify_then_healthy_activation`), movement tablet simulation tests (`crates/htap-movement/tests/tablet_simulation.rs`), and Criterion microbenchmark `coord/leadership_fenced_cas` (`crates/htap-bench/benches/local_mvp.rs`). Local same-root exclusive ProcessLock/cross-process contention rejection is implemented; direct `CatalogStore` CAS and older movement repair APIs bypass coordinator fence; distributed consensus (Raft/openraft), ZooKeeper backend, watches/locks/KV semantics, distributed/cross-node exclusion/consensus, remote physical movement, leader handoff, ongoing replication, capacity/rack placement, and live rebalance are not implemented. | `partial (local MVP)` |

> **This table must be filled with concrete test names before the project can
> be considered done.** A requirement whose evidence column still reads
> `pending` is an unproven requirement, regardless of whether code exists that
> appears to implement it.
