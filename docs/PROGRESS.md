# Progress

## Phases

| Phase | Status | Summary |
| ----- | ------ | ------- |
| Phase 0 — Research and workspace bootstrap | `Complete` | StarRocks source research completed and written up in [`RESEARCH.md`](./RESEARCH.md), covering the mechanisms adopted, those deliberately rejected, and the reasoning for each. A ten-crate cargo workspace was bootstrapped with crate boundaries drawn deliberately along the two seams that matter: the frontend/backend split and the row-format/column-format storage split, so that both remain deployment and routing choices rather than rewrites. `htap-common` carries the shared MVCC `Version` domain and the `FencingToken` type, together with the common error enum — these are shared by every other crate and so were built first. `ci.sh` runs `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo build --workspace` and `cargo test --workspace`; all four stages are green on a clean rebuild. |
| Phase 1 — Row store | `Complete` | Built the LSM row-store engine (`htap-rowstore`) with WAL, memtable, immutable SSTs, manifest tracking, snapshot isolation, and point lookups. Covered by unit tests, integration tests, bounded property-based reference model tests, and process-level crash tests: `wal_recovery` (record framing, torn-tail repair, atomicity, GC), `wal_crash` (`kill_9_loses_no_committed_data`, `a_cleanly_exiting_child_recovers_every_commit`), `sst` (block index, Bloom filter, multi-block iter, CRC corruption detection), `engine` (`test_commit_and_snapshot_visibility`, `test_reopen_preserves_data_and_version`, `test_first_writer_wins_conflict`, `test_delete_resurrection_regression`, `test_auto_flush_and_manual_flush`, `test_flush_then_reopen_empty_wal`), `mvcc_properties` (`prop_mvcc_engine_matches_model` proving snapshot stability, no future visibility, delete correctness, first-writer-wins, and recovery equivalence), and `engine_crash` (`engine_kill_9_recovers_all_reported_commits`, `engine_cleanly_exiting_child_recovers_every_commit`). |
| Phase 2 — Column store | `Complete` | Built the columnar storage engine (`htap-colstore`) with immutable binary segment layout, plain and dictionary encodings, optional zstd compression, CRC32C frame and footer checksums, typed per-block zone maps (min/max/nullability), and vectorized scans with selective column decoding and conservative pushdown pruning. Covered by unit tests, segment roundtrip and corruption tests (`crates/htap-colstore/tests/segment_roundtrip.rs`), vectorized scan reference tests (`crates/htap-colstore/tests/scan.rs`), and deterministic zone-map pruning acceptance test (`crates/htap-colstore/tests/zone_map_skip.rs`). |
| Phase 3 — SQL layer | `Complete (local MVP)` | Completed local slice: sqlparser MySQL dialect parsing, strict catalog binder with typed `PointSelect` and `AnalyticSelect`, structural route classifier (`Route::RowstorePointRead`, `Route::RowstoreWrite`, `Route::CatalogDdl`, `Route::OlapScan`), and durable catalog-backed synchronous `LocalServer` executing across unpartitioned tables (SQL `CREATE TABLE`) and partitioned tables created via SQL DDL (`CREATE TABLE ... PARTITION BY RANGE/LIST`) or native non-SQL API `LocalServer::create_partitioned_table` (finite Range/List topology). Supports multi-row `INSERT` routing across partitions in one commit version/payload, complete-PK `DELETE` and `SELECT` routed by partition key (preserving rowstore fast path), and narrow analytical scans over logical rowstore and base-plus-delta rows across all partitions using server-root `<root>/colstore` for materialized `Column`/`Converting` partitions with projection-aware compact reads (PK+requested column union, single safe predicate-leaf SegmentReader pushdown, delta suppression/overlay, deterministic PK ordering, and full residual SQL filter/aggregate/group evaluation; internal `ScanStats`/pruning evidence; logical rows used in SQL, vectorized aggregation deferred; conservative finite range/list partition pruning, bounded in-process partition scan workers, and deterministic global merge/order are implemented for narrow local OLAP; distributed fanout, disk spilling, query cancellation, and resource quotas remain deferred) with reopen recovery and `EmbeddedClient` façade. SQL partitioning verified: MySQL `CREATE TABLE ... PARTITION BY RANGE [COLUMNS]` and `PARTITION BY LIST [COLUMNS]` (including `VALUES LESS THAN MAXVALUE`) are parsed via vendored `sqlparser` and bound to validated catalog partition models; manual/generic `partition_by` ASTs and unsupported partition options/subpartitions/expressions/multi-column COLUMNS are rejected with clear errors. Typed SQL partition lifecycle DDL (`ALTER TABLE <table> ADD/DROP/REORGANIZE PARTITION`) is supported for strict finite range/list forms and final MAXVALUE where supported, routed to `CatalogDdl` and validated under `alter_partitions_internal` with rowstore snapshot collapse empty safety gates (populated DROP/REORGANIZE rejected). Native `LocalServer::alter_partitions` API remains available with candidate catalog validation, atomic CAS, empty safety, and checked ID allocation without ID burn. Catalog enforces non-null PK partition keys, half-open `[lower, upper)` range bounds, exact list values, and validates overlaps/duplicates/types. Format conversion is guarded to single-partition tables for `convert_table`. Covered by tests in `crates/htap-server/tests/local_server.rs` (including `test_partitioned_native_range_topology_catalog_reopen_continuation`, `test_partitioned_native_list_topology_catalog_reopen_continuation`, `test_partitioned_boundary_unmatched_null_type_errors`, `test_partitioned_multi_row_insert_spanning_partitions_one_version_point_delete`, `test_partitioned_composite_pk_partition_key_not_first`, `test_partitioned_olap_across_partitions_and_empty_aggregate`, `test_partition_pruning_range_and_list_and_conservative_cases`, `test_scan_worker_count_equivalence`, `test_multi_partition_order_by_directions_nulls_and_tie_breaking`, `test_convert_table_multi_partition_guard`, `test_partitioned_empty_topology_rejection_no_catalog_mutation`, `test_server_sql_alter_partition_lifecycle`, `test_server_alter_partitions_drop_empty_and_populated_guard`, `test_server_alter_partitions_reorganize_empty_and_populated_guard`), `crates/htap-catalog/tests/catalog_recovery.rs` (`test_partitioning_legacy_decode_and_reopen`, `test_range_partitioning_routing_and_boundaries`, `test_list_partitioning_routing`, `test_partitioning_duplicate_violations`, `test_range_overlap_and_order_violations`, `test_partitioning_type_and_null_violations`, `test_partitioning_ownership_and_method_consistency`, `test_partitioning_cas_and_reopen_lifecycle`, `test_partition_alteration_add_range_and_list`, `test_partition_alteration_drop_range_and_list`, `test_partition_alteration_reorganize_contiguous`, `test_partition_alteration_cas_and_reopen`), `crates/htap-sql/tests/parse_bind.rs` (including `test_mysql_partition_ddl_parsed_and_bound`, `test_mysql_partition_ddl_negative_parser_and_binder`, `test_mysql_alter_partition_parsed_and_bound`, `test_mysql_alter_partition_negative`, and `test_negative_create_table`), `crates/htap-sql/tests/route.rs`, `crates/htap-client/tests/embedded_client.rs`, and `crates/htap-convert/tests/materialization.rs`. Explicitly deferred at the time (joins, CTEs, expressions, `ORDER BY` expressions/aliases, `LIMIT`/`OFFSET`, `HAVING`, `OR`/`NOT`/arithmetic/casts, `AVG`/`DISTINCT` aggregates, and non-PK DML (`UPDATE`, `DROP TABLE`) were delivered in Phase 9, see that row): populated partition reorganization data migration, physical data reclamation for dropped partitions, multi-partition movement, hash tablets, distributed serving/failover, window functions, compound AND pushdown beyond one leaf, `!=` pushdown, vectorized aggregation / operator pipelines, hash buckets / tablet sharding, multi-tablet/distributed scans, quotas/spill/cancellation, DataFusion/Arrow, full MySQL breadth, sessions/`BEGIN`/`COMMIT`/`ROLLBACK`, and non-partition `ALTER`. |
| Phase 4 — HTAP conversion | `Complete (local MVP)` | Built the local partition-scoped row-to-column conversion engine (`htap-convert`) with durable columnar manifest envelope (`HTAPTBM1`), deterministic four-phase state machine (`SnapshotPinned` -> `SegmentsWritten` -> `ReadyToPublish` -> `Column`), atomic per-tablet manifest and catalog publication, rowstore-authoritative base-plus-delta overlay via converter APIs (`read_column_partition` and projection-aware compact reads `read_column_partition_compact`, verified in `crates/htap-convert/tests/materialization.rs`) and server-root `<root>/colstore` analytical scans with direct SegmentReader pushdown optimization for the compact base path, and online point writes and point reads on converting and columnar partitions. Added deterministic table-wide conversion reports (`TableConversionReport`, `PartitionConversionReport`), Row->Column conversion per partition (`convert_table_to_column`), Column->Row metadata demotion (`convert_table_to_row`) retaining rowstore authority and leaving columnar files on disk, explicit synchronous conversion policy ticks (`conversion_tick`, `tick`), and fail-closed startup validation on reopen (`LocalServer::open`). Covered by tests in `crates/htap-convert/tests/tablet_manifest.rs`, `crates/htap-convert/tests/materialization.rs` (`test_demote_partition_to_row_clearing_manifest_and_retained_data`, `test_conversion_tick_resumes_snapshot_pinned`), `crates/htap-sql/tests/route.rs`, `crates/htap-server/tests/local_server.rs` (`test_server_convert_table_multi_partition_reports_and_demotion_equivalence`, `test_server_conversion_tick_idempotent_and_resume_snapshot_pinned`, `test_server_open_fail_closed_missing_or_corrupt_manifest`), and catalog recovery conversion tests (`crates/htap-catalog/tests/catalog_recovery.rs`: `test_conversion_metadata_and_manifest_roundtrip_and_reopen`, `test_invalid_conversion_combinations`, `test_invalid_column_manifest_and_paths`, `test_conversion_stale_cas`). Explicitly deferred: physical reverse data transcoding, physical rowstore/columnar reclamation, delete vectors, background compaction, autonomous background conversion scheduler, compound AND pushdown beyond one leaf, `!=` pushdown, vectorized aggregation / operator pipelines, and full multi-tablet/distributed partition/table conversion semantics. |
| Phase 5 — Data movement | `Complete (local MVP)` | Built single-node data movement engine (`htap-movement`) and server integration (`LocalServer::data_mover` / `LocalServerDataMover`). Supports durable job lifecycle tracking (`HTAPJOB1`), CSV and JSONLines import streaming, export partition materialization (exports materialize full logical partition before writing), batching and atomic commit replay, and tablet snapshot clone/verify/repair with catalog CAS reconciliation. Covered by tests in `crates/htap-movement/tests/jobs.rs`, `crates/htap-movement/tests/import_export.rs`, `crates/htap-movement/tests/tablet_simulation.rs`, and `crates/htap-server/tests/local_server.rs` (`test_server_data_mover_integration`, `test_server_data_mover_clone_verify_repair`). Distributed multi-node movement, SQL COPY grammar, and background replication are deferred. |
| Phase 6 — Distribution and coordination | `Complete (local MVP)` | Built local coordination, leadership fencing, deterministic placement, and replica activation MVP (`htap-coord`). Implemented synchronous `Coordinator` trait and durable `LocalCoordinator` backed by `HTAPCRD1` binary envelopes, sorted membership, strictly monotonic fencing tokens, and coordinator-fenced catalog CAS (`fenced_catalog_compare_and_set`). Implemented pure deterministic placement planner (`plan_placement`) and coordinator-fenced staging and local activation simulation (`stage_placement_addition`, `activate_placement_addition`, `activate_placement_plan`). Covered by integration tests in `crates/htap-coord/tests/local_coordinator.rs`, `crates/htap-coord/tests/placement_movement.rs`, movement tablet simulation tests in `crates/htap-movement/tests/tablet_simulation.rs`, and server/movement integration tests in `crates/htap-server/tests/local_server.rs`. Local same-root exclusive ProcessLock/cross-process contention rejection is implemented; explicitly deferred: direct `CatalogStore` CAS and older movement repair APIs bypass coordinator fence; no Raft/`openraft`, ZooKeeper backend, watches/locks/KV semantics, distributed/cross-node exclusion/consensus, remote physical movement, leader handoff, ongoing replication, capacity/rack placement, or live rebalance are implemented. |
| Phase 7 — Hardening, benchmarks, local MVP | `Complete (local evidence MVP)` | Built local microbenchmark suite (`htap-bench`, `benches/local_mvp.rs`) measuring rowstore point lookups (`rowstore/point_get_stable_snapshot`), colstore zone-map scans (`colstore/equality_zone_map_scan`, verifying 99% skip), conversion (`convert/local_converter_row_to_column`), CSV import (`movement/fixed_csv_import`), coordinator placement planning (`coord/placement_planning`), and coordinator leadership fenced CAS (`coord/leadership_fenced_cas`). Maintained synchronous in-process embedded client façade (`htap-client`, `EmbeddedClient`) covered by integration tests in `crates/htap-client/tests/embedded_client.rs`. Added root `README.md`, `docs/BENCHMARKS.md`, `docs/OPERATIONS.md`, and updated `ci.sh` with compile-only benchmark check (`cargo bench --workspace --no-run`). Deferred at the time: `htapd` daemon, MySQL wire protocol, network endpoints (delivered in Phase 8); Docker image/Compose, ZooKeeper/Raft backends, physical power-loss fsync testing, and TPC-C/TPC-H compliance remain deferred. |
| Phase 8 — Network server | `Complete (local MVP)` | Built `htap-wire`, a hand-written synchronous MySQL text-protocol server (`WireServer`, `WireServerConfig`) over `Arc<LocalServer>`: one accept thread plus one thread per connection (`std::net`, no async runtime), statements serialized by the server's own `execution_lock`, every statement auto-committed. Handshake v10 with `mysql_native_password` (auth-switch for other proposed plugins); `COM_QUERY` (text result sets), `COM_PING`, `COM_INIT_DB`, `COM_QUIT`; every other command (`COM_STMT_PREPARE`/`EXECUTE`, `COM_RESET_CONNECTION`, `COM_CHANGE_USER`, ...) answered with `ERR 1047`. Start-up compatibility shim (`SET` as no-op OK, `USE`, `SELECT 1`/`VERSION()`/`DATABASE()`/`@@sysvar[, ...]`). Result-set terminator always header `0xFE`; OK-packet `info` carries a private `version=<n>`/`version=none`/empty convention recovered by the client. Type mapping for `Bool`/`Int32`/`Int64`/`Float64`/`String`/`Bytes`/`Timestamp`; error mapping from `HtapError` categories to MySQL codes 1064/1146/1213/1235/1105 plus protocol codes 1045/1040/1049/1047. Payloads ≥16 MB close the connection. Built `htapd` (`crates/htapd`), a thin binary (`--root`, `--listen` default `127.0.0.1:3307`, `--max-connections` default 64, `--password`/`HTAPD_PASSWORD`) with no signal handler (storage recovers on next start per ADR-004/008/009) and a non-loopback bind warning. Built `htap-client::RemoteClient` (`connect`, `connect_with`, `execute`, `ping`, `server_version`, `close`) returning the same `StatementResult` as `EmbeddedClient`, with server errors mapped back by code. Security model: loopback bind by default, one implicit (unchecked) username, single shared password with no TLS (cleartext query/result traffic), non-cryptographic scramble RNG. Covered by unit tests in `crates/htap-wire/src/*.rs` (`sha1_empty_string`, `sha1_abc`, `sha1_multi_block`, `native_password_scramble_matches_test_vector`, `packet_round_trip`, `oversize_packet_rejected`, `lenenc_int_round_trip`, `lenenc_str_and_nul_str_round_trip`, `seq_counter_wraps_and_resets`, `read_fully_handles_partial_reads_and_stop_at_boundary_only`, `handshake_v10_round_trip`, `handshake_response41_round_trip_with_and_without_db`, `auth_switch_round_trip`, `error_map_all_htap_variants`, `err_packet_round_trip`, `shim_set_returns_ok`, `shim_version_comment_single_row`, `shim_multi_sysvar_with_aliases`, `shim_use_db`, `shim_passthrough_for_normal_sql`, `column_def_round_trip_all_types`, `text_row_null_and_bytes_are_raw_not_hex`, `text_row_numeric_and_timestamp_round_trip`, `datetime_text_conversions`, `command_ok_header_is_always_0x00_and_never_in_trans`, `terminator_header_is_always_0xfe_legacy_and_deprecated`, `config_defaults_are_loopback_only`, `command_info_convention`); `crates/htap-wire/tests/wire_server.rs` (23 tests including `test_handshake_empty_password_ok`, `test_handshake_wrong_password_rejected_1045`, `test_handshake_correct_password_ok`, `test_auth_switch_to_native_password`, `test_ssl_request_rejected_and_pre41_rejected`, `test_ddl_insert_point_select_round_trip`, `test_analytic_select_round_trip`, `test_typed_values_null_bytes_float_timestamp_round_trip`, `test_syntax_error_maps_to_1064`, `test_missing_table_maps_to_1146`, `test_too_many_connections_returns_1040`, `test_concurrent_connections_dense_versions`, `test_com_ping`, `test_com_init_db_known_and_unknown_db`, `test_shim_set_and_version_comment`, `test_shutdown_joins_and_frees_port`, `test_unknown_command_returns_1047`, `test_prepared_statement_command_rejected_cleanly`, `test_oversized_packet_closes_connection`, `test_legacy_eof_terminator_used_when_client_does_not_negotiate_deprecate_eof`, `test_resultset_packets_modern_vs_legacy`, `test_dml_versions_are_reported`, `test_mysql_crate_driver_interop` against a dev-only `mysql` crate v28 driver); and `crates/htap-client/tests/remote_client.rs::test_remote_client_matches_embedded_client_ddl_dml_select`, `crates/htap-client/src/remote.rs::decode_command_convention`. Deferred: TLS, compression, prepared statements/binary protocol, multi-statements/multi-results, sessions/`BEGIN`/`COMMIT`/`ROLLBACK`, per-user ACL/RBAC, Docker packaging, and a selectable single-role `htapd` mode; see ADR-016. |
| Phase 9 — SQL breadth and cross-engine joins | `Complete (local MVP)` | Built a general query executor (`htap-sql::{query, expr, binder_query}` binding `BoundStatement::Query(BoundQuery)`; `htap-server::query_exec` executing it via new `Route::Query`) supporting `INNER`/`LEFT`/`RIGHT`/`CROSS` joins (left-deep chains, comma joins), table aliases, qualified names, `*`/`t.*`, arithmetic (`+ - * / %`, `/` always `Float64`, checked integer overflow), comparisons (incl. column-vs-column, literal-on-left), `AND`/`OR`/`NOT`, `IS [NOT] NULL`/`TRUE`/`FALSE`, `LIKE`, `IN (list)`, `BETWEEN`, `CASE` (simple/searched, lazy), `CAST`, scalar functions (`UPPER`/`LOWER`/`LENGTH`/`CHAR_LENGTH`/`CONCAT`/`ABS`/`COALESCE`/`IFNULL`/`NULLIF`), aggregates (`COUNT`/`SUM`/`AVG`/`MIN`/`MAX` with `DISTINCT`), `GROUP BY`/`HAVING`, `SELECT DISTINCT`, `ORDER BY` (expressions/aliases/ordinals, `NULLS FIRST`/`LAST`), `LIMIT`/`OFFSET` (incl. MySQL `LIMIT off, cnt`), `UNION`/`UNION ALL` with numeric widening, derived tables, non-recursive `WITH` CTEs, and uncorrelated scalar/`IN`/`EXISTS` subqueries. Every base table side of a join is materialized through the existing `scan_partition_compact` path (rowstore for `Row`, columnar+delta overlay for `Column`/manifest-bearing `Converting`, rowstore fallback for `SnapshotPinned` `Converting`) at **one** MVCC snapshot per statement, then hash-joined in memory with per-slot partition pruning and single-leaf predicate pushdown (residual on the outer-join null-supplying side). A purely syntactic shape gate (`is_narrow_select_shape` in `crates/htap-sql/src/binder.rs`) preserves R5 structurally: complete-PK `SELECT` still binds `PointSelect` -> `Route::RowstorePointRead`, narrow single-table scans still bind `AnalyticSelect` -> `Route::OlapScan` unchanged, everything else routes to `Route::Query`. Added `UPDATE t [alias] SET ... [WHERE ...]` (`Route::RowstoreUpdate`: complete-PK point read-modify-write + one `Mutation::Put`, or scan-all-partitions-at-one-snapshot + one transaction for all rewrites, inheriting the 16 MiB 2PC payload cap with no chunking; PK/partition-key assignment, subqueries, and `UPDATE ... FROM`/`JOIN`/`ORDER BY`/`LIMIT` rejected), `DROP TABLE [IF EXISTS] t` (`Route::CatalogDdl`, one catalog CAS, metadata-only — rowstore/columnar data of dropped tablets is not physically reclaimed, refuses while any partition is `Converting`), and `SHOW TABLES [LIKE p]`/`SHOW DATABASES`/`SHOW COLUMNS FROM t`/`DESCRIBE t`/`DESC t` (`Route::CatalogRead`, answered from the catalog snapshot only). To guarantee dropped identifiers are never reissued, `CatalogSnapshot.id_high_water: IdHighWater { table, partition, tablet, replica }` (`crates/htap-catalog/src/model.rs`) is now persisted and every allocator draws from `max(persisted, live max) + 1`; the `HTAPCAT1` catalog envelope format version bumped 1 -> 2 (`crates/htap-catalog/src/local.rs`), a version-1 catalog still decodes (counters fall back to the live maximum) and is rewritten as version 2 on the next CAS, and a version-1-only binary refuses to open a version-2 catalog (and a version-2 payload missing `id_high_water` is rejected as corruption). `LocalServer::open` runs a one-time legacy migration (`migrate_legacy_id_high_water`) that seeds the tablet counter from the `colstore/` on-disk inventory (needed because a v1-era `ALTER TABLE ... DROP PARTITION` could leave tablet directories the live catalog maximum cannot see), `LocalCatalogStore::compare_and_set` rejects any successor whose high-water mark would regress, and `htap-coord::plan_placement`/`stage_placement_addition` allocate and raise replica ids from the same persisted mark (a `ReplicaId` names a movement package directory). Known gap: the legacy migration does not recover replica ids removed the same way; see `docs/LIMITATIONS.md`. Covered by `crates/htap-sql/src/expr.rs` unit tests (`three_valued_logic_tables`, `numeric_promotion_and_overflow`, `like_in_between_case_cast`, `scalar_functions`, `subquery_and_aggregate_context`, `expr_type_inference`), `crates/htap-sql/tests/query_bind.rs` (`test_join_binding_kinds_aliases_and_wildcards`, `test_join_binding_errors`, `test_expressions_functions_and_type_checks`, `test_aggregates_group_by_having_and_grouping_rules`, `test_order_by_limit_distinct`, `test_subqueries_ctes_derived_tables_and_union`, `test_update_drop_show_binding`, `test_bound_predicate_evaluation_with_joined_rows`), `crates/htap-sql/tests/route.rs` (`test_route_classification`, `test_point_read_fast_path_pinned_against_general_query_path`), `crates/htap-sql/tests/parse_bind.rs` (`test_negative_select_and_delete`, `test_bind_analytic_select_negative`), `crates/htap-server/tests/query_exec.rs` (`test_joins_across_row_column_and_converting_tables`, `test_outer_joins_null_padding_residual_on_and_null_keys`, `test_expressions_aggregates_having_order_limit_distinct`, `test_union_derived_tables_ctes_and_subqueries`, `test_partition_pruning_and_pushdown_through_general_path`, `test_single_snapshot_across_engines_and_freshness`, `test_general_query_over_reopened_server`, `test_update_by_primary_key_and_reopen_recovery`, `test_update_by_filter_across_partitions_and_storage_formats_with_reopen`, `test_update_by_primary_key_on_column_and_converting_partitions`, `test_show_tables_databases_columns_and_describe`, `test_drop_table_reopen_and_no_id_reuse`, `test_legacy_catalog_seeds_tablet_high_water_from_colstore_inventory`), `crates/htap-server/tests/local_server.rs` (`test_analytic_unsupported_clauses`, `test_storage_descriptors_dml_and_point_reads_and_unsupported_non_point`), `crates/htap-catalog/tests/catalog_recovery.rs` (`test_catalog_v1_envelope_decodes_and_counters_fall_back_to_live_max`, `test_catalog_id_high_water_prevents_reuse_after_removal`, `test_catalog_cas_rejects_regressing_id_high_water`, `test_catalog_v2_payload_without_id_high_water_is_rejected`, `test_corruption_and_truncation`), `crates/htap-coord/tests/placement_movement.rs::test_plan_placement_allocates_above_id_high_water`, `crates/htap-client/tests/embedded_client.rs::test_embedded_client_unsupported_sql_preserves_error_categories`, `crates/htap-client/tests/remote_client.rs::test_remote_client_matches_embedded_client_ddl_dml_select`, and `crates/htap-wire/tests/wire_server.rs::test_general_sql_over_wire` (same SQL breadth reachable unchanged over the MySQL wire protocol). Deferred: window functions (`OVER`), correlated subqueries, `FULL OUTER`/`NATURAL`/`USING` joins, parenthesized nested join trees, recursive CTEs, `EXCEPT`/`INTERSECT`, `GROUP BY` ordinals, `LIMIT BY`, `INSERT ... SELECT`, `UPDATE` with joins/subqueries, filtered `DELETE`, `TRUNCATE`, non-partition `ALTER`, cost-based optimization, vectorized/pipelined execution, worker-pool parallelism for the general path, memory bounds/spilling, physical reclamation on `DROP TABLE`, semi-join rewrites of `IN`/`EXISTS`, integer `DIV`, broader string/date function coverage, and MySQL implicit string<->number coercion. |

---

## Requirement → Evidence map

Each requirement must be backed by a named, runnable test or benchmark. This
table is the contract between the requirements and the test suite.

| Requirement | Proving test / benchmark | Status |
| ----------- | ------------------------ | ------ |
| R1 — Block skipping via zone maps (≥90% skip criterion) | `crates/htap-colstore/tests/zone_map_skip.rs` (`test_deterministic_zone_map_skip_acceptance`), and Criterion microbenchmark `colstore/equality_zone_map_scan` (`crates/htap-bench/benches/local_mvp.rs`, asserting 99/100 block skip). | `complete` |
| R2 — Storage-format conversion (row ↔ column) | Completed local Row-to-Column conversion MVP with crash-resumable state machine (`SnapshotPinned` -> `SegmentsWritten` -> `ReadyToPublish` -> `Column`), atomic per-tablet manifest and catalog publication, authoritative rowstore base-plus-delta overlay via converter APIs (`read_column_partition` and projection-aware compact reads `read_column_partition_compact`), table-wide conversion reports (`convert_table_to_column`), Column-to-Row metadata demotion (`convert_table_to_row`) retaining rowstore authority and column files on disk, explicit synchronous conversion policy ticks (`conversion_tick`, `tick`), and fail-closed startup validation on reopen: `crates/htap-convert/tests/tablet_manifest.rs`, `crates/htap-convert/tests/materialization.rs` (including `test_compact_read_multiblock_pruning_and_stats`, `test_compact_read_mutation_sequence_and_deterministic_order`, `test_compact_read_pre_base_and_row_format_equivalence`, `test_compact_read_validation_errors`, `test_demote_partition_to_row_clearing_manifest_and_retained_data`, `test_conversion_tick_resumes_snapshot_pinned`), `crates/htap-sql/tests/route.rs`, `crates/htap-server/tests/local_server.rs` (`test_server_convert_table_multi_partition_reports_and_demotion_equivalence`, `test_server_conversion_tick_idempotent_and_resume_snapshot_pinned`, `test_server_open_fail_closed_missing_or_corrupt_manifest`), catalog recovery conversion tests (`crates/htap-catalog/tests/catalog_recovery.rs`), and Criterion microbenchmark `convert/local_converter_row_to_column` (`crates/htap-bench/benches/local_mvp.rs`). Physical reverse data transcoding, physical rowstore/columnar reclamation, delete vectors, background compaction, autonomous background conversion scheduling, vectorized aggregation, and distributed partition/table conversion are explicitly deferred. | `partial (local MVP)` |
| R3 — Sharding, placement, replication and repair | Completed single-node tablet placement, snapshot clone package manifest verification (`HTAPMNF1`), replica repair via catalog CAS, deterministic placement planner (`plan_placement`), and coordinator-fenced local replica activation simulation; placement is metadata/planning/local simulation, not sharded SQL serving: `crates/htap-movement/tests/tablet_simulation.rs`, `crates/htap-server/tests/local_server.rs` (`test_server_data_mover_clone_verify_repair`), `crates/htap-coord/tests/placement_movement.rs`, and Criterion microbenchmarks `coord/placement_planning` and `movement/fixed_csv_import` (`crates/htap-bench/benches/local_mvp.rs`). Since Phase 9, `plan_placement` allocates new replica ids from the catalog's persisted identifier high-water mark (`snapshot.id_high_water().replica + 1`), not just the live maximum, because a `ReplicaId` names an on-disk movement package directory that must never be reissued: `crates/htap-coord/tests/placement_movement.rs::test_plan_placement_allocates_above_id_high_water`. Distributed multi-node sharding, physical sharded SQL serving, dynamic network rebalancing, live peer replication, and capacity/rack placement are deferred. | `partial (local MVP)` |
| R4 — SQL breadth (CTEs, window functions, subqueries, cost model) | Completed narrow local slice: MySQL-dialect parser and strict catalog binder with typed `AnalyticSelect` (`crates/htap-sql/tests/parse_bind.rs`), structural route classifier (`crates/htap-sql/tests/route.rs`), durable catalog recovery tests (`crates/htap-catalog/tests/catalog_recovery.rs`), synchronous `LocalServer` executing narrow analytical scans over logical rowstore and base-plus-delta rows across all partitions using server-root `<root>/colstore` with projection-aware compact reads (PK+requested column union, single safe predicate-leaf `SegmentReader` pushdown, rowstore delta suppression/overlay, deterministic PK ordering, and full residual SQL filter/aggregate/group evaluation; conservative finite range/list partition pruning, bounded in-process partition scan workers, and deterministic global merge/order are implemented for narrow local OLAP; distributed fanout, disk spilling, query cancellation, and resource quotas remain deferred), and native partition execution (`LocalServer::create_partitioned_table`, `LocalServer::alter_partitions`) supporting multi-row `INSERT` across partitions in one commit version, complete-PK `DELETE` and `SELECT` (preserving rowstore fast path), covered by `crates/htap-server/tests/local_server.rs` (`test_analytic_select_row_and_column_formats`, `test_analytic_materialized_column_base_plus_delta`, `test_analytic_row_vs_column_base_plus_delta_equivalence`, `test_pushdown_predicate_selection_and_pruning_stats`, `test_partitioned_native_range_topology_catalog_reopen_continuation`, `test_partitioned_native_list_topology_catalog_reopen_continuation`, `test_partitioned_boundary_unmatched_null_type_errors`, `test_partitioned_multi_row_insert_spanning_partitions_one_version_point_delete`, `test_partitioned_composite_pk_partition_key_not_first`, `test_partitioned_olap_across_partitions_and_empty_aggregate`, `test_partition_pruning_range_and_list_and_conservative_cases`, `test_scan_worker_count_equivalence`, `test_multi_partition_order_by_directions_nulls_and_tie_breaking`, `test_convert_table_multi_partition_guard`, `test_partitioned_empty_topology_rejection_no_catalog_mutation`, `test_server_sql_alter_partition_lifecycle`, `test_server_alter_partitions_drop_empty_and_populated_guard`, `test_server_alter_partitions_reorganize_empty_and_populated_guard`), `crates/htap-convert/tests/materialization.rs` (`test_compact_read_multiblock_pruning_and_stats`, `test_compact_read_mutation_sequence_and_deterministic_order`), and synchronous `EmbeddedClient` (`crates/htap-client/tests/embedded_client.rs`) supporting `CREATE TABLE` (unpartitioned default or partitioned), typed `ALTER TABLE` partition lifecycle (`ADD`, `DROP`, `REORGANIZE` on empty sources), literal `INSERT`, PK `DELETE`, complete-PK `SELECT`, and narrow OLAP queries with reopen recovery and structured error mapping. Internal `ScanStats`/pruning evidence is captured, but SQL execution operates on materialized logical rows. Direct `SegmentReader` pushdown optimization is implemented for the compact base path. Proven SQL partition support: MySQL `CREATE TABLE ... PARTITION BY RANGE [COLUMNS]` and `PARTITION BY LIST [COLUMNS]` (including `VALUES LESS THAN MAXVALUE`) and `ALTER TABLE ... ADD/DROP/REORGANIZE PARTITION` are parsed via vendored `sqlparser` and bound to validated catalog models (`crates/htap-sql/tests/parse_bind.rs`: `test_mysql_partition_ddl_parsed_and_bound`, `test_mysql_partition_ddl_negative_parser_and_binder`, `test_mysql_alter_partition_parsed_and_bound`, `test_mysql_alter_partition_negative`), while binder rejects manual/unsupported partition ASTs (`test_negative_create_table`), avoiding lossy reinterpretation. Format conversion is guarded to single-partition tables for `convert_table`. Compound `AND` pushdown beyond one leaf and `!=` pushdown remain evaluated as residual SQL filters, and vectorized aggregation / operator pipelines remain deferred for the narrow `AnalyticSelect` path; simple unqualified source/projected column `ORDER BY` with ASC/DESC and NULLS FIRST/LAST/default policy, global deterministic tie-break is implemented for `AnalyticSelect`. **Phase 9 closed most of this row's remaining gap.** A general query executor (`htap-sql::{query, expr, binder_query}`, `htap-server::query_exec`, `Route::Query`) now implements joins (`INNER`/`LEFT`/`RIGHT`/`CROSS`, left-deep chains, comma joins), table aliases, qualified names, arithmetic/comparison/`AND`/`OR`/`NOT`/`IS [NOT] NULL`/`TRUE`/`FALSE`/`LIKE`/`IN`/`BETWEEN`/`CASE`/`CAST` expressions, scalar functions, aggregates (`COUNT`/`SUM`/`AVG`/`MIN`/`MAX` with `DISTINCT`), `GROUP BY`/`HAVING`, `SELECT DISTINCT`, `ORDER BY` of expressions/aliases/ordinals, `LIMIT`/`OFFSET`, `UNION`/`UNION ALL`, derived tables, non-recursive `WITH` CTEs, and uncorrelated scalar/`IN`/`EXISTS` subqueries — every base table side of a join materialized through the same per-partition storage path at one MVCC snapshot per statement, so a join across a `Row` table and a converted `Column`/`Converting` table is consistent. A purely syntactic shape gate (`is_narrow_select_shape`) keeps the narrow `AnalyticSelect`/`Route::OlapScan` path (and R5's `PointSelect`/`Route::RowstorePointRead`) unchanged and structurally isolated. `UPDATE` and `DROP TABLE` are also implemented (`Route::RowstoreUpdate`, `Route::CatalogDdl`). Verified in `crates/htap-sql/src/expr.rs` unit tests (`three_valued_logic_tables`, `numeric_promotion_and_overflow`, `like_in_between_case_cast`, `scalar_functions`, `subquery_and_aggregate_context`, `expr_type_inference`), `crates/htap-sql/tests/query_bind.rs` (`test_join_binding_kinds_aliases_and_wildcards`, `test_join_binding_errors`, `test_expressions_functions_and_type_checks`, `test_aggregates_group_by_having_and_grouping_rules`, `test_order_by_limit_distinct`, `test_subqueries_ctes_derived_tables_and_union`, `test_update_drop_show_binding`, `test_bound_predicate_evaluation_with_joined_rows`), and `crates/htap-server/tests/query_exec.rs` (`test_joins_across_row_column_and_converting_tables`, `test_outer_joins_null_padding_residual_on_and_null_keys`, `test_expressions_aggregates_having_order_limit_distinct`, `test_union_derived_tables_ctes_and_subqueries`, `test_partition_pruning_and_pushdown_through_general_path`, `test_single_snapshot_across_engines_and_freshness`, `test_general_query_over_reopened_server`). Window functions (`OVER`), correlated subqueries, a cost-based optimizer/planner, vectorized/pipelined execution, and worker-pool parallelism for the general query path remain deferred — this is still a general query executor over materialized rows, not a cost-based optimizer. `INSERT ... SELECT`, non-partition `ALTER`, hash buckets / tablet sharding, populated partition reorganization data migration, physical storage reclamation for dropped partitions, multi-tablet/distributed scans, quotas/spill/cancellation, and DataFusion/Arrow remain deferred. | `partial (local MVP)` |
| R5 — Mixed OLTP/OLAP: point lookups bypass the analytical engine | Row-store primary-key fast path: `crates/htap-rowstore/tests/engine.rs`, `crates/htap-rowstore/tests/mvcc_properties.rs`, and `crates/htap-rowstore/tests/engine_crash.rs`. Complete-PK `SELECT` strictly binds to `PointSelect` and routes to `Route::RowstorePointRead { key }` bypassing analytical planning and converter execution, remaining completely separate and unchanged. Verified in `crates/htap-sql/tests/route.rs`, `crates/htap-sql/tests/parse_bind.rs`, `crates/htap-server/tests/local_server.rs`, `crates/htap-client/tests/embedded_client.rs`, and Criterion microbenchmark `rowstore/point_get_stable_snapshot` (`crates/htap-bench/benches/local_mvp.rs`). Analytical queries route separately to `Route::OlapScan`. Since Phase 9, this separation holds even with a general query executor present (joins, expressions, subqueries — see R4): `bind_select` applies a purely syntactic shape test (`is_narrow_select_shape`, `crates/htap-sql/src/binder.rs`) *before* any deep binding, so a complete-PK lookup keeps taking `Route::RowstorePointRead` and a join/alias/`LIMIT`/subquery on what looks like a PK lookup fails the shape test and binds through `Route::Query` instead — clauses are never silently dropped. Pinned by `crates/htap-sql/tests/route.rs::test_point_read_fast_path_pinned_against_general_query_path`, which asserts complete-PK-shaped queries still bind `PointSelect` -> `Route::RowstorePointRead` while near-miss shapes with the same PK predicate plus a `LIMIT`, alias, `OR`, join, or arithmetic projection fail the shape gate and bind `BoundStatement::Query` -> `Route::Query` instead. Vectorized SQL pipelines and distributed scans remain deferred; worker-pool parallelism and cost-based planning for the general query path remain deferred. | `partial` |
| R6 — Fencing tokens prevent stale-leader split-brain | Completed local single-node coordinator and fenced placement MVP: durable synchronous `Coordinator` and `LocalCoordinator` persisting `HTAPCRD1` state envelopes with atomic publish, sorted membership, strictly monotonic fencing tokens, coordinator-fenced catalog compare-and-set (`fenced_catalog_compare_and_set`), deterministic placement planning (`plan_placement`), and local activation simulation (`activate_placement_addition`, `activate_placement_plan`); placement is metadata/planning/local simulation, not sharded SQL serving. Verified by tests in `crates/htap-coord/tests/local_coordinator.rs` (`test_stale_fence_rejection`, `test_fenced_catalog_cas_stale_and_current`, `test_token_monotonicity_and_reopen_no_reuse`), `crates/htap-coord/tests/placement_movement.rs` (`test_leadership_turnover_stale_token_rejects`, `test_corrupt_or_fence_failure_leaves_target_unready`, `test_target_staged_unhealthy_nonleader`, `test_clone_verify_then_healthy_activation`), movement tablet simulation tests (`crates/htap-movement/tests/tablet_simulation.rs`), and Criterion microbenchmark `coord/leadership_fenced_cas` (`crates/htap-bench/benches/local_mvp.rs`). Local same-root exclusive ProcessLock/cross-process contention rejection is implemented; direct `CatalogStore` CAS and older movement repair APIs bypass coordinator fence; distributed consensus (Raft/openraft), ZooKeeper backend, watches/locks/KV semantics, distributed/cross-node exclusion/consensus, remote physical movement, leader handoff, ongoing replication, capacity/rack placement, and live rebalance are not implemented. | `partial (local MVP)` |

> **This table must be filled with concrete test names before the project can
> be considered done.** A requirement whose evidence column still reads
> `pending` is an unproven requirement, regardless of whether code exists that
> appears to implement it.
